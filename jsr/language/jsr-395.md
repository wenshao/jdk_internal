# JSR 395: Records

> **状态**: ✅ Final | **JDK**: 16 | **链接**: https://jcp.org/en/jsr/detail?id=395

---

## 概述

JSR 395 定义了 Record (记录类) 的语言规范，用于简洁地声明不可变数据载体。

---

## 核心语法

```java
// 基本语法
public record Point(int x, int y) { }

// 使用
Point p = new Point(10, 20);
int x = p.x();
int y = p.y();
String str = p.toString();  // "Point[x=10, y=20]"
```

---

## 自动生成的方法

```java
public record Point(int x, int y) {
    // 自动生成:
    // - 构造器 Point(int x, int y)
    // - 访问器 x(), y()
    // - equals(Object)
    // - hashCode()
    // - toString()
}
```

---

## 自定义方法

```java
public record Point(int x, int y) {
    // 静态方法
    public static Point origin() {
        return new Point(0, 0);
    }
    
    // 实例方法
    public double distance() {
        return Math.sqrt(x * x + y * y);
    }
    
    // 紧凑构造器 (验证)
    public Point {
        if (x < 0 || y < 0) {
            throw new IllegalArgumentException("Coordinates must be non-negative");
        }
    }
}
```

---

## 实现接口

```java
public record Point(int x, int y) implements Comparable<Point> {
    @Override
    public int compareTo(Point other) {
        return Double.compare(this.distance(), other.distance());
    }
}
```

---

## 嵌套记录

```java
public record Order(
    long id,
    Customer customer,
    List<OrderItem> items,
    LocalDateTime createdAt
) { }

public record Customer(String name, String email) { }
public record OrderItem(String product, int quantity, BigDecimal price) { }
```

---

## 与其他特性结合

### 记录与模式匹配

```java
// instanceof 模式匹配
if (obj instanceof Point(int x, int y)) {
    System.out.println("x=" + x + ", y=" + y);
}

// switch 模式匹配
String describe = switch (obj) {
    case Point(int x, int y) -> "Point at (" + x + ", " + y + ")";
    default -> "Unknown";
};
```

### 记录与密封类

```java
public sealed interface Shape 
    permits Circle, Rectangle { }

public record Circle(double radius) implements Shape { }
public record Rectangle(double width, double height) implements Shape { }
```

---

## 底层实现

```java
// 编译后等价于:
public final class Point extends Record {
    private final int x;
    private final int y;
    
    public Point(int x, int y) { this.x = x; this.y = y; }
    public int x() { return x; }
    public int y() { return y; }
    // equals, hashCode, toString...
}
```

---

## 关联 JEP

| JEP | JDK | 说明 |
|-----|-----|------|
| JEP 359 | 14 | 预览 |
| JEP 384 | 15 | 第二次预览 |
| JEP 395 | 16 | 正式发布 |

---

## 相关链接

- [JSR 395 官方页面](https://jcp.org/en/jsr/detail?id=395)
- Record 指南 

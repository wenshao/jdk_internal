# JDK-8257912: Convert enum iteration to use range-based for loops

## 基本信息

| 属性 | 值 |
|------|-----|
| **Issue** | [JDK-8257912](https://bugs.openjdk.org/browse/JDK-8257912) |
| **PR** | [#1707](https://github.com/openjdk/jdk/pull/1707) |
| **Author** | iklam |
| **组织** | Oracle |
| **Created** | 2020-12-09 |
| **Merged** | 2020-12-10 |
| **审查天数** | 1 |
| **版本** | JDK 16 |
| **优先级** | P4 |
| **组件** | hotspot |
| **模块** | hotspot |
| **代码变更** | +95 / -48 (10 files) |
| **审查者** | gerard-ziemski, kimbarrett, tschatzl (3 人) |

## 描述

Now that [range-based for loops](https://github.com/openjdk/jdk/pull/1488) are allowed by the [HotSpot Style Guide](https://github.com/openjdk/jdk/blob/52ab72127ddc3ca6575d9d58503ec39c5dff7ab1/doc/hotspot-style.md), I have converted the more verbose syntax:

```
for (vmIntrinsicsIterator it = vmIntrinsicsRange.begin(); it != vmIntrinsicsRange.end(); ++it) {
  vmIntrinsicID index = *it;
  nt[as_int(index)] = string;
}
```

to

```
  for (vmIntrinsicID index : EnumRange<vmIntrinsicID>{}) {
    nt[as_int(index)] = string;
  }
```

I also removed the "convenient" declarations such as `vmIntrinsicsRange` and `vmIntrinsicsIterator` -- these are useful only when writing a traditional for loop, and are probably confusing to everyone else.  I've left examples in enumIterator.hpp in case anyone wants to write a traditional for loop.

I also added gtest cases for using range-based for loops with `EnumRange<>`.

<!-- Anything below this marker will be automatically updated, please do not edit manually! -->
---------
### Progress
- [x] Change must not contain extraneous whitespace
- [x] Commit message must refer to an issue
- [x] Change must be properly reviewed

### Issue
 * [JDK-8257912](https://bugs.openjdk.java.net/browse/JDK-8257912): Convert enum iteration to use range-based for loops


### Reviewers
 * [Kim Barrett](https://openjdk.java.net/census#kbarrett) (@kimbarrett - **Reviewer**) ⚠️ Review applies to 26ad3f0d125ad6ed118c1d9d9f1bf8c1174b0fbc
 * [Thomas Schatzl](https://openjdk.java.net/census#tschatzl) (@tschatzl - **Reviewer**) ⚠️ Review applies to 26ad3f0d125ad6ed118c1d9d9f1bf8c1174b0fbc
 * [Gerard Ziemski](https://openjdk.java.net/census#gziemski) (@gerard-ziemski - Committer) ⚠️ Review applies to 26ad3f0d125ad6ed118c1d9d9f1bf8c1174b0fbc


### Download
`$ git fetch https://git.openjdk.java.net/jdk pull/1707/head:pull/1707`
`$ git checkout pull/1707`

---

> **生成时间**: 2026-03-25

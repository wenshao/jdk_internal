# JDK-8326820: Metadata artificially kept alive

## 基本信息

| 属性 | 值 |
|------|-----|
| **Issue** | [JDK-8326820](https://bugs.openjdk.org/browse/JDK-8326820) |
| **PR** | [#19769](https://github.com/openjdk/jdk/pull/19769) |
| **Author** | xmas92 |
| **组织** | Oracle |
| **Created** | 2024-06-18 |
| **Merged** | 2024-06-27 |
| **审查天数** | 9 |
| **版本** | JDK 23 |
| **优先级** | P2 |
| **组件** | hotspot |
| **模块** | hotspot |
| **代码变更** | +55 / -49 (6 files) |
| **审查者** | coleenp, fisk, stefank, xmas92 (4 人) |

## 描述

ClassLoaderDataGraph provides APIs for walking different metadata. All the iterators which are not designed to be used by the GC also keep the holder of the CLDs alive and by extensions keeps all metadata alive. This is problematic for concurrent GC as it keeps otherwise unreachable classes from being unloaded and the respective metadata freed. 

This patch changes the default iteration behaviour to not keep the holder alive, with the exception of `loaded_classes_do` (renamed `loaded_classes_do_keepalive`) and `modules_do` (renamed `modules_do_keepalive`) which is used by jvmti APIs that requires that the holder is kept alive.

All other uses consumes all the metadata it queries during its safepoint or before releasing the `ClassLoaderDataGraph_lock`. 

Before this change some jcmd, new jfr chunks and some jfr events, all of which consumed these APIs, could cause class unloading to not occur. 

Been running our internal stress test in an even more stressful mode which without this patch reproduces the metaspace OOME [JDK-8326005](https://bugs.openjdk.org/browse/JDK-8326005) consistently within a few hours. And after this patch it does not.

Currently running tier1-tier8 testing.

<!-- Anything below this marker will be automatically updated, please do not edit manually! -->
---------
### Progress
- [x] Change must be properly reviewed (1 review required, with at least 1 [Reviewer](https://openjdk.org/bylaws#reviewer))
- [x] Change must not contain extraneous whitespace
- [x] Commit message must refer to an issue

### Issue
 * [JDK-8326820](https://bugs.openjdk.org/browse/JDK-8326820): Metadata artificially kept alive (**Bug** - P2)(⚠️ The fixVersion in this issue is [23] but the fixVersion in .jcheck/conf is 24, a new backport will be created when this pr is integrated.)


### Reviewers
 * [Erik Österlund](https://openjdk.org/census#eosterlund) (@fisk - **Reviewer**) ⚠️ Review applies to [0048f8bd](https://git.openjdk.org/jdk/pull/19769/files/0048f8bdd3ea2

## 变更文件

| 文件 | 状态 | +/- |
|------|------|-----|
| src/hotspot/share/classfile/classLoaderDataGraph.cpp | modified | +42/-41 |
| src/hotspot/share/classfile/classLoaderDataGraph.hpp | modified | +9/-4 |
| src/hotspot/share/classfile/classLoaderStats.cpp | modified | +1/-1 |
| src/hotspot/share/classfile/systemDictionary.hpp | modified | +1/-1 |
| src/hotspot/share/prims/jvmtiEnvBase.cpp | modified | +1/-1 |
| src/hotspot/share/prims/jvmtiGetLoadedClasses.cpp | modified | +1/-1 |

## 审查讨论

- **fisk**: Looks like this code exposes an OopHandle backed by the CLD handle area, which isn't a strong root that the GC will start tracing from. So it would se
- **fisk**: This one looks like it might not be safe to exposes without keeping the classes alive.
- **fisk**: Looks like we fetch the mirror with Klass::java_mirror() and not Klass::java_mirror_no_keepalive(). Its tempting to think that java_mirror will, unlik

---

> **生成时间**: 2026-03-25

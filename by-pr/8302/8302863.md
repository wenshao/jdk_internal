# JDK-8302863: Speed up String::encodeASCII using countPositives

## 基本信息

| 属性 | 值 |
|------|-----|
| **Issue** | [JDK-8302863](https://bugs.openjdk.org/browse/JDK-8302863) |
| **PR** | [#12640](https://github.com/openjdk/jdk/pull/12640) |
| **Author** | cl4es |
| **组织** | Oracle |
| **Created** | 2023-02-18 |
| **Merged** | 2023-02-21 |
| **审查天数** | 3 |
| **版本** | JDK 21 |
| **优先级** | P4 |
| **组件** | core-libs |
| **模块** | core-libs |
| **代码变更** | +22 / -13 (1 files) |
| **审查者** | AlanBateman, bokken, cl4es, schlosna (4 人) |

## 描述

When encoding Strings to US-ASCII we can speed up the happy path significantly by using `StringCoding.countPositives` as a speculative check for whether there are any chars that needs to be replaced by `'?'`. Once a non-ASCII char is encountered we fall back to the slow loop and replace as needed.

An alternative could be unrolling or using a byte array VarHandle, as show-cased by Brett Okken here: https://mail.openjdk.org/pipermail/core-libs-dev/2023-February/100573.html Having to replace chars with `?` is essentially an encoding error so it might be safe to assume this case is exceptional in practice.
<!--
Replace this text with a description of your pull request (also remove the surrounding HTML comment markers).
If in doubt, feel free to delete everything in this edit box first, the bot will restore the progress section as needed.
-->

<!-- Anything below this marker will be automatically updated, please do not edit manually! -->
---------
### Progress
- [x] Change must be properly reviewed (1 review required, with at least 1 [Reviewer](https://openjdk.org/bylaws#reviewer))
- [x] Change must not contain extraneous whitespace
- [x] Commit message must refer to an issue

### Issue
 * [JDK-8302863](https://bugs.openjdk.org/browse/JDK-8302863): Speed up String::encodeASCII using countPositives


### Reviewers
 * [Alan Bateman](https://openjdk.org/census#alanb) (@AlanBateman - **Reviewer**)
 * @bokken (no known openjdk.org user name / role)


### Reviewing
<details><summary>Using <code>git</code></summary>

Checkout this PR locally: \
`$ git fetch https://git.openjdk.org/jdk pull/12640/head:pull/12640` \
`$ git checkout pull/12640`

Update a local copy of the PR: \
`$ git checkout pull/12640` \
`$ git pull https://git.openjdk.org/jdk pull/12640/head`

</details>
<details><summary>Using Skara CLI tools</summary>

Checkout this PR locally: \
`$ git pr checkout 12640`

View PR using the GUI difftool: \
`$ git pr show -t 12640`

</details>
<details><summary>Using d

---

> **生成时间**: 2026-03-25

# JDK-8325495: C2: implement optimization for series of Add of unique value

## 基本信息

| 属性 | 值 |
|------|-----|
| **Issue** | [JDK-8325495](https://bugs.openjdk.org/browse/JDK-8325495) |
| **PR** | [#20754](https://github.com/openjdk/jdk/pull/20754) |
| **Author** | tabjy |
| **Created** | 2024-08-28 |
| **Merged** | 2024-10-09 |
| **审查天数** | 42 |
| **版本** | JDK 23 |
| **优先级** | P4 |
| **组件** | hotspot |
| **模块** | compiler/c2 |
| **代码变更** | +414 / -0 (3 files) |
| **审查者** | chhagedorn, rwestrel, tabjy (3 人) |

## 描述

This pull request resolves [JDK-8325495](https://bugs.openjdk.org/browse/JDK-8325495) by converting series of additions of the same operand into multiplications. I.e., `a + a + ... + a + a + a => n*a`.

As an added benefit, it also converts `C * a + a` into `(C+1) * a` and `a << C + a` into `(2^C + 1) * a` (with respect to constant `C`). This is actually a side effect of IGVN being iterative: at converting the `i`-th addition, the previous `i-1` additions would have already been optimized to multiplication (and thus, further into bit shifts and additions/subtractions if possible).  

Some notable examples of this transformation include:
- `a + a + a` => `a*3` => `(a<<1) + a`
- `a + a + a + a` => `a*4` => `a<<2`
- `a*3 + a` => `a*4` => `a<<2`
- `(a << 1) + a + a` => `a*2 + a + a` => `a*3 + a` => `a*4 => a<<2`

See included IR unit tests for more.

<!-- Anything below this marker will be automatically updated, please do not edit manually! -->
---------
### Progress
- [x] Change must be properly reviewed (1 review required, with at least 1 [Reviewer](https://openjdk.org/bylaws#reviewer))
- [x] Change must not contain extraneous whitespace
- [x] Commit message must refer to an issue

### Issue
 * [JDK-8325495](https://bugs.openjdk.org/browse/JDK-8325495): C2: implement optimization for series of Add of unique value (**Enhancement** - P4)


### Reviewers
 * [Roland Westrelin](https://openjdk.org/census#roland) (@rwestrel - **Reviewer**) Review applies to [af6f8084](https://git.openjdk.org/jdk/pull/20754/files/af6f808472bc34eedbd095b3f1e2c5957f0c1694)
 * [Christian Hagedorn](https://openjdk.org/census#chagedorn) (@chhagedorn - **Reviewer**)

### Reviewing
<details><summary>Using <code>git</code></summary>

Checkout this PR locally: \
`$ git fetch https://git.openjdk.org/jdk.git pull/20754/head:pull/20754` \
`$ git checkout pull/20754`

Update a local copy of the PR: \
`$ git checkout pull/20754` \
`$ git pull https://git.openjdk.org/jdk.git pull/20754/head`

</d

---

> **生成时间**: 2026-03-25

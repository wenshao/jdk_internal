# JDK-8367857: Implement JEP 525: Structured Concurrency (Sixth Preview)

> **JEP 525: Structured Concurrency (Sixth Preview)** | Closed / Delivered

## 基本信息

| 属性 | 值 |
|------|-----|
| **Issue** | [JDK-8367857](https://bugs.openjdk.org/browse/JDK-8367857) |
| **PR** | [#27392](https://github.com/openjdk/jdk/pull/27392) |
| **Author** | AlanBateman |
| **组织** | Oracle |
| **Created** | 2025-09-19 |
| **Merged** | 2025-11-10 |
| **审查天数** | 52 |
| **版本** | JDK 26 |
| **优先级** | P3 |
| **组件** | core-libs |
| **代码变更** | +784 / -313 (6 文件) |
| **审查者** | AlanBateman, pavelrappo, viktorklang-ora (3 人) |

## PR 描述

Updates for JEP 525.

- `Joiner:onTimeout` is added  with `join` method changed to invoke this joiner method if
  a timeout is configured and the timeout expires before or while waiting. The `onTimeout`
  throws `TimeoutException` or may do nothing. This allows for `Joiner` implementation that
  are capable of returning a result from the subtasks that complete before the timeout expires.
- The `configFunction` parameter to the 3-arg `open` is changed from
  `Function<Configuration, Configuration>` to `UnaryOperator<Configuration>`.
- `Subtask::get` and `Subtask::exception` changed to consistently throw if called from
  any thread before the scope owner has joined.
- `StructuredTaskScope::join` is now specified so that it may be called again if interrupted.
- The parameter on `Joiner::onFork` and `Joiner::onComplete` is changed from
  `Subtask<? extends T` to `Subtask<T>`.
- `Joiner.allSuccessOrThrow` is changed to return a list of results instead of a stream of
  subtasks. `Joiner.allUntil` is changed to return a list of subtasks.
- `Joiner.anySuccessfulResultOrThrow` is renamed to `anySuccessfulOrThrow`.
- `Joiner.allUntil(Predicate)` is changed to allow `join` return the stream of all forked
subtasks when the timeout expires.
- `Joiner` is no longer a `@FunctionalInterface`.

Most of the changes are to API docs and tests. Some links are changed to use double hash mark.

<!-- Anything below this marker will be automatically updated, please do not edit man

...(截断)

## 变更文件

| 文件 | 状态 | +/- |
|------|------|-----|
| src/java.base/share/classes/java/util/concurrent/Joiners.java | modified | +47/-30 |
| ...va.base/share/classes/java/util/concurrent/StructuredTaskScope.java | modified | +239/-158 |
| ...ase/share/classes/java/util/concurrent/StructuredTaskScopeImpl.java | modified | +70/-45 |
| src/java.base/share/classes/jdk/internal/javac/PreviewFeature.java | modified | +1/-1 |
| ...dk/java/util/concurrent/StructuredTaskScope/StressCancellation.java | modified | +1/-1 |
| ...va/util/concurrent/StructuredTaskScope/StructuredTaskScopeTest.java | modified | +426/-78 |

## JEP 525 详情

- **标题**: Structured Concurrency (Sixth Preview)
- **负责人**: Alan Bateman
- **状态**: Closed / Delivered
- **目标版本**: JDK 26
- **链接**: [openjdk.org/jeps/525](https://openjdk.org/jeps/525)

## 审查讨论摘要

- **viktorklang-ora**: Is the second "unnamed" referring to the ThreadGroup itself? If so, it might be clearer to say "configuration has an unnamed ThreadFactory that create
- **viktorklang-ora**: Might be worth mentioning that said TimeoutException will then propagate upon the invocation of STS::join()?
- **viktorklang-ora**: Nice catch!

---

> **数据来源**: GitHub API + JBS | **生成时间**: 2026-03-25

# JDK-8367530: The exhaustiveness errors could be improved

## 基本信息

| 属性 | 值 |
|------|-----|
| **Issue** | [JDK-8367530](https://bugs.openjdk.org/browse/JDK-8367530) |
| **PR** | [#27256](https://github.com/openjdk/jdk/pull/27256) |
| **Author** | lahodaj |
| **组织** | Oracle |
| **Created** | 2025-09-12 |
| **Merged** | 2026-02-04 |
| **审查天数** | 145 |
| **版本** | JDK 26 |
| **优先级** | P2 |
| **组件** | tools |
| **模块** | tools |
| **代码变更** | +1449 / -99 (16 files) |
| **审查者** | biboudis, davidalayachew, lahodaj, mcimadamore (4 人) |

## 描述

Consider code like:
```
package test;
public class Test {
    private int test(Root r) {
        return switch (r) {
            case Root(R2(R1 _), R2(R1 _)) -> 0;
            case Root(R2(R1 _), R2(R2 _)) -> 0;
            case Root(R2(R2 _), R2(R1 _)) -> 0;
        };
    }
    sealed interface Base {}
    record R1() implements Base {}
    record R2(Base b1) implements Base {}
    record Root(R2 b2, R2 b3) {}
}
``` 

This is missing a case for `Root(R2(R2 _), R2(R2 _))`. javac will produce an error correctly, but the error is not very helpful:
```
$ javac test/Test.java
.../test/Test.java:4: error: the switch expression does not cover all possible input values
        return switch (r) {
               ^
1 error
```

The goal of this PR is to improve the error, at least in some cases to something along these lines:
```
$ javac test/Test.java 
.../test/Test.java:4: error: the switch expression does not cover all possible input values
        return switch (r) {
               ^
  missing patterns:
      Root(R2(R2 _), R2(R2 _))
1 error
```

The (very simplified) way it works in a recursive (or induction) way:
- start with defining the missing pattern as the binding pattern for the selector type. This would certainly exhaust the switch.
- for a current missing pattern, try to enhance it:
    - if the current type is a sealed type, try to expand to its (direct) permitted subtypes. Remove those that are not needed.
    - if the current (binding pattern) type is a record type, expand it to a record type, generate all possible combinations of its component types based on sealed hierarchies. Remove those that are not needed.

This approach relies heavily on our ability to compute exhaustiveness, which is evaluated repeatedly in the process.

There are some cases where the algorithm does not produce ideal results (see the tests), but overall seems much better than what we have now.

Another significant limitation is the sp

## 变更文件

| 文件 | 状态 | +/- |
|------|------|-----|
| .../share/classes/com/sun/tools/javac/comp/ExhaustivenessComputer.java | modified | +650/-79 |
| src/jdk.compiler/share/classes/com/sun/tools/javac/comp/Flow.java | modified | +60/-7 |
| ...ler/share/classes/com/sun/tools/javac/resources/compiler.properties | modified | +21/-1 |
| test/langtools/tools/javac/diags/examples/BindingPattern.java | added | +33/-0 |
| test/langtools/tools/javac/diags/examples/EnumConstantPattern.java | added | +37/-0 |
| test/langtools/tools/javac/diags/examples/NotExhaustive.java | modified | +2/-1 |
| test/langtools/tools/javac/diags/examples/NotExhaustiveStatement.java | modified | +2/-1 |
| test/langtools/tools/javac/diags/examples/RecordPattern.java | added | +37/-0 |
| test/langtools/tools/javac/patterns/Exhaustiveness.java | modified | +2/-1 |
| .../langtools/tools/javac/patterns/ExhaustivenessConvenientErrors.java | added | +593/-0 |
| test/langtools/tools/javac/patterns/PrimitiveInstanceOfComboTest.java | modified | +4/-1 |
| ...angtools/tools/javac/patterns/PrimitivePatternsSwitchConstants.java | modified | +1/-1 |
| test/langtools/tools/javac/patterns/PrimitivePatternsSwitchErrors.java | modified | +1/-1 |
| test/langtools/tools/javac/patterns/SwitchErrors.java | modified | +1/-1 |
| test/langtools/tools/javac/platform/NonExportedPermittedTypes.java | modified | +4/-4 |

> 共 16 个文件

## 审查讨论

- **biboudis**: This extra boolean parameter is subtle (because previously we didn't have a new mode of execution). Can you introduce either a top-level comment at th
- **biboudis**: Can this be subsumed into `computeMissingPatternDescriptions` just to handle the generation of all kinds of incomplete patterns inside there? It seems
- **biboudis**: Can you reuse the word "viablePermittedPatterns" in this comment? That way the reader can formulate the properties of what this variable represents, f

---

> **生成时间**: 2026-03-25

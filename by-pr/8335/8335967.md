# JDK-8335967: "text-decoration: none" does not work with "A" HTML tags

## 基本信息

| 属性 | 值 |
|------|-----|
| **Issue** | [JDK-8335967](https://bugs.openjdk.org/browse/JDK-8335967) |
| **PR** | [#20313](https://github.com/openjdk/jdk/pull/20313) |
| **Author** | aivanov-jdk |
| **组织** | Oracle |
| **Created** | 2024-07-24 |
| **Merged** | 2024-07-26 |
| **审查天数** | 2 |
| **版本** | JDK 23 |
| **优先级** | P2 |
| **组件** | client-libs |
| **模块** | client |
| **代码变更** | +132 / -0 (2 files) |
| **审查者** | dmarkov20, kumarabhi006, prrace (3 人) |

## 描述

After I integrated [JDK-8326734](https://bugs.openjdk.org/browse/JDK-8326734), there's no way to remove text decoration from an HTML element. The most common HTML element is `<a>` which is usually rendered _underlined_. You could add an inline style or apply a rule with `text-decoration: none` to remove underline.

This fix restores this functionality.

The test contains five cases. The first case ensures the `<a>` element is rendered with underline by default. The second and third cases verify that underline is removed if `text-decoration: none` is added to the `style` attribute or is applied by a style-sheet rule.

The fourth and fifth cases verify that underline is preserved the containing element, `<p>` in this case, has `text-decoration: underline`.

These five cases render the same way in browsers. However, the fourth and fifth cases render without underline in JDKs without JDK-8326734.

<!-- Anything below this marker will be automatically updated, please do not edit manually! -->
---------
### Progress
- [x] Change must be properly reviewed (1 review required, with at least 1 [Reviewer](https://openjdk.org/bylaws#reviewer))
- [x] Change must not contain extraneous whitespace
- [x] Commit message must refer to an issue

### Issue
 * [JDK-8335967](https://bugs.openjdk.org/browse/JDK-8335967): "text-decoration: none" does not work with "A" HTML tags (**Bug** - P2)


### Reviewers
 * [Abhishek Kumar](https://openjdk.org/census#abhiscxk) (@kumarabhi006 - Committer)
 * [Dmitry Markov](https://openjdk.org/census#dmarkov) (@dmarkov20 - **Reviewer**)
 * [Harshitha Onkar](https://openjdk.org/census#honkar) (@honkar-jdk - **Reviewer**)
 * [Phil Race](https://openjdk.org/census#prr) (@prrace - **Reviewer**)

### Reviewing
<details><summary>Using <code>git</code></summary>

Checkout this PR locally: \
`$ git fetch https://git.openjdk.org/jdk.git pull/20313/head:pull/20313` \
`$ git checkout pull/20313`

Update a local copy of the PR: \
`$ git checkout pull/20313`

## 变更文件

| 文件 | 状态 | +/- |
|------|------|-----|
| src/java.desktop/share/classes/javax/swing/text/html/CSS.java | modified | +4/-0 |
| .../jdk/javax/swing/text/html/HTMLDocument/HTMLTextDecorationNone.java | added | +128/-0 |

## 审查讨论

- **kumarabhi006**: Is it correct to have these bug ids in jtreg tag?
 I think you should add 8335967 bug id .
 
- **kumarabhi006**: I guess It is supposed to run `HTMLTextDecorationNone` test not `HTMLTextDecoration`
 ```suggestion
  * @run main HTMLTextDecorationNone
 ```
- **kumarabhi006**: Need to update summary ?


> **Backported to**: JDK 17u, JDK 21u

---

> **生成时间**: 2026-03-25

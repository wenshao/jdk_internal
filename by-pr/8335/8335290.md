# JDK-8335290: Rename ClassFile::transform to ClassFile::transformClass

## 基本信息

| 属性 | 值 |
|------|-----|
| **Issue** | [JDK-8335290](https://bugs.openjdk.org/browse/JDK-8335290) |
| **PR** | [#19952](https://github.com/openjdk/jdk/pull/19952) |
| **Author** | liach |
| **组织** | Oracle |
| **Created** | 2024-06-28 |
| **Merged** | 2024-07-03 |
| **审查天数** | 5 |
| **版本** | JDK 23 |
| **优先级** | P4 |
| **组件** | core-libs |
| **模块** | core-libs/java.io |
| **代码变更** | +172 / -175 (58 files) |
| **审查者** | asotona (1 人) |

## 描述

`ClassFile::transform` was initially `ClassModel::transform`, which transforms the receiver class model to a new class byte array. This functionality was in parallel with `ClassBuilder::transform`, which accepts a `ClassModel` and a `ClassTransform` and forwards the results to the receiver builder.

After the `ClassFile` context object introduction, `ClassModel::transform` becomes `ClassFile::transform`; now, its role is more similar to `ClassBuilder::transformMethod`, `ClassBuilder::transformField`, or `MethodBuilder::transformCode` (transforming subtypes), and it is confusing with `ClassFileBuilder::transform` (which accepts the same model type as the built type). We should rename `ClassFile::transform` to `ClassFile::transformClass` to make this method's role more clear.

This is separate from #19928 as this has much more user impact.

<!-- Anything below this marker will be automatically updated, please do not edit manually! -->
---------
### Progress
- [x] Change must be properly reviewed (1 review required, with at least 1 [Reviewer](https://openjdk.org/bylaws#reviewer))
- [x] Change must not contain extraneous whitespace
- [x] Change requires CSR request [JDK-8335359](https://bugs.openjdk.org/browse/JDK-8335359) to be approved
- [x] Commit message must refer to an issue

### Issues
 * [JDK-8335290](https://bugs.openjdk.org/browse/JDK-8335290): Rename ClassFile::transform to ClassFile::transformClass (**Enhancement** - P4)
 * [JDK-8335359](https://bugs.openjdk.org/browse/JDK-8335359): Rename ClassFile::transform to ClassFile::transformClass (**CSR**)


### Reviewers
 * [Adam Sotona](https://openjdk.org/census#asotona) (@asotona - **Reviewer**)

### Reviewing
<details><summary>Using <code>git</code></summary>

Checkout this PR locally: \
`$ git fetch https://git.openjdk.org/jdk.git pull/19952/head:pull/19952` \
`$ git checkout pull/19952`

Update a local copy of the PR: \
`$ git checkout pull/19952` \
`$ git pull https://git.openjdk.org/jdk.git pull/19952/h

---

> **生成时间**: 2026-03-25

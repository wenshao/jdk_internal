# JDK-8335091: NMT: VMATree reserve_mapping and commit_mapping APIs need MEMFLAGS while un/-committing API has no MEMFLAGS arg

## 基本信息

| 属性 | 值 |
|------|-----|
| **Issue** | [JDK-8335091](https://bugs.openjdk.org/browse/JDK-8335091) |
| **PR** | [#20330](https://github.com/openjdk/jdk/pull/20330) |
| **Author** | afshin-zafari |
| **组织** | Oracle |
| **Created** | 2024-07-25 |
| **Merged** | 2024-10-14 |
| **审查天数** | 81 |
| **版本** | JDK 23 |
| **优先级** | P4 |
| **组件** | hotspot |
| **模块** | runtime/nmt |
| **代码变更** | +57 / -13 (3 files) |
| **审查者** | afshin-zafari, gerard-ziemski, johan-sjolen, tstuefe (4 人) |

## 描述

In committing a region, it is not mandatory to provide a MEMFLAGS flag where the committed region inherits the flag from the main region it resides in.
In un-committing there is no need to a  MEMFLAGS at all. 
The `register_mapping` API of the VMATree *requires* a MEMFLAGS (via metadata arg) in both of these two operations. To do the flag inheriting, it is possible to copy the flag of the left node in the tree to the newly inserted ones. 

An optional bool arg (default is false) is added to VMATree API to copy the existing flag of the left node to the new nodes.

<!-- Anything below this marker will be automatically updated, please do not edit manually! -->
---------
### Progress
- [x] Change must be properly reviewed (1 review required, with at least 1 [Reviewer](https://openjdk.org/bylaws#reviewer))
- [x] Change must not contain extraneous whitespace
- [x] Commit message must refer to an issue

### Issue
 * [JDK-8335091](https://bugs.openjdk.org/browse/JDK-8335091): NMT: VMATree reserve_mapping and commit_mapping APIs need MEMFLAGS while un/-committing API has no MEMFLAGS arg (**Enhancement** - P4)


### Reviewers
 * [Johan Sjölen](https://openjdk.org/census#jsjolen) (@jdksjolen - **Reviewer**)
 * [Gerard Ziemski](https://openjdk.org/census#gziemski) (@gerard-ziemski - Committer)

### Reviewing
<details><summary>Using <code>git</code></summary>

Checkout this PR locally: \
`$ git fetch https://git.openjdk.org/jdk.git pull/20330/head:pull/20330` \
`$ git checkout pull/20330`

Update a local copy of the PR: \
`$ git checkout pull/20330` \
`$ git pull https://git.openjdk.org/jdk.git pull/20330/head`

</details>
<details><summary>Using Skara CLI tools</summary>

Checkout this PR locally: \
`$ git pr checkout 20330`

View PR using the GUI difftool: \
`$ git pr show -t 20330`

</details>
<details><summary>Using diff file</summary>

Download this PR as a diff file: \
<a href="https://git.openjdk.org/jdk/pull/20330.diff">https://git.openjdk.org/jdk/pull/20330.diff</a>

---

> **生成时间**: 2026-03-25

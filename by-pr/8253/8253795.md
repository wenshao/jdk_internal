# JDK-8253795: Implementation of JEP 391: macOS/AArch64 Port

> **JEP 391: macOS/AArch64 Port** | Closed / Delivered

## 基本信息

| 属性 | 值 |
|------|-----|
| **Issue** | [JDK-8253795](https://bugs.openjdk.org/browse/JDK-8253795) |
| **PR** | [#2200](https://github.com/openjdk/jdk/pull/2200) |
| **Author** | AntonKozlov |
| **组织** | Azul |
| **Created** | 2021-01-22 |
| **Merged** | 2021-03-25 |
| **审查天数** | 62 |
| **版本** | JDK 17 |
| **优先级** | P2 |
| **组件** | hotspot |
| **代码变更** | +2933 / -109 (75 文件) |
| **审查者** | AntonKozlov, VladimirKempik, coleenp, erikj79, lewurm, magicus, plummercj, prrace (9 人) |

## PR 描述

Please review the implementation of JEP 391: macOS/AArch64 Port.

It's heavily based on existing ports to linux/aarch64, macos/x86_64, and windows/aarch64. 

Major changes are in:
* src/hotspot/cpu/aarch64: support of the new calling convention (subtasks JDK-8253817, JDK-8253818)
* src/hotspot/os_cpu/bsd_aarch64: copy of os_cpu/linux_aarch64 with necessary adjustments (JDK-8253819)
* src/hotspot/share, test/hotspot/gtest: support of write-xor-execute (W^X), required on macOS/AArch64 platform. It's implemented with pthread_jit_write_protect_np provided by Apple. The W^X mode is local to a thread, so W^X mode change relates to the java thread state change (for java threads). In most cases, JVM executes in write-only mode, except when calling a generated stub like SafeFetch, which requires a temporary switch to execute-only mode. The same execute-only mode is enabled when a java thread executes in java or native states. This approach of managing W^X mode turned out to be simple and efficient enough.
* src/jdk.hotspot.agent: serviceability agent implementation (JDK-8254941)

<!-- Anything below this marker will be automatically updated, please do not edit manually! -->
---------
### Progress
- [x] Change must not contain extraneous whitespace
- [x] Commit message must refer to an issue
- [x] Change must be properly reviewed

### Issues
 * [JDK-8253795](https://bugs.openjdk.java.net/browse/JDK-8253795): Implementation of JEP 391: macOS/AArch64 Port
 * [JDK-8253816](https:/

...(截断)

## 变更文件

| 文件 | 状态 | +/- |
|------|------|-----|
| make/autoconf/build-aux/config.guess | modified | +10/-1 |
| make/autoconf/flags.m4 | modified | +9/-3 |
| make/autoconf/jvm-features.m4 | modified | +8/-6 |
| make/common/NativeCompilation.gmk | modified | +2/-2 |
| make/hotspot/gensrc/GensrcAdlc.gmk | modified | +4/-1 |
| make/modules/java.desktop/lib/Awt2dLibraries.gmk | modified | +2/-1 |
| make/modules/jdk.hotspot.agent/Lib.gmk | modified | +1/-1 |
| src/hotspot/cpu/aarch64/globalDefinitions_aarch64.hpp | modified | +2/-2 |
| src/hotspot/cpu/aarch64/interpreterRT_aarch64.cpp | modified | +65/-17 |
| src/hotspot/cpu/aarch64/interpreterRT_aarch64.hpp | modified | +5/-2 |
| src/hotspot/cpu/aarch64/macroAssembler_aarch64.hpp | modified | +1/-0 |
| src/hotspot/cpu/aarch64/sharedRuntime_aarch64.cpp | modified | +27/-2 |
| src/hotspot/cpu/aarch64/vm_version_aarch64.hpp | modified | +9/-0 |
| src/hotspot/os/bsd/os_bsd.cpp | modified | +3/-1 |
| src/hotspot/os/posix/signals_posix.cpp | modified | +9/-12 |

> 共 75 个文件变更

## JEP 391 详情

- **标题**: macOS/AArch64 Port
- **负责人**: Vladimir Kempik
- **状态**: Closed / Delivered
- **目标版本**: JDK 17
- **链接**: [openjdk.org/jeps/391](https://openjdk.org/jeps/391)

## 审查讨论摘要

- **erikj79**: Globally disabling a warning needs some motivation. Why is this needed? Does it really need to be applied everywhere or is there a specific binary or 
- **theRealAph**: I don't think you need such a large switch statement. I think this can be expressed as ``` if (num_int_args <= 6) {     ldr(as_Register(num_int_arg
- **theRealAph**: I'm not sure these names should be in the global name space, and they're not very descriptive. Something like NativeStack::byteSpace would be better. 

---

> **数据来源**: GitHub API + JBS | **生成时间**: 2026-03-25

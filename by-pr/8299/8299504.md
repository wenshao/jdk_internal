# JDK-8299504: Resolve `uses` and `provides` at run time if the service is optional and missing

## 基本信息

| 属性 | 值 |
|------|-----|
| **Issue** | [JDK-8299504](https://bugs.openjdk.org/browse/JDK-8299504) |
| **PR** | [#23147](https://github.com/openjdk/jdk/pull/23147) |
| **Author** | AlanBateman |
| **组织** | Oracle |
| **Created** | 2025-01-16 |
| **Merged** | 2025-01-25 |
| **审查天数** | 9 |
| **版本** | JDK 25 |
| **优先级** | P4 |
| **组件** | core-libs |
| **模块** | core-libs/java.util |
| **代码变更** | +530 / -302 (4 files) |
| **审查者** | sundararajana (1 人) |

## 描述

Extend the support for optional dependences to allow for a service to be optional. The post-resolution consistency check specified by `Configuration.resolve` is relaxed to allow for the possibility that the service from a module in the module graph at compile-time but the module is not in the module graph at run-time.

ConfigurationTest is ported from TestNG to JUnit and updated to add new tests testUsesOptionalService$N and testProvidesOptionalService$N. The port to JUnit is mostly trivial/mechanical changes but it does create a bit of noise - I can move this port to another issue if it makes the harder to see the additions to the test.

<!-- Anything below this marker will be automatically updated, please do not edit manually! -->
---------
### Progress
- [x] Change must be properly reviewed (1 review required, with at least 1 [Reviewer](https://openjdk.org/bylaws#reviewer))
- [x] Change must not contain extraneous whitespace
- [x] Commit message must refer to an issue
- [x] Change requires CSR request [JDK-8347915](https://bugs.openjdk.org/browse/JDK-8347915) to be approved

### Issues
 * [JDK-8299504](https://bugs.openjdk.org/browse/JDK-8299504): Resolve `uses` and `provides` at run time if the service is optional and missing (**Bug** - P4)
 * [JDK-8347915](https://bugs.openjdk.org/browse/JDK-8347915): Service use or provides in module creates required dependency (**CSR**)


### Reviewers
 * [Athijegannathan Sundararajan](https://openjdk.org/census#sundar) (@sundararajana - **Reviewer**)

### Contributors
 * Alan Bateman `<alanb@openjdk.org>`
 * Alex Buckley `<abuckley@openjdk.org>`

### Reviewing
<details><summary>Using <code>git</code></summary>

Checkout this PR locally: \
`$ git fetch https://git.openjdk.org/jdk.git pull/23147/head:pull/23147` \
`$ git checkout pull/23147`

Update a local copy of the PR: \
`$ git checkout pull/23147` \
`$ git pull https://git.openjdk.org/jdk.git pull/23147/head`

</details>
<details><summary>Using Skara CLI tools</summary>

---

> **生成时间**: 2026-03-25

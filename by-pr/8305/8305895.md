# JDK-8305895: Implement JEP 450: Compact Object Headers (Experimental)

> **JEP 450: Compact Object Headers (Experimental)** | Closed / Delivered

## 基本信息

| 属性 | 值 |
|------|-----|
| **Issue** | [JDK-8305895](https://bugs.openjdk.org/browse/JDK-8305895) |
| **PR** | [#20677](https://github.com/openjdk/jdk/pull/20677) |
| **Author** | rkennke |
| **组织** | Datadog |
| **Created** | 2024-08-22 |
| **Merged** | 2024-11-08 |
| **审查天数** | 78 |
| **版本** | JDK 23 |
| **优先级** | P4 |
| **组件** | hotspot |
| **代码变更** | +4348 / -1627 (218 文件) |
| **审查者** | Hamlin-Li, RealFYang, albertnetymk, iklam, lmesnik, magicus, plummercj, rkennke (11 人) |

## PR 描述

This is the main body of the JEP 450: Compact Object Headers (Experimental).

It is also a follow-up to #20640, which now also includes (and supersedes) #20603 and #20605, plus the Tiny Class-Pointers parts that have been previously missing.

Main changes:
 - Introduction of the (experimental) flag UseCompactObjectHeaders. All changes in this PR are protected by this flag. The purpose of the flag is to provide a fallback, in case that users unexpectedly observe problems with the new implementation. The intention is that this flag will remain experimental and opt-in for at least one release, then make it on-by-default and diagnostic (?), and eventually deprecate and obsolete it. However, there are a few unknowns in that plan, specifically, we may want to further improve compact headers to 4 bytes, we are planning to enhance the Klass* encoding to support virtually unlimited number of Klasses, at which point we could also obsolete UseCompressedClassPointers.
 - The compressed Klass* can now be stored in the mark-word of objects. In order to be able to do this, we are add some changes to GC forwarding (see below) to protect the relevant (upper 22) bits of the mark-word. Significant parts of this PR deal with loading the compressed Klass* from the mark-word. This PR also changes some code paths (mostly in GCs) to be more careful when accessing Klass* (or mark-word or size) to be able to fetch it from the forwardee in case the object is forwarded.
 - Self-forwarding in GCs 

...(截断)

## 变更文件

| 文件 | 状态 | +/- |
|------|------|-----|
| make/Images.gmk | modified | +17/-3 |
| make/autoconf/configure.ac | modified | +1/-0 |
| make/autoconf/jdk-options.m4 | modified | +31/-0 |
| make/autoconf/spec.gmk.template | modified | +1/-0 |
| src/hotspot/cpu/aarch64/aarch64.ad | modified | +19/-1 |
| src/hotspot/cpu/aarch64/c1_LIRAssembler_aarch64.cpp | modified | +5/-42 |
| src/hotspot/cpu/aarch64/c1_MacroAssembler_aarch64.cpp | modified | +12/-8 |
| src/hotspot/cpu/aarch64/c2_MacroAssembler_aarch64.cpp | modified | +9/-0 |
| src/hotspot/cpu/aarch64/c2_MacroAssembler_aarch64.hpp | modified | +2/-0 |
| src/hotspot/cpu/aarch64/compressedKlass_aarch64.cpp | modified | +0/-16 |
| src/hotspot/cpu/aarch64/macroAssembler_aarch64.cpp | modified | +58/-19 |
| src/hotspot/cpu/aarch64/macroAssembler_aarch64.hpp | modified | +3/-1 |
| src/hotspot/cpu/aarch64/templateTable_aarch64.cpp | modified | +13/-6 |
| src/hotspot/cpu/ppc/c1_LIRAssembler_ppc.cpp | modified | +10/-48 |
| src/hotspot/cpu/ppc/c1_MacroAssembler_ppc.cpp | modified | +11/-4 |

> 共 218 个文件变更

## JEP 450 详情

- **标题**: Compact Object Headers (Experimental)
- **负责人**: Roman Kennke
- **状态**: Closed / Delivered
- **目标版本**: JDK 24
- **链接**: [openjdk.org/jeps/450](https://openjdk.org/jeps/450)

## 审查讨论摘要

- **albertnetymk**: I wonder if not modifying successful-forwarded objs is cleaner. Sth like:  ```c reset_self_forwarded_in_space(space) {   cur = space->bottom();  
- **albertnetymk**: I feel it's more correct to go through the forwardee for forwarded objs even for the non-COMPACT_HEADERS case. (This method is meant to cover all objs
- **albertnetymk**: Can one use `MaxHeapSize` here?

---

> **数据来源**: GitHub API + JBS | **生成时间**: 2026-03-25

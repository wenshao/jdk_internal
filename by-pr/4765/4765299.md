# JDK-4765299: componentResized() not always called with nested JSplitPanes

## 基本信息

| 属性 | 值 |
|------|-----|
| **Issue** | [JDK-4765299](https://bugs.openjdk.org/browse/JDK-4765299) |
| **PR** | [#29063](https://github.com/openjdk/jdk/pull/29063) |
| **Author** | prsadhuk |
| **组织** | Oracle |
| **Created** | 2026-01-06 |
| **Merged** | 2026-01-13 |
| **审查天数** | 7 |
| **版本** | JDK 26 |
| **优先级** | P3 |
| **组件** | client-libs |
| **模块** | client |
| **代码变更** | +175 / -2 (2 files) |
| **审查者** | TejeshR13, azuev-java (2 人) |

## 描述

If a component "comp" be nested inside a "inner" JSplitPane with VERTICAL_SPLIT mode and if then "inner" be nested inside a
"outer" JSplitPane with HORIZONTAL_SPLIT mode and a component listener is added to "comp"
then with this setup, componentResized() on the listener is called ONLY when the divider of "outer" is moved manually
however, it is not called when the divider of "outer" is moved using "One touch expandable" left button at the top of the divider.

This is because the `layoutContainer` bails out if the width or height is 0 which logically should be to bail out if both width and height is 0 and when "One touch expandable" left button is pressed, width becomes 0 so `layoutContainer` bails out without sending the event.

With this fix, componentResized is called

No regression is observed in CI.

<!-- Anything below this marker will be automatically updated, please do not edit manually! -->
---------
### Progress
- [x] Change must be properly reviewed (1 review required, with at least 1 [Reviewer](https://openjdk.org/bylaws#reviewer))
- [x] Change must not contain extraneous whitespace
- [x] Commit message must refer to an issue

### Issue
 * [JDK-4765299](https://bugs.openjdk.org/browse/JDK-4765299): componentResized() not always called with nested JSplitPanes (**Bug** - P5)


### Reviewers
 * [Tejesh R](https://openjdk.org/census#tr) (@TejeshR13 - **Reviewer**)
 * [Alexander Zuev](https://openjdk.org/census#kizune) (@azuev-java - **Reviewer**)

### Reviewing
<details><summary>Using <code>git</code></summary>

Checkout this PR locally: \
`$ git fetch https://git.openjdk.org/jdk.git pull/29063/head:pull/29063` \
`$ git checkout pull/29063`

Update a local copy of the PR: \
`$ git checkout pull/29063` \
`$ git pull https://git.openjdk.org/jdk.git pull/29063/head`

</details>
<details><summary>Using Skara CLI tools</summary>

Checkout this PR locally: \
`$ git pr checkout 29063`

View PR using the GUI difftool: \
`$ git pr show -t 29063`

</details>
<

---

> **生成时间**: 2026-03-25

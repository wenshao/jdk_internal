# JDK-8309467: Pattern dominance should be adjusted

## 基本信息

| 属性 | 值 |
|------|-----|
| **Issue** | [JDK-8309467](https://bugs.openjdk.org/browse/JDK-8309467) |
| **PR** | [#14314](https://github.com/openjdk/jdk/pull/14314) |
| **Author** | lahodaj |
| **组织** | Oracle |
| **Created** | 2023-06-05 |
| **Merged** | 2023-06-12 |
| **审查天数** | 7 |
| **版本** | JDK 21 |
| **优先级** | P3 |
| **组件** | tools |
| **模块** | tools |
| **代码变更** | +70 / -18 (4 files) |
| **审查者** | vicente-romero-oracle (1 人) |

## 描述

Currently, when a switch contains both patterns and constant labels, any pattern case dominates the constant label. E.g. the following example currently produces a compile-time error:
```
switch (...) {
    case Integer i when i == 0 -> {}
    case 1 -> {} //dominated by the preceding case
    case Integer i -> {}
}
```

But, the rules are now (to be) relaxed in the spec, so that guarded cases (with patterns) don't dominate cases with constant labels. I.e. the above example is supposed to compile and run fine.

This PR is implementing this change - when checking patter-constant dominance, we only consider unguarded cases.

<!-- Anything below this marker will be automatically updated, please do not edit manually! -->
---------
### Progress
- [x] Change must be properly reviewed (1 review required, with at least 1 [Reviewer](https://openjdk.org/bylaws#reviewer))
- [x] Change must not contain extraneous whitespace
- [x] Commit message must refer to an issue
- [x] Change requires CSR request [JDK-8309469](https://bugs.openjdk.org/browse/JDK-8309469) to be approved

### Issues
 * [JDK-8309467](https://bugs.openjdk.org/browse/JDK-8309467): Pattern dominance should be adjusted (**Bug** - P3)
 * [JDK-8309469](https://bugs.openjdk.org/browse/JDK-8309469): Pattern dominance should be adjusted (**CSR**)


### Reviewers
 * [Vicente Romero](https://openjdk.org/census#vromero) (@vicente-romero-oracle - **Reviewer**)


### Reviewing
<details><summary>Using <code>git</code></summary>

Checkout this PR locally: \
`$ git fetch https://git.openjdk.org/jdk.git pull/14314/head:pull/14314` \
`$ git checkout pull/14314`

Update a local copy of the PR: \
`$ git checkout pull/14314` \
`$ git pull https://git.openjdk.org/jdk.git pull/14314/head`

</details>
<details><summary>Using Skara CLI tools</summary>

Checkout this PR locally: \
`$ git pr checkout 14314`

View PR using the GUI difftool: \
`$ git pr show -t 14314`

</details>
<details><summary>Using diff file</summary>

Down

---

> **生成时间**: 2026-03-25

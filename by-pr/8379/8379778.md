# JDK-8379778: SunGraphics2D improvements

## 基本信息

| 属性 | 值 |
|------|-----|
| **Issue** | [JDK-8379778](https://bugs.openjdk.org/browse/JDK-8379778) |
| **PR** | [#30191](https://github.com/openjdk/jdk/pull/30191) |
| **Author** | gredler |
| **组织** | Independent |
| **Merged** | 2026-03-12 |
| **版本** | JDK 26 |
| **优先级** | P5 |
| **组件** | client-libs |
| **模块** | client |
| **代码变更** | +25 / -128 (1 files) |
| **审查者** | avu, prrace |

## 描述

There is a some unused code in SunGraphics2D which can be removed:

- The private static variable `slowTextTransformMask` is never used.
- In `checkFontInfo`, the variable `txFontType` is never used.
- The public static method `isRotated` is never used.
- The commented out method `XcopyArea` is never used (and has been commented out for 19+ years).
- The method `transformBounds` is never used (it was used by `XcopyArea` but was never deleted or commented out).

Additionally, there are quite a few `instanceof`s + casts which could benefit from `instanceof` pattern matching.

(I've been spending some time in this area of the code figuring out some of the remaining macOS printing issues, and these rough edges kept catching my eye, so I figured it was better to spin them out into a small, independent PR.)

<!-- Anything below this marker will be automatically updated, please do not edit manually! -->
---------
### Progress
- [x] Change must be properly reviewed (1 review required, with at least 1 [Reviewer](https://openjdk.org/bylaws#reviewer))
- [x] Change must not contain extraneous whitespace
- [x] Commit message must refer to an issue

### Issue
 * [JDK-8379778](https://bugs.openjdk.org/browse/JDK-8379778): SunGraphics2D improvements (**Bug** - P5)


### Reviewers
 * [Alexey Ushakov](https://openjdk.org/census#avu) (@avu - Committer)
 * [Phil Race](https://openjdk.org/census#prr) (@prrace - **Reviewer**)

### Reviewing
<details><summary>Using <code>git</code></summary>

Checkout this PR locally: \
`$ git fetch https://git.openjdk.org/jdk.git pull/30191/head:pull/30191` \
`$ git checkout pull/30191`

Update a local copy of the PR: \
`$ git checkout pull/30191` \
`$ git pull https://git.openjdk.org/jdk.git pull/30191/head`

</details>
<details><summary>Using Skara CLI tools</summary>

Checkout this PR locally: \
`$ git pr checkout 30191`

View PR using the GUI difftool: \
`$ git pr show -t 30191`

</details>
<details><summary>Using diff file</summary>

Downl

---

> **生成时间**: 2026-03-25

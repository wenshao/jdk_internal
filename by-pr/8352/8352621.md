# JDK-8352621: MatchException from backwards incompatible change to switch expressions

## 基本信息

| 属性 | 值 |
|------|-----|
| **Issue** | [JDK-8352621](https://bugs.openjdk.org/browse/JDK-8352621) |
| **PR** | [#24241](https://github.com/openjdk/jdk/pull/24241) |
| **Author** | lahodaj |
| **组织** | Oracle |
| **Created** | 2025-03-26 |
| **Merged** | 2025-04-07 |
| **审查天数** | 12 |
| **版本** | JDK 25 |
| **优先级** | P2 |
| **组件** | tools |
| **模块** | tools |
| **代码变更** | +181 / -3 (3 files) |
| **审查者** | biboudis (1 人) |

## 描述

Considering code like:
```
record R2(int i1, int i2) {}
...
        case R2(int i1, int i2) when i1 == 0 -> 2;
        case R2(int i1, int i2) -> 3;
....
```

javac will compile this along these lines:
```
   case R2 $temp: int $c1 = $temp.i1();
                               switch (i1) {
                                   case int i1 when ...
                                   ...
...
```

The nested switch is a pattern matching switch, and uses `SwitchBootstraps.typeSwitch`, passing the case label types (classes) as static arguments. The problem is that since primitive type patterns have been introduced, `typeSwitch` accepts primitive types as case labels, and javac will use such types in this case. Which works OK on JDK 23+.

But, on JDK 21 (and 22), `typeSwitch` cannot handle case labels of primitive types correctly, and the matching fails.

The solution proposed herein is to not use the primitive type case labels when targeting JDK 21 and 22, which makes the behavior similar to what javac did for this code in JDK 21.

<!-- Anything below this marker will be automatically updated, please do not edit manually! -->
---------
### Progress
- [x] Change must be properly reviewed (1 review required, with at least 1 [Reviewer](https://openjdk.org/bylaws#reviewer))
- [x] Change must not contain extraneous whitespace
- [x] Commit message must refer to an issue

### Issue
 * [JDK-8352621](https://bugs.openjdk.org/browse/JDK-8352621): MatchException from backwards incompatible change to switch expressions (**Bug** - P2)


### Reviewers
 * [Aggelos Biboudis](https://openjdk.org/census#abimpoudis) (@biboudis - **Reviewer**)

### Reviewing
<details><summary>Using <code>git</code></summary>

Checkout this PR locally: \
`$ git fetch https://git.openjdk.org/jdk.git pull/24241/head:pull/24241` \
`$ git checkout pull/24241`

Update a local copy of the PR: \
`$ git checkout pull/24241` \
`$ git pull https://git.openjdk.org/jdk.git pull/24241/head`

</details>

## 变更文件

| 文件 | 状态 | +/- |
|------|------|-----|
| ....compiler/share/classes/com/sun/tools/javac/comp/TransPatterns.java | modified | +7/-2 |
| src/jdk.compiler/share/classes/com/sun/tools/javac/jvm/Target.java | modified | +8/-1 |
| test/langtools/tools/javac/patterns/NoPrimitivesAsCaseLabelsFor21.java | added | +166/-0 |

## 审查讨论

- **biboudis**: small fix about the name of the test is needed here

---

> **生成时间**: 2026-03-25

# JDK-8183569: Assert the same limits are used in parse_xss and globals.hpp

## 基本信息

| 属性 | 值 |
|------|-----|
| **Issue** | [JDK-8183569](https://bugs.openjdk.org/browse/JDK-8183569) |
| **PR** | [#2688](https://github.com/openjdk/jdk/pull/2688) |
| **Author** | iklam |
| **组织** | Oracle |
| **Created** | 2021-02-23 |
| **Merged** | 2021-03-03 |
| **审查天数** | 8 |
| **版本** | JDK 17 |
| **优先级** | P4 |
| **组件** | hotspot |
| **模块** | arch/x86 |
| **代码变更** | +58 / -15 (4 files) |
| **审查者** | iklam, kimbarrett, tstuefe (3 人) |

## 描述

We have the following in Arguments::parse_xss():

```
  const julong min_ThreadStackSize = 0;
  const julong max_ThreadStackSize = 1 * M;
  const julong min_size = min_ThreadStackSize * K;
  const julong max_size = max_ThreadStackSize * K;
```

which duplicates the min/max range specified in globals.hpp:

```
  product_pd(intx, ThreadStackSize, \
          "Thread Stack Size (in Kbytes)") \
          range(0, 1 * M) \
```

I added an API to query the min/max range of a flag, so parse_xss can be rewritten as

```
  const JVMTypedFlagLimit<intx>* limit = 
    JVMFlagLimit::get_range_at(FLAG_MEMBER_ENUM(ThreadStackSize))->cast<intx>();
  const julong min_size = limit->min() * K;
  const julong max_size = limit->max() * K;
```

<!-- Anything below this marker will be automatically updated, please do not edit manually! -->
---------
### Progress
- [x] Change must not contain extraneous whitespace
- [x] Commit message must refer to an issue
- [x] Change must be properly reviewed

### Issue
 * [JDK-8183569](https://bugs.openjdk.java.net/browse/JDK-8183569): Assert the same limits are used in parse_xss and globals.hpp


### Reviewers
 * [Thomas Stuefe](https://openjdk.java.net/census#stuefe) (@tstuefe - **Reviewer**) ⚠️ Review applies to e28c07ccd457cb38b1bdae6f16ca0a04d493246f
 * [Kim Barrett](https://openjdk.java.net/census#kbarrett) (@kimbarrett - **Reviewer**) ⚠️ Review applies to e28c07ccd457cb38b1bdae6f16ca0a04d493246f


### Download
`$ git fetch https://git.openjdk.java.net/jdk pull/2688/head:pull/2688`
`$ git checkout pull/2688`

---

> **生成时间**: 2026-03-25

# JDK-8155259: Suspicious buffer allocation in com.sun.tools.javac.file.BaseFileManager

## 基本信息

| 属性 | 值 |
|------|-----|
| **Issue** | [JDK-8155259](https://bugs.openjdk.org/browse/JDK-8155259) |
| **PR** | [#10803](https://github.com/openjdk/jdk/pull/10803) |
| **Author** | archiecobbs |
| **组织** | Oracle |
| **Created** | 2022-10-20 |
| **Merged** | 2023-01-04 |
| **审查天数** | 76 |
| **版本** | JDK 20 |
| **优先级** | P4 |
| **组件** | tools |
| **模块** | core-libs/java.io |
| **代码变更** | +1 / -1 (1 files) |
| **审查者** | vicente-romero-oracle (1 人) |

## 描述

JDK-8155259 notes that in `BaseFileManager` this code:
```java
ByteBuffer.allocate(capacity + capacity>>1)
```
is actually equivalent (assuming no overflow) to:
```java
ByteBuffer.allocate(capacity)
```
because in Java the `+` operator has higher precedence than the `>>` operator.

In the bug it is suggested that this be changed to reflect the presumed original intent to over-allocate by 50%, i.e.:
```java
ByteBuffer.allocate(capacity + (capacity>>1))
```
however, this patch is more conservative in that it leaves the existing behavior alone and instead just simplifies it to:
```java
ByteBuffer.allocate(capacity)
```
allowing us to at least resolve JDK-8155259.

<!-- Anything below this marker will be automatically updated, please do not edit manually! -->
---------
### Progress
- [x] Change must be properly reviewed (1 review required, with at least 1 [Reviewer](https://openjdk.org/bylaws#reviewer))
- [x] Change must not contain extraneous whitespace
- [x] Commit message must refer to an issue

### Issues
 * [JDK-8155259](https://bugs.openjdk.org/browse/JDK-8155259): Suspicious buffer allocation in com.sun.tools.javac.file.BaseFileManager
 * [JDK-8172106](https://bugs.openjdk.org/browse/JDK-8172106): javac throws exception when compiling source file of size 1.5G


### Reviewers
 * [Vicente Romero](https://openjdk.org/census#vromero) (@vicente-romero-oracle - **Reviewer**)


### Reviewing
<details><summary>Using <code>git</code></summary>

Checkout this PR locally: \
`$ git fetch https://git.openjdk.org/jdk pull/10803/head:pull/10803` \
`$ git checkout pull/10803`

Update a local copy of the PR: \
`$ git checkout pull/10803` \
`$ git pull https://git.openjdk.org/jdk pull/10803/head`

</details>
<details><summary>Using Skara CLI tools</summary>

Checkout this PR locally: \
`$ git pr checkout 10803`

View PR using the GUI difftool: \
`$ git pr show -t 10803`

</details>
<details><summary>Using diff file</summary>

Download this PR as a diff file: \
<a

---

> **生成时间**: 2026-03-25

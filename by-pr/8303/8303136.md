# JDK-8303136: MemoryPoolMBean/isCollectionUsageThresholdExceeded/isexceeded005 failed with "isCollectionUsageThresholdExceede

## 基本信息

| 属性 | 值 |
|------|-----|
| **Issue** | [JDK-8303136](https://bugs.openjdk.org/browse/JDK-8303136) |
| **PR** | [#12823](https://github.com/openjdk/jdk/pull/12823) |
| **Author** | kevinjwalls |
| **组织** | Oracle |
| **Created** | 2023-03-02 |
| **Merged** | 2023-03-08 |
| **审查天数** | 6 |
| **版本** | JDK 21 |
| **优先级** | P4 |
| **组件** | core-svc |
| **模块** | core-libs/java.util |
| **代码变更** | +8 / -11 (1 files) |
| **审查者** | kevinjwalls, plummercj, sspitsyn (3 人) |

## 描述

Test update for an occasional failure, which does not reproduce.

The test failure in JDK-8303136 is at line 141 in the updated file here.  It's the failure where isExceeded is true, but our sampled "used" value is not above the threshold.  But while the comment says it's refreshing values, it does not not refresh "used", so there could have been gc/promotion activity which hits the threshold outside of the test's control.  Refreshing "used" is the addition here.

Separately, the code at line 123 in the new file also claims to refresh the values, but it only refreshes the threshold, which we aren't changing.  Not making it refresh "used" at that point looks correct, so remove the "if (used >= threshold)" as we have already checked that at line 116.

<!-- Anything below this marker will be automatically updated, please do not edit manually! -->
---------
### Progress
- [x] Change must be properly reviewed (1 review required, with at least 1 [Reviewer](https://openjdk.org/bylaws#reviewer))
- [x] Change must not contain extraneous whitespace
- [x] Commit message must refer to an issue

### Issue
 * [JDK-8303136](https://bugs.openjdk.org/browse/JDK-8303136): MemoryPoolMBean/isCollectionUsageThresholdExceeded/isexceeded005 failed with "isCollectionUsageThresholdExceeded() returned true, while threshold = 1 and used = 0"


### Reviewers
 * [Chris Plummer](https://openjdk.org/census#cjplummer) (@plummercj - **Reviewer**)
 * [Serguei Spitsyn](https://openjdk.org/census#sspitsyn) (@sspitsyn - **Reviewer**)


### Reviewing
<details><summary>Using <code>git</code></summary>

Checkout this PR locally: \
`$ git fetch https://git.openjdk.org/jdk pull/12823/head:pull/12823` \
`$ git checkout pull/12823`

Update a local copy of the PR: \
`$ git checkout pull/12823` \
`$ git pull https://git.openjdk.org/jdk pull/12823/head`

</details>
<details><summary>Using Skara CLI tools</summary>

Checkout this PR locally: \
`$ git pr checkout 12823`

View PR using the GUI difftool: \
`$ gi

---

> **生成时间**: 2026-03-25

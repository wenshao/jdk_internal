# JDK-8372635: Lambdas do not copy over SYNTHETIC flag for local variables

## 基本信息

| 属性 | 值 |
|------|-----|
| **Issue** | [JDK-8372635](https://bugs.openjdk.org/browse/JDK-8372635) |
| **PR** | [#28724](https://github.com/openjdk/jdk/pull/28724) |
| **Author** | lahodaj |
| **组织** | Oracle |
| **Created** | 2025-12-09 |
| **Merged** | 2025-12-17 |
| **审查天数** | 8 |
| **版本** | JDK 26 |
| **优先级** | P4 |
| **组件** | tools |
| **模块** | tools |
| **代码变更** | +141 / -2 (2 files) |
| **审查者** | covers1624, lahodaj, liach, vicente-romero-oracle (4 人) |

## 描述

Consider case like:
```
Object o = ...;

//synthetic variable for the instanceof's expression
boolean b = o.toString() instanceof String str && str.isEmpty();

Runnable r = () -> {
        //synthetic variable for the instanceof's expression
        boolean b = o.toString() instanceof String str && str.isEmpty();
};
```

There are synthetic variables generated for `o.toString()`, to ensure the method is invoked only once for each `instanceof`. These variables are marked as synthetic in `TransPatterns` (and then consequently omitted in `LocalVariableTable`), but when the flag is lost for the variable in the lambda.

This PR proposes to keep the flag, as suggested in the bug.

<!-- Anything below this marker will be automatically updated, please do not edit manually! -->
---------
### Progress
- [x] Change must be properly reviewed (1 review required, with at least 1 [Reviewer](https://openjdk.org/bylaws#reviewer))
- [x] Change must not contain extraneous whitespace
- [x] Commit message must refer to an issue

### Issue
 * [JDK-8372635](https://bugs.openjdk.org/browse/JDK-8372635): Lambdas do not copy over SYNTHETIC flag for local variables (**Bug** - P4)


### Reviewers
 * [Vicente Romero](https://openjdk.org/census#vromero) (@vicente-romero-oracle - **Reviewer**)
 * [Chen Liang](https://openjdk.org/census#liach) (@liach - **Reviewer**)

### Reviewing
<details><summary>Using <code>git</code></summary>

Checkout this PR locally: \
`$ git fetch https://git.openjdk.org/jdk.git pull/28724/head:pull/28724` \
`$ git checkout pull/28724`

Update a local copy of the PR: \
`$ git checkout pull/28724` \
`$ git pull https://git.openjdk.org/jdk.git pull/28724/head`

</details>
<details><summary>Using Skara CLI tools</summary>

Checkout this PR locally: \
`$ git pr checkout 28724`

View PR using the GUI difftool: \
`$ git pr show -t 28724`

</details>
<details><summary>Using diff file</summary>

Download this PR as a diff file: \
<a href="https://git.openjdk.org/jd

---

> **生成时间**: 2026-03-25

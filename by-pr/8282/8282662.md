# JDK-8282662: Use List.of() factory method to reduce memory consumption

## 基本信息

| 属性 | 值 |
|------|-----|
| **Issue** | [JDK-8282662](https://bugs.openjdk.org/browse/JDK-8282662) |
| **PR** | [#7729](https://github.com/openjdk/jdk/pull/7729) |
| **Author** | stsypanov |
| **组织** | Independent |
| **Created** | 2022-03-07 |
| **Merged** | 2022-06-01 |
| **审查天数** | 86 |
| **版本** | JDK 19 |
| **优先级** | P4 |
| **组件** | core-libs |
| **模块** | core-libs |
| **代码变更** | +10 / -11 (4 files) |
| **审查者** | ExE-Boss, RogerRiggs, cl4es, liach, stsypanov, wangweij (6 人) |

## 描述

`List.of()` along with `Set.of()` create unmodifiable `List/Set` but with smaller footprint comparing to `Arrays.asList()` / `new HashSet()` when called with vararg of size 0, 1, 2.

In general replacement of `Arrays.asList()` with `List.of()` is dubious as the latter is null-hostile, however in some cases we are sure that arguments are non-null. Into this PR I've included the following cases (in addition to those where the argument is proved to be non-null at compile-time):
- `MethodHandles.longestParameterList()` never returns null
- parameter types are never null
- interfaces used for proxy construction and returned from `Class.getInterfaces()` are never null
- exceptions types of method signature are never null

<!-- Anything below this marker will be automatically updated, please do not edit manually! -->
---------
### Progress
- [x] Change must be properly reviewed (1 review required, with at least 1 [Reviewer](https://openjdk.java.net/bylaws#reviewer))
- [x] Change must not contain extraneous whitespace
- [x] Commit message must refer to an issue

### Issue
 * [JDK-8282662](https://bugs.openjdk.java.net/browse/JDK-8282662): Use List.of() factory method to reduce memory consumption


### Reviewers
 * [Claes Redestad](https://openjdk.java.net/census#redestad) (@cl4es - **Reviewer**) ⚠️ Review applies to [3a845a3d](https://git.openjdk.java.net/jdk/pull/7729/files/3a845a3d2d63ccd9595fea1d2ea22eb4c4559376)
 * [Roger Riggs](https://openjdk.java.net/census#rriggs) (@RogerRiggs - **Reviewer**)


### Reviewing
<details><summary>Using <code>git</code></summary>

Checkout this PR locally: \
`$ git fetch https://git.openjdk.java.net/jdk pull/7729/head:pull/7729` \
`$ git checkout pull/7729`

Update a local copy of the PR: \
`$ git checkout pull/7729` \
`$ git pull https://git.openjdk.java.net/jdk pull/7729/head`

</details>
<details><summary>Using Skara CLI tools</summary>

Checkout this PR locally: \
`$ git pr checkout 7729`

View PR using the GUI difftool: \
`$ g

---

> **生成时间**: 2026-03-25

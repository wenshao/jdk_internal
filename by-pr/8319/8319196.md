# JDK-8319196: ExecutableElement.getReceiverType doesn't return receiver types for methods loaded from bytecode

## 基本信息

| 属性 | 值 |
|------|-----|
| **Issue** | [JDK-8319196](https://bugs.openjdk.org/browse/JDK-8319196) |
| **PR** | [#16482](https://github.com/openjdk/jdk/pull/16482) |
| **Author** | cushon |
| **组织** | Google |
| **Created** | 2023-11-02 |
| **Merged** | 2023-11-14 |
| **审查天数** | 12 |
| **版本** | JDK 22 |
| **优先级** | P4 |
| **组件** | tools |
| **模块** | core-libs/classfile |
| **代码变更** | +94 / -29 (4 files) |
| **审查者** | cushon, liach, vicente-romero-oracle (3 人) |

## 描述

Hello,

Please consider this fix for [JDK-8319196: ExecutableElement.getReceiverType doesn't return receiver types for methods loaded from bytecode](https://bugs.openjdk.org/browse/JDK-8319196).

As discussed in [the accompanying CSR](https://bugs.openjdk.org/browse/JDK-8319330), `ExecutableElement.getReceiverType()` and `ExecutableType.getReceiverType()` currently returns `NoType` unless the receiver parameter syntax was present in source, which also means that `getReceiverType()` doesn't work for methods loaded from class files.

This change updates the implementation to check if type annotations are present on the receiver type of a method read from bytecode, and fill in the receiver type of that method to host the type annotations.

<!-- Anything below this marker will be automatically updated, please do not edit manually! -->
---------
### Progress
- [x] Change must be properly reviewed (1 review required, with at least 1 [Reviewer](https://openjdk.org/bylaws#reviewer))
- [x] Change must not contain extraneous whitespace
- [x] Commit message must refer to an issue
- [x] Change requires CSR request [JDK-8319330](https://bugs.openjdk.org/browse/JDK-8319330) to be approved

### Issues
 * [JDK-8319196](https://bugs.openjdk.org/browse/JDK-8319196): ExecutableElement.getReceiverType doesn't return receiver types for methods loaded from bytecode (**Bug** - P4)
 * [JDK-8319330](https://bugs.openjdk.org/browse/JDK-8319330): ExecutableElement.getReceiverType doesn't return receiver types for methods loaded from bytecode (**CSR**)


### Reviewers
 * [Vicente Romero](https://openjdk.org/census#vromero) (@vicente-romero-oracle - **Reviewer**) ⚠️ Review applies to [32f0050a](https://git.openjdk.org/jdk/pull/16482/files/32f0050a70fe2b807090d5698dfd61339c503880)


### Reviewing
<details><summary>Using <code>git</code></summary>

Checkout this PR locally: \
`$ git fetch https://git.openjdk.org/jdk.git pull/16482/head:pull/16482` \
`$ git checkout pull/16482`

Update a loc

---

> **生成时间**: 2026-03-25

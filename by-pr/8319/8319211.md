# JDK-8319211: Regression in LoopOverNonConstantFP

## 基本信息

| 属性 | 值 |
|------|-----|
| **Issue** | [JDK-8319211](https://bugs.openjdk.org/browse/JDK-8319211) |
| **PR** | [#16451](https://github.com/openjdk/jdk/pull/16451) |
| **Author** | mcimadamore |
| **组织** | Oracle |
| **Created** | 2023-11-01 |
| **Merged** | 2023-11-01 |
| **审查天数** | 0 |
| **版本** | JDK 22 |
| **优先级** | P2 |
| **组件** | core-libs |
| **模块** | core-libs |
| **代码变更** | +42 / -37 (4 files) |
| **审查者** | JornVernee (1 人) |

## 描述

This PR fixes an inexact var handle call that sneaked in as part of https://git.openjdk.org/jdk/pull/16224.

As some default methods were moved from `MemorySegment` to `AbstractMemorySegmentImpl`, the type of `this` has changed, and this made all the var handle calls in the memory segment accessors inexact.

To prevent this issue from occurring again, I've added a new JDK flag to create _exact_ segment view var handles, so that we can test that segment accessors work even when using the stricter behavior.

I've considered using a flag for _all_ var handles (even non-FFM ones), but gave up when I saw that there is some JDK code that needs to pass `null` to some var handles, in which case exact behavior is not possible (at least not without fixing the use site to cast the `null` to the right type). This might be pursued as a separate effort.

<!-- Anything below this marker will be automatically updated, please do not edit manually! -->
---------
### Progress
- [x] Change must be properly reviewed (1 review required, with at least 1 [Reviewer](https://openjdk.org/bylaws#reviewer))
- [x] Change must not contain extraneous whitespace
- [x] Commit message must refer to an issue

### Issue
 * [JDK-8319211](https://bugs.openjdk.org/browse/JDK-8319211): Regression in LoopOverNonConstantFP (**Bug** - P2)


### Reviewers
 * [Jorn Vernee](https://openjdk.org/census#jvernee) (@JornVernee - **Reviewer**)


### Reviewing
<details><summary>Using <code>git</code></summary>

Checkout this PR locally: \
`$ git fetch https://git.openjdk.org/jdk.git pull/16451/head:pull/16451` \
`$ git checkout pull/16451`

Update a local copy of the PR: \
`$ git checkout pull/16451` \
`$ git pull https://git.openjdk.org/jdk.git pull/16451/head`

</details>
<details><summary>Using Skara CLI tools</summary>

Checkout this PR locally: \
`$ git pr checkout 16451`

View PR using the GUI difftool: \
`$ git pr show -t 16451`

</details>
<details><summary>Using diff file</summary>

Download this PR as a diff

## 变更文件

| 文件 | 状态 | +/- |
|------|------|-----|
| src/java.base/share/classes/java/lang/invoke/MethodHandleStatics.java | modified | +3/-0 |
| src/java.base/share/classes/java/lang/invoke/VarHandles.java | modified | +2/-1 |
| ...e/share/classes/jdk/internal/foreign/AbstractMemorySegmentImpl.java | modified | +36/-36 |
| test/jdk/java/foreign/TestMemoryAccessInstance.java | modified | +1/-0 |

---

> **生成时间**: 2026-03-25

# JDK-8261812: C2 compilation fails with assert(!had_error) failed: bad dominance

## 基本信息

| 属性 | 值 |
|------|-----|
| **Issue** | [JDK-8261812](https://bugs.openjdk.org/browse/JDK-8261812) |
| **PR** | [#2774](https://github.com/openjdk/jdk/pull/2774) |
| **Author** | rwestrel |
| **组织** | Red Hat |
| **Created** | 2021-03-01 |
| **Merged** | 2021-03-05 |
| **审查天数** | 4 |
| **版本** | JDK 17 |
| **优先级** | P2 |
| **组件** | hotspot |
| **模块** | compiler/c2 |
| **代码变更** | +170 / -23 (6 files) |
| **审查者** | TobiHartmann, rwestrel, vnkozlov (3 人) |

## 描述

In the testByte() test case, byteField is assigned int values that
don't fit in a byte.

obj.byteField = (byte)(1 << i);

At parse time, c2 expands this to (in pseudo code):

int v = (1 << i);
obj.byteField = (v << 24) >> 24;

next:

StoreBNode::Ideal() runs and StoreNode::Ideal_sign_extended_input()
causes this to become:

obj.byteField = (1 << i);

i is then constant folded to 9:

obj.byteField = 512;

obj doesn't escape and is scalarized. The value of the byteField needs
to be recorded in the debug info. Because there are 2 such a store, a
Phi is created:

(Phi 512 1024)

The Phi is created with type TypeInt::BYTE. Because that doesn't
overlap with the input values, the Phi is transformed to top. The test
case then triggers a deoptimization and a crash occurs in the
deoptimization code.

The failure of the bug report is not a crash during deoptimization but
a c2 crash. The root cause is the same but in that case a chain of Phi
is involved and when one becomes top, it causes an uncommon trap to
have an input that doesn't dominate the uncommon trap.

I noticed LoadBNode::Ideal() re-inserts the 2 shifts when it returns a
value from a Store. I propose the same fix here in the allocation
elimination code.

Same problem applies to short and char. They are covered by the fix. I
also included boolean even though I don't see how to trigger the
failure with it.

<!-- Anything below this marker will be automatically updated, please do not edit manually! -->
---------
### Progress
- [x] Change must not contain extraneous whitespace
- [x] Commit message must refer to an issue
- [x] Change must be properly reviewed

### Issue
 * [JDK-8261812](https://bugs.openjdk.java.net/browse/JDK-8261812): C2 compilation fails with assert(!had_error) failed: bad dominance


### Reviewers
 * [Vladimir Kozlov](https://openjdk.java.net/census#kvn) (@vnkozlov - **Reviewer**) ⚠️ Review applies to 7ec1fab0998682a8cbae6fd418b311a1adfaa83a
 * [Tobias Hartmann](https://openjdk.java.net/census#thartm

## 变更文件

| 文件 | 状态 | +/- |
|------|------|-----|
| src/hotspot/share/opto/compile.cpp | modified | +24/-0 |
| src/hotspot/share/opto/compile.hpp | modified | +4/-3 |
| src/hotspot/share/opto/macro.cpp | modified | +3/-0 |
| src/hotspot/share/opto/memnode.cpp | modified | +25/-13 |
| src/hotspot/share/opto/parse2.cpp | modified | +6/-7 |
| ...t/jtreg/compiler/escapeAnalysis/TestValAtSafepointOverflowsInt.java | added | +108/-0 |

## 审查讨论

- **vnkozlov**: Use `is_subword_type(bt)` defined in `globalDefinitions.hpp`
- **rwestrel**: Thanks for the review. I pushed a commit with your suggestion.
- **TobiHartmann**: There's some excess whitespacing.

---

> **生成时间**: 2026-03-25

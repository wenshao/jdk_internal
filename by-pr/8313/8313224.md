# JDK-8313224: Avoid calling JavaThread::current() in MemAllocator::Allocation constructor

## 基本信息

| 属性 | 值 |
|------|-----|
| **Issue** | [JDK-8313224](https://bugs.openjdk.org/browse/JDK-8313224) |
| **PR** | [#15058](https://github.com/openjdk/jdk/pull/15058) |
| **Author** | iklam |
| **组织** | Oracle |
| **Created** | 2023-07-27 |
| **Merged** | 2023-08-11 |
| **审查天数** | 15 |
| **版本** | JDK 21 |
| **优先级** | P4 |
| **组件** | hotspot |
| **模块** | runtime/threading |
| **代码变更** | +15 / -12 (2 files) |
| **审查者** | coleenp, dholmes-ora, iklam, tschatzl (4 人) |

## 描述

Summary: Avoid calling `JavaThread::current()` since we already have a `Thread*` in `MemAllocator::_thread`.

Although the latter is declared to be a `Thead*` and not a `JavaThread*`, the calling context requires the current thread to be a `JavaThread`, and  `MemAllocator::_thread` was initialized to be  `Thread::current`, so I changed the code from 

```
_thread(JavaThread::current()),
```

to

```
assert(Thread::current()->is_Java_thread(), "must be used by JavaThreads only");
assert(Thread::current() == allocator._thread, "do not pass MemAllocator across threads");
_thread = JavaThread::cast(allocator._thread);
```

I also clean up a few other lines to limit the explicit use of `JavaThread*`.

This fix improved `java --version` by about 0.6% for my prototype of [JDK-8310823](https://bugs.openjdk.org/browse/JDK-8310823), which allocates ~24000 heap objects at VM start-up.

<!-- Anything below this marker will be automatically updated, please do not edit manually! -->
---------
### Progress
- [x] Change must be properly reviewed (1 review required, with at least 1 [Reviewer](https://openjdk.org/bylaws#reviewer))
- [x] Change must not contain extraneous whitespace
- [x] Commit message must refer to an issue

### Issue
 * [JDK-8313224](https://bugs.openjdk.org/browse/JDK-8313224): Avoid calling JavaThread::current() in MemAllocator::Allocation constructor (**Enhancement** - P4)


### Reviewers
 * [Thomas Schatzl](https://openjdk.org/census#tschatzl) (@tschatzl - **Reviewer**) ⚠️ Review applies to [da211ff9](https://git.openjdk.org/jdk/pull/15058/files/da211ff932cbc115dba150c0060eba93e16666fc)
 * [Coleen Phillimore](https://openjdk.org/census#coleenp) (@coleenp - **Reviewer**)


### Reviewing
<details><summary>Using <code>git</code></summary>

Checkout this PR locally: \
`$ git fetch https://git.openjdk.org/jdk.git pull/15058/head:pull/15058` \
`$ git checkout pull/15058`

Update a local copy of the PR: \
`$ git checkout pull/15058` \
`$ git pull htt

---

> **生成时间**: 2026-03-25

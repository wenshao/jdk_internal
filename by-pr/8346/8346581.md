# JDK-8346581: JRadioButton/ButtonGroupFocusTest.java fails in CI on Linux

## 基本信息

| 属性 | 值 |
|------|-----|
| **Issue** | [JDK-8346581](https://bugs.openjdk.org/browse/JDK-8346581) |
| **PR** | [#22977](https://github.com/openjdk/jdk/pull/22977) |
| **Author** | aivanov-jdk |
| **组织** | Oracle |
| **Created** | 2025-01-08 |
| **Merged** | 2025-01-10 |
| **审查天数** | 2 |
| **版本** | JDK 25 |
| **优先级** | P4 |
| **组件** | client-libs |
| **模块** | test |
| **代码变更** | +111 / -39 (1 files) |
| **审查者** | DamonGuy (1 人) |

## 描述

**Problem:**

The `javax/swing/JRadioButton/ButtonGroupFocus/ButtonGroupFocusTest.java` test fails in CI on Linux.

The focus is on _Button 2_ instead of _Button 4_.

**Root cause:**

The additional logging revealed, an unexpected `KEY_PRESS` event. The test uses `Robot` API to press the <kbd>Tab</kbd> key and release it. When the test fails, there are two `KEY_PRESS` events followed by a single `KEY_RELEASE` event. Because the <kbd>Tab</kbd> key is pressed twice, the focus moves twice: from _Button&nbsp;2_ (the initial state) to _Button&nbsp;4_ and then back to _Button&nbsp;2_.

**Fix:**

Use `CountDownLatch`es to track whether a radio button received focus. Detect the case where two `KEY_PRESS` events moved focus to _Button&nbsp;2_ and report the failure.

Log focus movements and dispatched key events to facilitate failure analysis.

Take a screenshot of the test frame in case of failure.

These CI hosts seem to be quite slow, removing the delay added by `robot.setAutoDelay(100)` has reduced the number of unexpected `KEY_PRESS` events. This didn't affect Windows or macOS.

<!-- Anything below this marker will be automatically updated, please do not edit manually! -->
---------
### Progress
- [x] Change must be properly reviewed (1 review required, with at least 1 [Reviewer](https://openjdk.org/bylaws#reviewer))
- [x] Change must not contain extraneous whitespace
- [x] Commit message must refer to an issue

### Issue
 * [JDK-8346581](https://bugs.openjdk.org/browse/JDK-8346581): JRadioButton/ButtonGroupFocusTest.java fails in CI on Linux (**Bug** - P4)


### Reviewers
 * [Harshitha Onkar](https://openjdk.org/census#honkar) (@honkar-jdk - **Reviewer**)
 * [Damon Nguyen](https://openjdk.org/census#dnguyen) (@DamonGuy - Committer)

### Reviewing
<details><summary>Using <code>git</code></summary>

Checkout this PR locally: \
`$ git fetch https://git.openjdk.org/jdk.git pull/22977/head:pull/22977` \
`$ git checkout pull/22977`


> **Backported to**: JDK 17u, JDK 21u

---

> **生成时间**: 2026-03-25

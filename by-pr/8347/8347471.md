# JDK-8347471: Provide valid flags and mask in AccessFlag.Location

## 基本信息

| 属性 | 值 |
|------|-----|
| **Issue** | [JDK-8347471](https://bugs.openjdk.org/browse/JDK-8347471) |
| **PR** | [#23095](https://github.com/openjdk/jdk/pull/23095) |
| **Author** | liach |
| **组织** | Oracle |
| **Created** | 2025-01-14 |
| **Merged** | 2025-04-30 |
| **审查天数** | 106 |
| **版本** | JDK 25 |
| **优先级** | P4 |
| **组件** | core-libs |
| **模块** | core-libs |
| **代码变更** | +579 / -321 (9 files) |
| **审查者** | ExE-Boss, RogerRiggs, liach (3 人) |

## 描述

Some AccessFlag parsing methods throw IAE because a flag mask is not valid in a location. However, there is no easy way to check what flag mask bits or what flags are valid for a location. We need such APIs to check, specific to each class file format version.

Also in the investigation, it's noted that `ACC_SYNTHETIC` is incorrectly represented - it is available since release 5.0 instead of release 7. This bug is fixed together for implementation simplicity.

The new methods are all in `AccessFlag.Location`:
- `Set<AccessFlag> flags()`
- `int flagsMask()`
- `Set<AccessFlag> flags(ClassFileFormatVersion)`
- `int flagsMask(ClassFileFormatVersion)`

Also there is some simplification to `AccessFlag` itself to remove the anonymous classes, which should be more startup-friendly.

Testing: Tier 1-3

<!-- Anything below this marker will be automatically updated, please do not edit manually! -->
---------
### Progress
- [x] Change must be properly reviewed (1 review required, with at least 1 [Reviewer](https://openjdk.org/bylaws#reviewer))
- [x] Change must not contain extraneous whitespace
- [x] Change requires CSR request [JDK-8347626](https://bugs.openjdk.org/browse/JDK-8347626) to be approved
- [x] Commit message must refer to an issue

### Issues
 * [JDK-8347471](https://bugs.openjdk.org/browse/JDK-8347471): Provide valid flags and mask in AccessFlag.Location (**Enhancement** - P4)
 * [JDK-8347626](https://bugs.openjdk.org/browse/JDK-8347626): Provide valid flags and mask in AccessFlag.Location (**CSR**)


### Reviewers
 * [Roger Riggs](https://openjdk.org/census#rriggs) (@RogerRiggs - **Reviewer**)

### Reviewing
<details><summary>Using <code>git</code></summary>

Checkout this PR locally: \
`$ git fetch https://git.openjdk.org/jdk.git pull/23095/head:pull/23095` \
`$ git checkout pull/23095`

Update a local copy of the PR: \
`$ git checkout pull/23095` \
`$ git pull https://git.openjdk.org/jdk.git pull/23095/head`

</details>
<details><summary>Using Skara

---

> **生成时间**: 2026-03-25

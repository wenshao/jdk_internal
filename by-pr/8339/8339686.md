# JDK-8339686: java/foreign/TestMappedHandshake.java fails with assert(depth < max_critical_stack_depth) failed: can't have mo

## 基本信息

| 属性 | 值 |
|------|-----|
| **Issue** | [JDK-8339686](https://bugs.openjdk.org/browse/JDK-8339686) |
| **PR** | [#20914](https://github.com/openjdk/jdk/pull/20914) |
| **Author** | mcimadamore |
| **组织** | Oracle |
| **Created** | 2024-09-09 |
| **Merged** | 2024-09-11 |
| **审查天数** | 2 |
| **版本** | JDK 24 |
| **优先级** | P4 |
| **组件** | core-libs |
| **模块** | arch/x86 |
| **代码变更** | +99 / -57 (7 files) |
| **审查者** | AlanBateman, JornVernee, TheRealMDoerr, mcimadamore (4 人) |

## 描述

The new test added by https://github.com/openjdk/jdk/pull/20854 fails spuriously.
While JNI lookup is now moved into the static initializer of the `MappedMemoryUtils` class, this class might only get initialized while in the middle of a scoped context.

To address this, I created a new proxy interface, namely `MappedMemoryUtilsProxy`. This interface is used by `ScopedMemoryAccess` to call the various `force`/`load`/`isLoaded`/`unload` methods, and a singleton instance is provided inside `MappedMemoryUtils` itself, and then exposed via the `SharedSecrets` mechanism.

Crucially, `MappedMemorySegmentImpl` will now _first_ obtain a `MappedMemoryUtilsProxy` and _then_ call `ScopedMemoryAccess`. This should move all class initializer side-effects out of the scoped method context.

<!-- Anything below this marker will be automatically updated, please do not edit manually! -->
---------
### Progress
- [x] Change must be properly reviewed (1 review required, with at least 1 [Reviewer](https://openjdk.org/bylaws#reviewer))
- [x] Change must not contain extraneous whitespace
- [x] Commit message must refer to an issue

### Issues
 * [JDK-8339686](https://bugs.openjdk.org/browse/JDK-8339686): java/foreign/TestMappedHandshake.java fails with assert(depth &lt; max_critical_stack_depth) failed: can't have more than 10 critical frames (**Bug** - P4)
 * [JDK-8339780](https://bugs.openjdk.org/browse/JDK-8339780): TestByteBuffer fails on AIX after 8339285 (**Bug** - P4)


### Reviewers
 * [Jorn Vernee](https://openjdk.org/census#jvernee) (@JornVernee - **Reviewer**) Review applies to [32389c43](https://git.openjdk.org/jdk/pull/20914/files/32389c43996bc4dce40c2e8b192837468a711a59)
 * [Alan Bateman](https://openjdk.org/census#alanb) (@AlanBateman - **Reviewer**)

### Reviewing
<details><summary>Using <code>git</code></summary>

Checkout this PR locally: \
`$ git fetch https://git.openjdk.org/jdk.git pull/20914/head:pull/20914` \
`$ git checkout pull/20914`

Update a local copy of the PR

---

> **生成时间**: 2026-03-25

# JDK-8353185: Introduce the concept of upgradeable files in context of JEP 493

> **JEP 493: Linking Run-Time Images without JMODs** | Closed / Delivered

## 基本信息

| 属性 | 值 |
|------|-----|
| **Issue** | [JDK-8353185](https://bugs.openjdk.org/browse/JDK-8353185) |
| **PR** | [#24388](https://github.com/openjdk/jdk/pull/24388) |
| **Author** | jerboaa |
| **组织** | IBM |
| **Created** | 2025-04-02 |
| **Merged** | 2025-04-15 |
| **审查天数** | 13 |
| **版本** | JDK 25 |
| **优先级** | P3 |
| **组件** | tools |
| **代码变更** | +254 / -4 (5 文件) |
| **审查者** | AlanBateman, RealCLanger, jerboaa, magicus (4 人) |

## PR 描述

For JEP 493-enabled builds there are no JMODs. Certain files come from the installed JDK image when a user creates a custom run-time from it. This is problematic for example for files that often come from a different package (e.g. `cacerts` file for Linux distro builds of OpenJDK packaged as RPMs), or more generally when they get updated out-of-band of the JDK build itself like the tzupdater tool.

When that happens the hash sum recorded at JDK build time of those files no longer match, causing `jlink` to fail. I propose to allow for those files to get "upgraded" should this happen. The way this works is as follows:

1. The list of upgradeable files is configured by a resource file in `jdk.jlink` on a per module basis. Right now, only two files from the `java.base` module will be allowed to be upgraded with a link from the current run-time image.
2. For those files the hash sum checks are skipped.

**Testing**

- [x] GHA
- [x] `jdk/tools/jlink` jtreg tests (also on [GHA](https://github.com/jerboaa/jdk/actions/runs/14376644254))
- [x] Some manual tests with updated `tzdb.dat` and `cacerts` files.

Thoughts?

<!-- Anything below this marker will be automatically updated, please do not edit manually! -->
---------
### Progress
- [x] Change must not contain extraneous whitespace
- [x] Commit message must refer to an issue
- [x] Change must be properly reviewed (2 reviews required, with at least 1 [Reviewer](https://openjdk.org/bylaws#reviewer), 1 [Author](https://ope

...(截断)

## 变更文件

| 文件 | 状态 | +/- |
|------|------|-----|
| make/modules/jdk.jlink/Java.gmk | added | +32/-0 |
| src/jdk.jlink/share/classes/jdk/tools/jlink/internal/JRTArchive.java | modified | +20/-2 |
| ...nk/share/classes/jdk/tools/jlink/internal/LinkableRuntimeImage.java | modified | +45/-2 |
| ...s/jdk/tools/jlink/internal/runtimelink/upgrade_files_java.base.conf | added | +4/-0 |
| test/jdk/tools/jlink/runtimeImage/UpgradeableFileCacertsTest.java | added | +153/-0 |

## JEP 493 详情

- **标题**: Linking Run-Time Images without JMODs
- **负责人**: Severin Gehwolf
- **状态**: Closed / Delivered
- **目标版本**: JDK 24
- **链接**: [openjdk.org/jeps/493](https://openjdk.org/jeps/493)

## 审查讨论摘要

- **RealCLanger**: Wouldn't `COPY += upgrade_files_java.base` work here ?
- **RealCLanger**: ```suggestion      * Certain files in a module are considered upgradeable. That is, ```
- **RealCLanger**: ```suggestion      * @return {@code true} if the file is upgradeable. {@code false} otherwise. ```

---

> **数据来源**: GitHub API + JBS | **生成时间**: 2026-03-25

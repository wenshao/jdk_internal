# JDK-8277072: ObjectStreamClass caches keep ClassLoaders alive

## 基本信息

| 属性 | 值 |
|------|-----|
| **Issue** | [JDK-8277072](https://bugs.openjdk.org/browse/JDK-8277072) |
| **PR** | [#6375](https://github.com/openjdk/jdk/pull/6375) |
| **Author** | rkennke |
| **组织** | Red Hat |
| **Created** | 2021-11-12 |
| **Merged** | 2021-12-10 |
| **审查天数** | 28 |
| **版本** | JDK 18 |
| **优先级** | P4 |
| **组件** | core-libs |
| **模块** | core-libs/java.util |
| **代码变更** | +311 / -213 (4 files) |
| **审查者** | RogerRiggs, plevart, rkennke (3 人) |

## 描述

The caches in ObjectStreamClass basically map WeakReference<Class> to SoftReference<ObjectStreamClass>, where the ObjectStreamClass also references the same Class. That means that the cache entry, and thus the class and its class-loader, will not get reclaimed, unless the GC determines that memory pressure is very high.

However, this seems bogus, because that unnecessarily keeps ClassLoaders and all its classes alive much longer than necessary: as soon as a ClassLoader (and all its classes) become unreachable, there is no point in retaining the stuff in OSC's caches.

The proposed change is to use WeakReference instead of SoftReference for the values in caches.

Testing:
 - [x] tier1
 - [x] tier2
 - [x] tier3
 - [ ] tier4

<!-- Anything below this marker will be automatically updated, please do not edit manually! -->
---------
### Progress
- [x] Change must not contain extraneous whitespace
- [x] Commit message must refer to an issue
- [x] Change must be properly reviewed

### Issue
 * [JDK-8277072](https://bugs.openjdk.java.net/browse/JDK-8277072): ObjectStreamClass caches keep ClassLoaders alive


### Reviewers
 * [Roger Riggs](https://openjdk.java.net/census#rriggs) (@RogerRiggs - **Reviewer**) ⚠️ Review applies to e6e34ceae491684a9ed0641809dad11d450d778f
 * [Peter Levart](https://openjdk.java.net/census#plevart) (@plevart - **Reviewer**)


### Reviewing
<details><summary>Using <code>git</code></summary>

Checkout this PR locally: \
`$ git fetch https://git.openjdk.java.net/jdk pull/6375/head:pull/6375` \
`$ git checkout pull/6375`

Update a local copy of the PR: \
`$ git checkout pull/6375` \
`$ git pull https://git.openjdk.java.net/jdk pull/6375/head`

</details>
<details><summary>Using Skara CLI tools</summary>

Checkout this PR locally: \
`$ git pr checkout 6375`

View PR using the GUI difftool: \
`$ git pr show -t 6375`

</details>
<details><summary>Using diff file</summary>

Download this PR as a diff file: \
<a href="https://git.openjdk.java.net

---

> **生成时间**: 2026-03-25

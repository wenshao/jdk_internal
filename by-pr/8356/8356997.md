# JDK-8356997: /etc/krb5.conf parser should not forbid include/includedir directives after sections

## 基本信息

| 属性 | 值 |
|------|-----|
| **Issue** | [JDK-8356997](https://bugs.openjdk.org/browse/JDK-8356997) |
| **PR** | [#25421](https://github.com/openjdk/jdk/pull/25421) |
| **Author** | wangweij |
| **组织** | Oracle |
| **Created** | 2025-05-23 |
| **Merged** | 2025-07-23 |
| **审查天数** | 61 |
| **版本** | JDK 25 |
| **优先级** | P4 |
| **组件** | security-libs |
| **模块** | build |
| **代码变更** | +487 / -108 (4 files) |
| **审查者** | myankelev, valeriepeng, wangweij (3 人) |

## 描述

Several changes are made:

1. The "include" and "includedir" directives can appear everywhere, even inside a section or a sub-section. However, it only means the content is inserted here but the included file still need its own full structure -- from section to subsections.
2. The same file can be included multiple times as long as not recursively.
3. Everything is merged. For duplicated values, `get` returns the first one and `getAll` returns all joining by spaces.

Two new tests added. I also separately confirmed that they are parsed in the same way as [MIT krb5](https://github.com/krb5/krb5/blob/master/src/util/profile/test_parse.c). MIT krb5 ignores directory name after "include" but here it's an error.

<!-- Anything below this marker will be automatically updated, please do not edit manually! -->
---------
### Progress
- [x] Change must be properly reviewed (1 review required, with at least 1 [Reviewer](https://openjdk.org/bylaws#reviewer))
- [x] Change must not contain extraneous whitespace
- [x] Commit message must refer to an issue

### Issue
 * [JDK-8356997](https://bugs.openjdk.org/browse/JDK-8356997): /etc/krb5.conf parser should not forbid include/includedir directives after sections (**Bug** - P4)


### Reviewers
 * [Valerie Peng](https://openjdk.org/census#valeriep) (@valeriepeng - **Reviewer**)

### Reviewing
<details><summary>Using <code>git</code></summary>

Checkout this PR locally: \
`$ git fetch https://git.openjdk.org/jdk.git pull/25421/head:pull/25421` \
`$ git checkout pull/25421`

Update a local copy of the PR: \
`$ git checkout pull/25421` \
`$ git pull https://git.openjdk.org/jdk.git pull/25421/head`

</details>
<details><summary>Using Skara CLI tools</summary>

Checkout this PR locally: \
`$ git pr checkout 25421`

View PR using the GUI difftool: \
`$ git pr show -t 25421`

</details>
<details><summary>Using diff file</summary>

Download this PR as a diff file: \
<a href="https://git.openjdk.org/jdk/pull/25421.diff">https://git.open

---

> **生成时间**: 2026-03-25

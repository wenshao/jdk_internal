# JDK-8356838: AOT incorrectly sets a cached class's loader type to boot

## 基本信息

| 属性 | 值 |
|------|-----|
| **Issue** | [JDK-8356838](https://bugs.openjdk.org/browse/JDK-8356838) |
| **PR** | [#25199](https://github.com/openjdk/jdk/pull/25199) |
| **Author** | iklam |
| **组织** | Oracle |
| **Created** | 2025-05-13 |
| **Merged** | 2025-05-14 |
| **审查天数** | 1 |
| **版本** | JDK 25 |
| **优先级** | P4 |
| **组件** | hotspot |
| **模块** | runtime/cds |
| **代码变更** | +181 / -27 (8 files) |
| **审查者** | calvinccheung, veresov (2 人) |

## 描述

When the AOT training run sees a class `X` loaded by a custom class loader, we should set `X`'s loader type to `unreg`. However, since [JDK-8348426](https://bugs.openjdk.org/browse/JDK-8348426), if the classfile for `X` can also be found under the module path, we will incorrectly set the loader type to `boot`. See `test3()` in the new test code.

The fix is to check for custom class loaders in `ClassLoader::record_result()` before checking the `classpath_index`.

I also cleaned up the code in `ClassLoader::record_result()` and `ClassLoaderExt::record_result_for_builtin_loader()` to reduce redundancy and improve readability.

I added `AOTClassLocationConfig::print()` which can be called inside gdb for debugging purposes.

<!-- Anything below this marker will be automatically updated, please do not edit manually! -->
---------
### Progress
- [x] Change must be properly reviewed (1 review required, with at least 1 [Reviewer](https://openjdk.org/bylaws#reviewer))
- [x] Change must not contain extraneous whitespace
- [x] Commit message must refer to an issue

### Issue
 * [JDK-8356838](https://bugs.openjdk.org/browse/JDK-8356838): AOT incorrectly sets a cached class's loader type to boot (**Bug** - P4)


### Reviewers
 * [Igor Veresov](https://openjdk.org/census#iveresov) (@veresov - **Reviewer**) Review applies to [542dc0c2](https://git.openjdk.org/jdk/pull/25199/files/542dc0c2911e5fe7eb03452d4e91e4e02aa7b256)
 * [Calvin Cheung](https://openjdk.org/census#ccheung) (@calvinccheung - **Reviewer**)

### Reviewing
<details><summary>Using <code>git</code></summary>

Checkout this PR locally: \
`$ git fetch https://git.openjdk.org/jdk.git pull/25199/head:pull/25199` \
`$ git checkout pull/25199`

Update a local copy of the PR: \
`$ git checkout pull/25199` \
`$ git pull https://git.openjdk.org/jdk.git pull/25199/head`

</details>
<details><summary>Using Skara CLI tools</summary>

Checkout this PR locally: \
`$ git pr checkout 25199`

View PR using the GUI difftool: \
`$

---

> **生成时间**: 2026-03-25

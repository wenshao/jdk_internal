# JDK-8370766: JVM crashes when running compiler/exceptions/TestAccessErrorInCatch.java fails with -XX:+VerifyStack

## 基本信息

| 属性 | 值 |
|------|-----|
| **Issue** | [JDK-8370766](https://bugs.openjdk.org/browse/JDK-8370766) |
| **PR** | [#28486](https://github.com/openjdk/jdk/pull/28486) |
| **Author** | dean-long |
| **组织** | Oracle |
| **Created** | 2025-11-25 |
| **Merged** | 2025-12-02 |
| **审查天数** | 7 |
| **版本** | JDK 26 |
| **优先级** | P3 |
| **组件** | hotspot |
| **模块** | compiler |
| **代码变更** | +14 / -7 (2 files) |
| **审查者** | chhagedorn, dean-long, mhaessig, sendaoYan (4 人) |

## 描述

The problem is C2 is throwing an exception and then deoptimizing, and the -XX:+VerifyStack logic expects the stack to be empty, match the "before" state if the reexecute flag is set, or match the "after" state.  C2 is using the "before" state, so for correctness it also needs to set the reexecute flag.

I played around with other approaches, like:
1. setting the stack to empty
2. adding all the bytecodes that can throw to the list in AbstractInterpreter::bytecode_should_reexecute()
3. always setting the reexecute flag in add_safepoint_edges() if must_throw is set
but in the end I decided to go with the minimal localized low-risk change.

<!-- Anything below this marker will be automatically updated, please do not edit manually! -->
---------
### Progress
- [x] Change must be properly reviewed (1 review required, with at least 1 [Reviewer](https://openjdk.org/bylaws#reviewer))
- [x] Change must not contain extraneous whitespace
- [x] Commit message must refer to an issue

### Issue
 * [JDK-8370766](https://bugs.openjdk.org/browse/JDK-8370766): JVM crashes when running compiler/exceptions/TestAccessErrorInCatch.java fails with -XX:+VerifyStack (**Bug** - P3)


### Reviewers
 * [Manuel Hässig](https://openjdk.org/census#mhaessig) (@mhaessig - Committer)
 * [Christian Hagedorn](https://openjdk.org/census#chagedorn) (@chhagedorn - **Reviewer**)

### Contributors
 * Manuel Hässig `<mhaessig@openjdk.org>`

### Reviewing
<details><summary>Using <code>git</code></summary>

Checkout this PR locally: \
`$ git fetch https://git.openjdk.org/jdk.git pull/28486/head:pull/28486` \
`$ git checkout pull/28486`

Update a local copy of the PR: \
`$ git checkout pull/28486` \
`$ git pull https://git.openjdk.org/jdk.git pull/28486/head`

</details>
<details><summary>Using Skara CLI tools</summary>

Checkout this PR locally: \
`$ git pr checkout 28486`

View PR using the GUI difftool: \
`$ git pr show -t 28486`

</details>
<details><summary>Using diff file</summary>

Download this P

---

> **生成时间**: 2026-03-25

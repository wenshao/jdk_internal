# JDK-8273358: macOS Monterey does not have the font Times needed by Serif

## 基本信息

| 属性 | 值 |
|------|-----|
| **Issue** | [JDK-8273358](https://bugs.openjdk.org/browse/JDK-8273358) |
| **PR** | [#5420](https://github.com/openjdk/jdk/pull/5420) |
| **Author** | prrace |
| **组织** | Oracle |
| **Created** | 2021-09-08 |
| **Merged** | 2021-09-10 |
| **审查天数** | 2 |
| **版本** | JDK 18 |
| **优先级** | P2 |
| **组件** | client-libs |
| **模块** | client |
| **代码变更** | +65 / -3 (2 files) |
| **审查者** | azuev-java, prrace, prsadhuk (3 人) |

## 描述

Times has been the hard coded name for Serif in the JDK sources for macOS since the port was contributed by Apple.
But it seems that macOS Monterey doesn't have this font.
And the hard-coded fallback is also Times ! So we end up returning Dialog which is very bad.

The safest fix here is to make Times New Roman the fall back, so that on older macOS versions there
is absolutely no change. Only on Monterey and presumably later, will it use Times New Roman which,
I suspect, is what Times actually ends up being anyway.

Also to prevent unconditional warnings being printed I needed to change those to be conditional on logging being set.

<!-- Anything below this marker will be automatically updated, please do not edit manually! -->
---------
### Progress
- [x] Change must not contain extraneous whitespace
- [x] Commit message must refer to an issue
- [x] Change must be properly reviewed

### Issue
 * [JDK-8273358](https://bugs.openjdk.java.net/browse/JDK-8273358): macOS Monterey does not have the font Times needed by Serif


### Reviewers
 * [Alexander Zuev](https://openjdk.java.net/census#kizune) (@azuev-java - **Reviewer**)
 * [Alexey Ivanov](https://openjdk.java.net/census#aivanov) (@aivanov-jdk - **Reviewer**)


### Reviewing
<details><summary>Using <code>git</code></summary>

Checkout this PR locally: \
`$ git fetch https://git.openjdk.java.net/jdk pull/5420/head:pull/5420` \
`$ git checkout pull/5420`

Update a local copy of the PR: \
`$ git checkout pull/5420` \
`$ git pull https://git.openjdk.java.net/jdk pull/5420/head`

</details>
<details><summary>Using Skara CLI tools</summary>

Checkout this PR locally: \
`$ git pr checkout 5420`

View PR using the GUI difftool: \
`$ git pr show -t 5420`

</details>
<details><summary>Using diff file</summary>

Download this PR as a diff file: \
<a href="https://git.openjdk.java.net/jdk/pull/5420.diff">https://git.openjdk.java.net/jdk/pull/5420.diff</a>

</details>

## 变更文件

| 文件 | 状态 | +/- |
|------|------|-----|
| src/java.desktop/macosx/classes/sun/font/CFontManager.java | modified | +14/-3 |
| test/jdk/java/awt/FontClass/LogicalFontsTest.java | added | +51/-0 |

## 审查讨论

- **prsadhuk**: Can we do it under macos >= Monterey check so that we are sure it's not affecting older releases just as we do for JTabbedPane legibility issue using 
- **prsadhuk**: Is othervm option needed as we are not setting new paramter in command line?
- **prsadhuk**: Just to clarify, I see those 2 fonts in BigSur and Catalina..


> **Backported to**: JDK 11u

---

> **生成时间**: 2026-03-25

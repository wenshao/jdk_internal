# JDK-8200610: Compiling fails with java.nio.file.ReadOnlyFileSystemException

## 基本信息

| 属性 | 值 |
|------|-----|
| **Issue** | [JDK-8200610](https://bugs.openjdk.org/browse/JDK-8200610) |
| **PR** | [#10818](https://github.com/openjdk/jdk/pull/10818) |
| **Author** | archiecobbs |
| **组织** | Oracle |
| **Created** | 2022-10-21 |
| **Merged** | 2023-01-04 |
| **审查天数** | 75 |
| **版本** | JDK 20 |
| **优先级** | P3 |
| **组件** | tools |
| **模块** | core-libs/java.io |
| **代码变更** | +8 / -1 (1 files) |
| **审查者** | archiecobbs, jonathan-gibbons, vicente-romero-oracle (3 人) |

## 描述

In `JavaCompiler`, when trying to write a class file, if an `IOException`s is thrown, instead of crashing, we report a normal error" `error while writing [file]: ...`.

However there are some I/O errors that can generate `RuntimeException`s not caught by this logic. So instead these errors cause a compiler crash.

An example is given in JDK-8200610, where the compiler tries to write into a ZIP file system and a `java.nio.file.ReadOnlyFileSystemException` is thrown.

This patch adds that exception and a couple of others which (in my estimation) could reasonably be expected to be thrown in various obscure cases involving alternative file systems: `FileSystemNotFoundException` and `InvalidPathException`.

Any suggestions on what the most appropriate list of exception types to include here are appreciated.

<!-- Anything below this marker will be automatically updated, please do not edit manually! -->
---------
### Progress
- [x] Change must be properly reviewed (1 review required, with at least 1 [Reviewer](https://openjdk.org/bylaws#reviewer))
- [x] Change must not contain extraneous whitespace
- [x] Commit message must refer to an issue

### Issue
 * [JDK-8200610](https://bugs.openjdk.org/browse/JDK-8200610): Compiling fails with java.nio.file.ReadOnlyFileSystemException


### Reviewers
 * [Vicente Romero](https://openjdk.org/census#vromero) (@vicente-romero-oracle - **Reviewer**) ⚠️ Review applies to [1bcf9501](https://git.openjdk.org/jdk/pull/10818/files/1bcf9501441bcda9c60fe1c6e3b5162c52c632c3)


### Reviewing
<details><summary>Using <code>git</code></summary>

Checkout this PR locally: \
`$ git fetch https://git.openjdk.org/jdk pull/10818/head:pull/10818` \
`$ git checkout pull/10818`

Update a local copy of the PR: \
`$ git checkout pull/10818` \
`$ git pull https://git.openjdk.org/jdk pull/10818/head`

</details>
<details><summary>Using Skara CLI tools</summary>

Checkout this PR locally: \
`$ git pr checkout 10818`

View PR using the GUI difftool: \
`$

---

> **生成时间**: 2026-03-25

# JDK-8349620: Add VMProps for static JDK

## 基本信息

| 属性 | 值 |
|------|-----|
| **Issue** | [JDK-8349620](https://bugs.openjdk.org/browse/JDK-8349620) |
| **PR** | [#23528](https://github.com/openjdk/jdk/pull/23528) |
| **Author** | jianglizhou |
| **组织** | Oracle |
| **Created** | 2025-02-07 |
| **Merged** | 2025-02-20 |
| **审查天数** | 13 |
| **版本** | JDK 25 |
| **优先级** | P4 |
| **组件** | core-libs |
| **模块** | core-libs |
| **代码变更** | +19 / -3 (6 files) |
| **审查者** | AlanBateman, caoman (2 人) |

## 描述

Please review this change that adds the `jdk.static` VMProps. It can be used to skip tests not for running on static JDK.

This also adds a new WhiteBox native method, `jdk.test.whitebox.WhiteBox.isStatic()`, which is used by VMProps to determine if it's static at runtime. 

`@requires !jdk.static` is added in `test/hotspot/jtreg/runtime/modules/ModulesSymLink.java` to skip running the test on static JDK. This test uses `bin/jlink`, which is not provided on static JDK. There are other tests that require tools in `bin/`. Those are not modified by the current PR to skip running on static JDK. Those can be done after the current change is fully discussed and reviewed/approved.

<!-- Anything below this marker will be automatically updated, please do not edit manually! -->
---------
### Progress
- [x] Change must be properly reviewed (1 review required, with at least 1 [Reviewer](https://openjdk.org/bylaws#reviewer))
- [x] Change must not contain extraneous whitespace
- [x] Commit message must refer to an issue

### Issue
 * [JDK-8349620](https://bugs.openjdk.org/browse/JDK-8349620): Add VMProps for static JDK (**Enhancement** - P4)


### Reviewers
 * [Alan Bateman](https://openjdk.org/census#alanb) (@AlanBateman - **Reviewer**)
 * [Man Cao](https://openjdk.org/census#manc) (@caoman - Committer)

### Reviewing
<details><summary>Using <code>git</code></summary>

Checkout this PR locally: \
`$ git fetch https://git.openjdk.org/jdk.git pull/23528/head:pull/23528` \
`$ git checkout pull/23528`

Update a local copy of the PR: \
`$ git checkout pull/23528` \
`$ git pull https://git.openjdk.org/jdk.git pull/23528/head`

</details>
<details><summary>Using Skara CLI tools</summary>

Checkout this PR locally: \
`$ git pr checkout 23528`

View PR using the GUI difftool: \
`$ git pr show -t 23528`

</details>
<details><summary>Using diff file</summary>

Download this PR as a diff file: \
<a href="https://git.openjdk.org/jdk/pull/23528.diff">https://git.openjdk.org/jdk/pull/2352

---

> **生成时间**: 2026-03-25

# JDK-8349914: ZipFile::entries and ZipFile::getInputStream not consistent with each other when there are duplicate entries

## 基本信息

| 属性 | 值 |
|------|-----|
| **Issue** | [JDK-8349914](https://bugs.openjdk.org/browse/JDK-8349914) |
| **PR** | [#25635](https://github.com/openjdk/jdk/pull/25635) |
| **Author** | jaikiran |
| **组织** | Oracle |
| **Created** | 2025-06-04 |
| **Merged** | 2025-06-06 |
| **审查天数** | 2 |
| **版本** | JDK 25 |
| **优先级** | P4 |
| **组件** | core-libs |
| **模块** | core-libs/java.io |
| **代码变更** | +237 / -4 (3 files) |
| **审查者** | LanceAndersen (1 人) |

## 描述

Can I please get a review of this change which proposes to address the issue noted in https://bugs.openjdk.org/browse/JDK-8349914?

The ZIP specification allows for more than one entry to have the same file name (and may have different file data). In such situation, as noted in the linked issue, the `ZipFile.getInputStream(ZipEntry)` may end up picking up an "incorrect" entry content (and metadata) for the passed entry.

The commit in this PR addresses that issue by holding on to the LOC offset of the entry in the `ZipEntry` class. This way, whenever that `ZipEntry` instance is used, it knows which exact entry it represents.

A new jtreg test has been introduced to reproduce the issue and verify the fix. The new test and existing tests in tier1, tier2 and tier3 pass with this change.

<!-- Anything below this marker will be automatically updated, please do not edit manually! -->
---------
### Progress
- [x] Change must be properly reviewed (1 review required, with at least 1 [Reviewer](https://openjdk.org/bylaws#reviewer))
- [x] Change must not contain extraneous whitespace
- [x] Commit message must refer to an issue

### Issue
 * [JDK-8349914](https://bugs.openjdk.org/browse/JDK-8349914): ZipFile::entries and ZipFile::getInputStream not consistent with each other when there are duplicate entries (**Bug** - P4)


### Reviewers
 * [Lance Andersen](https://openjdk.org/census#lancea) (@LanceAndersen - **Reviewer**)

### Contributors
 * Lance Andersen `<lancea@openjdk.org>`

### Reviewing
<details><summary>Using <code>git</code></summary>

Checkout this PR locally: \
`$ git fetch https://git.openjdk.org/jdk.git pull/25635/head:pull/25635` \
`$ git checkout pull/25635`

Update a local copy of the PR: \
`$ git checkout pull/25635` \
`$ git pull https://git.openjdk.org/jdk.git pull/25635/head`

</details>
<details><summary>Using Skara CLI tools</summary>

Checkout this PR locally: \
`$ git pr checkout 25635`

View PR using the GUI difftool: \
`$ git pr show -t 25635`

---

> **生成时间**: 2026-03-25

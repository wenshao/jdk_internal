# JDK-8342382: Implement JEP 522: G1 GC: Improve Throughput by Reducing Synchronization

> **JEP 522: G1 GC: Improve Throughput by Reducing Synchronization** | Closed / Delivered

## 基本信息

| 属性 | 值 |
|------|-----|
| **Issue** | [JDK-8342382](https://bugs.openjdk.org/browse/JDK-8342382) |
| **PR** | [#23739](https://github.com/openjdk/jdk/pull/23739) |
| **Author** | tschatzl |
| **组织** | Oracle |
| **Created** | 2025-02-23 |
| **Merged** | 2025-09-22 |
| **审查天数** | 211 |
| **版本** | JDK 25 |
| **优先级** | P4 |
| **组件** | hotspot |
| **代码变更** | +3572 / -4628 (114 文件) |
| **审查者** | TheRealMDoerr, albertnetymk, tschatzl, walulyai (4 人) |

## PR 描述

Hi all,

  please review this change that implements (currently Draft) JEP: G1: Improve Application Throughput with a More Efficient Write-Barrier.

The reason for posting this early is that this is a large change, and the JEP process is already taking very long with no end in sight but we would like to have this ready by JDK 25.

### Current situation

With this change, G1 will reduce the post write barrier to much more resemble Parallel GC's as described in the JEP. The reason is that G1 lacks in throughput compared to Parallel/Serial GC due to larger barrier.

The main reason for the current barrier is how g1 implements concurrent refinement:
* g1 tracks dirtied cards using sets (dirty card queue set - dcqs) of buffers (dirty card queues - dcq) containing the location of dirtied cards. Refinement threads pick up their contents to re-refine. The barrier needs to enqueue card locations.
* For correctness dirty card updates requires fine-grained synchronization between mutator and refinement threads,
* Finally there is generic code to avoid dirtying cards altogether (filters), to avoid executing the synchronization and the enqueuing as much as possible.

These tasks require the current barrier to look as follows for an assignment `x.a = y` in pseudo code:

```
// Filtering
if (region(@x.a) == region(y)) goto done; // same region check
if (y == null) goto done;     // null value check
if (card(@x.a) == young_card) goto done;  // write to young gen check
S

...(截断)

## 变更文件

| 文件 | 状态 | +/- |
|------|------|-----|
| src/hotspot/cpu/aarch64/gc/g1/g1BarrierSetAssembler_aarch64.cpp | modified | +73/-166 |
| src/hotspot/cpu/aarch64/gc/g1/g1BarrierSetAssembler_aarch64.hpp | modified | +9/-8 |
| src/hotspot/cpu/aarch64/gc/g1/g1_aarch64.ad | modified | +4/-4 |
| src/hotspot/cpu/arm/gc/g1/g1BarrierSetAssembler_arm.cpp | modified | +38/-199 |
| src/hotspot/cpu/arm/gc/g1/g1BarrierSetAssembler_arm.hpp | modified | +9/-8 |
| src/hotspot/cpu/arm/gc/g1/g1_arm.ad | modified | +4/-4 |
| src/hotspot/cpu/ppc/gc/g1/g1BarrierSetAssembler_ppc.cpp | modified | +51/-209 |
| src/hotspot/cpu/ppc/gc/g1/g1BarrierSetAssembler_ppc.hpp | modified | +11/-10 |
| src/hotspot/cpu/ppc/gc/g1/g1_ppc.ad | modified | +4/-4 |
| src/hotspot/cpu/riscv/gc/g1/g1BarrierSetAssembler_riscv.cpp | modified | +80/-163 |
| src/hotspot/cpu/riscv/gc/g1/g1BarrierSetAssembler_riscv.hpp | modified | +10/-8 |
| src/hotspot/cpu/riscv/gc/g1/g1_riscv.ad | modified | +4/-4 |
| src/hotspot/cpu/s390/gc/g1/g1BarrierSetAssembler_s390.cpp | modified | +59/-272 |
| src/hotspot/cpu/s390/gc/g1/g1BarrierSetAssembler_s390.hpp | modified | +10/-8 |
| src/hotspot/cpu/s390/gc/g1/g1_s390.ad | modified | +4/-4 |

> 共 114 个文件变更

## JEP 522 详情

- **标题**: G1 GC: Improve Throughput by Reducing Synchronization
- **负责人**: Ivan Walulya
- **状态**: Closed / Delivered
- **目标版本**: JDK 26
- **链接**: [openjdk.org/jeps/522](https://openjdk.org/jeps/522)

## 审查讨论摘要

- **TheRealMDoerr**: Comment: R0 is used instead of tmp1
- **TheRealMDoerr**: Comment: tmp2 is used, here
- **TheRealMDoerr**: Can you remove the comment, please? It's wrong.

---

> **数据来源**: GitHub API + JBS | **生成时间**: 2026-03-25

# JDK-8331193: Return references when possible in GrowableArray

## 基本信息

| 属性 | 值 |
|------|-----|
| **Issue** | [JDK-8331193](https://bugs.openjdk.org/browse/JDK-8331193) |
| **PR** | [#18975](https://github.com/openjdk/jdk/pull/18975) |
| **Author** | johan-sjolen |
| **组织** | Oracle |
| **Created** | 2024-04-26 |
| **Merged** | 2024-06-04 |
| **审查天数** | 39 |
| **版本** | JDK 23 |
| **优先级** | P4 |
| **组件** | hotspot |
| **模块** | hotspot |
| **代码变更** | +46 / -5 (2 files) |
| **审查者** | eme64, johan-sjolen, kimbarrett, stefank, turbanoff (5 人) |

## 描述

Hi,

This PR introduces the possibility of using references more often when using GrowableArray, where as previously this was only possible when using the `at()` method. This lets us avoid copying and redundant method calls and makes the API more streamlined. After the patch, we can use `at_grow` just like `at` works. The same goes for `top`, `first`, and `last`.


Some example code:
```c++
// Before this patch this worked:
GrowableArray<int> arr(8,8,-1); // Pre-fill with 8 -1s
int& x = arr.at(7);
if (x == -1) {
  x = 2;
}
assert(arr.at(7) == 2, "this holds");
// but this was forbidden
int& x = arr.at_grow(9, -1); // Compilation error! at_grow returns E, not E&
// so we had to do
int x = arr.at_grow(9, -1);
if (x == -1) {
  arr.at_put(9, 2);
}
```

Thanks.

<!-- Anything below this marker will be automatically updated, please do not edit manually! -->
---------
### Progress
- [x] Change must be properly reviewed (1 review required, with at least 1 [Reviewer](https://openjdk.org/bylaws#reviewer))
- [x] Change must not contain extraneous whitespace
- [x] Commit message must refer to an issue

### Issue
 * [JDK-8331193](https://bugs.openjdk.org/browse/JDK-8331193): Return references when possible in GrowableArray (**Enhancement** - P4)


### Reviewers
 * [Stefan Karlsson](https://openjdk.org/census#stefank) (@stefank - **Reviewer**) ⚠️ Review applies to [7a575e5a](https://git.openjdk.org/jdk/pull/18975/files/7a575e5ae32be25e70b3dbe1d1dd830eb76b19bf)
 * [Kim Barrett](https://openjdk.org/census#kbarrett) (@kimbarrett - **Reviewer**) ⚠️ Review applies to [7a575e5a](https://git.openjdk.org/jdk/pull/18975/files/7a575e5ae32be25e70b3dbe1d1dd830eb76b19bf)
 * [Emanuel Peter](https://openjdk.org/census#epeter) (@eme64 - **Reviewer**)


### Reviewing
<details><summary>Using <code>git</code></summary>

Checkout this PR locally: \
`$ git fetch https://git.openjdk.org/jdk.git pull/18975/head:pull/18975` \
`$ git checkout pull/18975`

Update a local copy of the

---

> **生成时间**: 2026-03-25

# JDK-8331671: Implement JEP 472: Prepare to Restrict the Use of JNI

> **JEP 472: Prepare to Restrict the Use of JNI** | Closed / Delivered

## 基本信息

| 属性 | 值 |
|------|-----|
| **Issue** | [JDK-8331671](https://bugs.openjdk.org/browse/JDK-8331671) |
| **PR** | [#19213](https://github.com/openjdk/jdk/pull/19213) |
| **Author** | mcimadamore |
| **组织** | Oracle |
| **Created** | 2024-05-13 |
| **Merged** | 2024-08-26 |
| **审查天数** | 105 |
| **版本** | JDK 23 |
| **优先级** | P4 |
| **组件** | core-libs |
| **代码变更** | +552 / -183 (107 文件) |
| **审查者** | AlanBateman, ExE-Boss, dholmes-ora, jaikiran, mcimadamore (5 人) |

## PR 描述

This PR implements [JEP 472](https://openjdk.org/jeps/472), by restricting the use of JNI in the following ways:

* `System::load` and `System::loadLibrary` are now restricted methods
* `Runtime::load` and `Runtime::loadLibrary` are now restricted methods
* binding a JNI `native` method declaration to a native implementation is now considered a restricted operation

This PR slightly changes the way in which the JDK deals with restricted methods, even for FFM API calls. In Java 22, the single `--enable-native-access` was used both to specify a set of modules for which native access should be allowed *and* to specify whether illegal native access (that is, native access occurring from a module not specified by `--enable-native-access`) should be treated as an error or a warning. More specifically, an error is only issued if the `--enable-native-access flag` is used at least once.

Here, a new flag is introduced, namely `illegal-native-access=allow/warn/deny`, which is used to specify what should happen when access to a restricted method and/or functionality is found outside the set of modules specified with `--enable-native-access`. The default policy is `warn`, but users can select `allow` to suppress the warnings, or `deny` to cause `IllegalCallerException` to be thrown. This aligns the treatment of restricted methods with other mechanisms, such as `--illegal-access` and the more recent `--sun-misc-unsafe-memory-access`.

Some changes were required in the package-in

...(截断)

## 变更文件

| 文件 | 状态 | +/- |
|------|------|-----|
| make/conf/module-loader-map.conf | modified | +5/-27 |
| make/test/BuildTestLib.gmk | modified | +1/-1 |
| src/hotspot/share/classfile/vmClassMacros.hpp | modified | +1/-0 |
| src/hotspot/share/classfile/vmSymbols.hpp | modified | +2/-1 |
| src/hotspot/share/prims/nativeLookup.cpp | modified | +19/-5 |
| src/hotspot/share/runtime/arguments.cpp | modified | +7/-0 |
| src/java.base/share/classes/java/lang/ClassLoader.java | modified | +19/-2 |
| src/java.base/share/classes/java/lang/Module.java | modified | +31/-12 |
| src/java.base/share/classes/java/lang/ModuleLayer.java | modified | +1/-1 |
| src/java.base/share/classes/java/lang/Runtime.java | modified | +14/-3 |
| src/java.base/share/classes/java/lang/System.java | modified | +17/-6 |
| src/java.base/share/classes/java/lang/foreign/AddressLayout.java | modified | +1/-1 |
| src/java.base/share/classes/java/lang/foreign/Linker.java | modified | +3/-3 |
| src/java.base/share/classes/java/lang/foreign/SymbolLookup.java | modified | +4/-4 |
| src/java.base/share/classes/java/lang/foreign/package-info.java | modified | +5/-4 |

> 共 107 个文件变更

## JEP 472 详情

- **标题**: Prepare to Restrict the Use of JNI
- **负责人**: Ron Pressler
- **状态**: Closed / Delivered
- **目标版本**: JDK 24
- **链接**: [openjdk.org/jeps/472](https://openjdk.org/jeps/472)

## 审查讨论摘要

- **mcimadamore**: The list of allowed modules has been rewritten from scratch, by looking at the set of modules containing at least one `native` method declaration.
- **mcimadamore**: This is the biggest change in this PR. That is, we need to pass enough arguments to `ClassLoader::findNative` so that the method can start a restricte
- **mcimadamore**: There are some changes in this code: * this code is no-op if `--illegal-native-access` is set to `allow` * we also attach the location of the proble

---

> **数据来源**: GitHub API + JBS | **生成时间**: 2026-03-25

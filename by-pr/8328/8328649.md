# JDK-8328649: Disallow enclosing instances for local classes in constructor prologues

## 基本信息

| 属性 | 值 |
|------|-----|
| **Issue** | [JDK-8328649](https://bugs.openjdk.org/browse/JDK-8328649) |
| **PR** | [#18416](https://github.com/openjdk/jdk/pull/18416) |
| **Author** | archiecobbs |
| **组织** | Oracle |
| **Created** | 2024-03-21 |
| **Merged** | 2024-04-04 |
| **审查天数** | 14 |
| **版本** | JDK 23 |
| **优先级** | P4 |
| **组件** | tools |
| **模块** | arch/x86 |
| **代码变更** | +40 / -33 (5 files) |
| **审查者** | vicente-romero-oracle (1 人) |

## 描述

A local class declared in a static context is not supposed to have an immediately enclosing instance (§15.9.2). That includes local classes declared in constructors prior to `super()` (during the "constructor prologue" in the new lingo).

However, the compiler is allowing code like this to successfully compile:
```java
import java.util.concurrent.atomic.*;
public class Test extends AtomicReference<Object> {
  
    public Test() {
        super(switch (0) {
          default -> {
              class Local { { Test.this.hashCode(); } }   // this should fail
              yield null;
            }
        });
    }
}
```
This patch fixes the "leak".

<!-- Anything below this marker will be automatically updated, please do not edit manually! -->
---------
### Progress
- [x] Change must be properly reviewed (1 review required, with at least 1 [Reviewer](https://openjdk.org/bylaws#reviewer))
- [x] Change must not contain extraneous whitespace
- [x] Commit message must refer to an issue
- [x] Change requires CSR request [JDK-8328706](https://bugs.openjdk.org/browse/JDK-8328706) to be approved

### Issues
 * [JDK-8328649](https://bugs.openjdk.org/browse/JDK-8328649): Disallow enclosing instances for local classes in constructor prologues (**Bug** - P4)
 * [JDK-8328706](https://bugs.openjdk.org/browse/JDK-8328706): Disallow enclosing instances for local classes in constructor prologues (**CSR**)


### Reviewers
 * [Vicente Romero](https://openjdk.org/census#vromero) (@vicente-romero-oracle - **Reviewer**)


### Reviewing
<details><summary>Using <code>git</code></summary>

Checkout this PR locally: \
`$ git fetch https://git.openjdk.org/jdk.git pull/18416/head:pull/18416` \
`$ git checkout pull/18416`

Update a local copy of the PR: \
`$ git checkout pull/18416` \
`$ git pull https://git.openjdk.org/jdk.git pull/18416/head`

</details>
<details><summary>Using Skara CLI tools</summary>

Checkout this PR locally: \
`$ git pr checkout 18416`

View PR using the G

---

> **生成时间**: 2026-03-25

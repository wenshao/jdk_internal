# JDK-8298730: Refactor subsystem_file_line_contents and add docs and tests

## 基本信息

| 属性 | 值 |
|------|-----|
| **Issue** | [JDK-8298730](https://bugs.openjdk.org/browse/JDK-8298730) |
| **PR** | [#11667](https://github.com/openjdk/jdk/pull/11667) |
| **Author** | johan-sjolen |
| **组织** | Oracle |
| **Created** | 2022-12-14 |
| **Merged** | 2023-01-09 |
| **审查天数** | 26 |
| **版本** | JDK 20 |
| **优先级** | P4 |
| **组件** | hotspot |
| **模块** | core-libs/java.io |
| **代码变更** | +264 / -60 (5 files) |
| **审查者** | iklam, jerboaa, johan-sjolen (3 人) |

## 描述

Hi!

Citing the ticket directly:

The function subsystem_file_line_contents does the simple job of parsing lines in a file, but does so in a fairly complex way. This function can be simplified. The function also has a surprising API, as you need to know that in one case the format string needs to take 2 specifiers, instead of 1. 

Some more context:

`subsystem_file_line_contents` either parses files that look like this:

```
one_value
```

Called as: `subsystem_file_line_contents(ctrl, fname, nullptr, "%s")`

Or like this:

```
key1 val1
key2 val2
```

Called as:  `subsystem_file_line_contents(ctrl, fname, "key1", "%s %s")`

The API for the key/value case is changed to: `subsystem_file_line_contents(ctrl, fname, "key1", "%s")`. Note: `"%s"`, not `"%s %s"`.

<!-- Anything below this marker will be automatically updated, please do not edit manually! -->
---------
### Progress
- [x] Change must be properly reviewed (1 review required, with at least 1 [Reviewer](https://openjdk.org/bylaws#reviewer))
- [x] Change must not contain extraneous whitespace
- [x] Commit message must refer to an issue

### Issue
 * [JDK-8298730](https://bugs.openjdk.org/browse/JDK-8298730): Refactor subsystem_file_line_contents and add docs and tests


### Reviewers
 * [Severin Gehwolf](https://openjdk.org/census#sgehwolf) (@jerboaa - **Reviewer**) ⚠️ Review applies to [6488af3b](https://git.openjdk.org/jdk/pull/11667/files/6488af3b4f17ce70e6190baa4f66203afadca917)
 * [Ioi Lam](https://openjdk.org/census#iklam) (@iklam - **Reviewer**) ⚠️ Review applies to [5a17527d](https://git.openjdk.org/jdk/pull/11667/files/5a17527d583a4a3880a0b83652757155bcc29d15)


### Reviewing
<details><summary>Using <code>git</code></summary>

Checkout this PR locally: \
`$ git fetch https://git.openjdk.org/jdk pull/11667/head:pull/11667` \
`$ git checkout pull/11667`

Update a local copy of the PR: \
`$ git checkout pull/11667` \
`$ git pull https://git.openjdk.org/jdk pull/11667/head`

</deta


> **Backported to**: JDK 11u, JDK 17u

---

> **生成时间**: 2026-03-25

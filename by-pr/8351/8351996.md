# JDK-8351996: Behavioral updates for ClassValue::remove

## 基本信息

| 属性 | 值 |
|------|-----|
| **Issue** | [JDK-8351996](https://bugs.openjdk.org/browse/JDK-8351996) |
| **PR** | [#24043](https://github.com/openjdk/jdk/pull/24043) |
| **Author** | liach |
| **组织** | Oracle |
| **Created** | 2025-03-14 |
| **Merged** | 2025-05-15 |
| **审查天数** | 62 |
| **版本** | JDK 25 |
| **优先级** | P3 |
| **组件** | core-libs |
| **模块** | core-libs |
| **代码变更** | +509 / -259 (2 files) |
| **审查者** | ExE-Boss, jaikiran, liach, rose00, viktorklang-ora, wenshao (6 人) |

## 描述

The recent patch #23866 makes calling `ClassValue::remove()` from `ClassValue::computeValue()` end up in infinite loops while fixing the stale value risk from the method.

The proposed fix is to preserve the stale value risk fix, and update the remove-from-compute behavior from the original designated no-op behavior to throwing an exception, as the original behavior conflicts with the stale value fix.

The implementation track the owner thread in promises (accessed in locked section); as a result, we can fail-fast on recursive removals from `computeValue`. I did not choose to use `ThreadTracker` as it is designed for single tracker and multiple threads, while this case here sees often just one thread, and the threads outlive the promise objects.

Also updated the API specs for `remove` to more concisely describe the memory effects. Please review the associated CSR as well.

<!-- Anything below this marker will be automatically updated, please do not edit manually! -->
---------
### Progress
- [x] Change must be properly reviewed (1 review required, with at least 1 [Reviewer](https://openjdk.org/bylaws#reviewer))
- [x] Change must not contain extraneous whitespace
- [x] Commit message must refer to an issue
- [x] Change requires CSR request [JDK-8352433](https://bugs.openjdk.org/browse/JDK-8352433) to be approved

### Issues
 * [JDK-8351996](https://bugs.openjdk.org/browse/JDK-8351996): Behavioral updates for ClassValue::remove (**Bug** - P3)
 * [JDK-8352433](https://bugs.openjdk.org/browse/JDK-8352433): Behavioral updates for ClassValue::remove (**CSR**)


### Reviewers
 * [John R Rose](https://openjdk.org/census#jrose) (@rose00 - **Reviewer**) Review applies to [221d51e7](https://git.openjdk.org/jdk/pull/24043/files/221d51e78a7ee96397c99e874d2fbfaa246a0f14)
 * [Viktor Klang](https://openjdk.org/census#vklang) (@viktorklang-ora - **Reviewer**)

### Contributors
 * John R Rose `<jrose@openjdk.org>`

### Reviewing
<details><summary>Using <code>git</code></summar

---

> **生成时间**: 2026-03-25

# JDK-8374113: Taughtological if check in Reflection::array_set

## 基本信息

| 属性 | 值 |
|------|-----|
| **Issue** | [JDK-8374113](https://bugs.openjdk.org/browse/JDK-8374113) |
| **PR** | [#28921](https://github.com/openjdk/jdk/pull/28921) |
| **Author** | stefank |
| **组织** | Oracle |
| **Created** | 2025-12-19 |
| **Merged** | 2025-12-22 |
| **审查天数** | 3 |
| **版本** | JDK 26 |
| **优先级** | P4 |
| **组件** | hotspot |
| **模块** | hotspot |
| **代码变更** | +8 / -9 (1 files) |
| **审查者** | fparain, liach (2 人) |

## 描述

While reviewing code in the Valhalla repository I saw that Reflection::array_set has this funny-looking code:
```
  if (a->is_objArray()) {
    if (value_type == T_OBJECT) {
    ...
    } // what happens in the else case? here
  } else {
    assert(a->is_typeArray(), "just checking");
  }
```
And wondered what would happen if the `value_type == T_OBJECT` check failed, and how is this handled with flattened arrays, given that we have T_FLAT_ELEMENT there.

It turns out that the calling code passes down T_OBJECT even for flat arrays so they do take the `value_type == T_OBJECT` path, so that's fine.

So, I propose that we just replace one of the ifs with an assert.

<!-- Anything below this marker will be automatically updated, please do not edit manually! -->
---------
### Progress
- [x] Change must be properly reviewed (1 review required, with at least 1 [Reviewer](https://openjdk.org/bylaws#reviewer))
- [x] Change must not contain extraneous whitespace
- [x] Commit message must refer to an issue

### Issue
 * [JDK-8374113](https://bugs.openjdk.org/browse/JDK-8374113): Taughtological if check in Reflection::array_set (**Enhancement** - P4)


### Reviewers
 * [Frederic Parain](https://openjdk.org/census#fparain) (@fparain - **Reviewer**)
 * [Chen Liang](https://openjdk.org/census#liach) (@liach - **Reviewer**)

### Reviewing
<details><summary>Using <code>git</code></summary>

Checkout this PR locally: \
`$ git fetch https://git.openjdk.org/jdk.git pull/28921/head:pull/28921` \
`$ git checkout pull/28921`

Update a local copy of the PR: \
`$ git checkout pull/28921` \
`$ git pull https://git.openjdk.org/jdk.git pull/28921/head`

</details>
<details><summary>Using Skara CLI tools</summary>

Checkout this PR locally: \
`$ git pr checkout 28921`

View PR using the GUI difftool: \
`$ git pr show -t 28921`

</details>
<details><summary>Using diff file</summary>

Download this PR as a diff file: \
<a href="https://git.openjdk.org/jdk/pull/28921.diff">https://gi

---

> **生成时间**: 2026-03-25

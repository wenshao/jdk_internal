# JDK-8374489: Template Library: need to tag Float16.float16ToRawShortBits as having non-deterministic result because of multi

## 基本信息

| 属性 | 值 |
|------|-----|
| **Issue** | [JDK-8374489](https://bugs.openjdk.org/browse/JDK-8374489) |
| **PR** | [#29033](https://github.com/openjdk/jdk/pull/29033) |
| **Author** | eme64 |
| **组织** | Oracle |
| **Created** | 2026-01-05 |
| **Merged** | 2026-01-06 |
| **审查天数** | 1 |
| **版本** | JDK 26 |
| **优先级** | P4 |
| **组件** | hotspot |
| **模块** | hotspot |
| **代码变更** | +4 / -3 (1 files) |
| **审查者** | chhagedorn, vnkozlov (2 人) |

## 描述

In [JDK-8370922](https://bugs.openjdk.org/browse/JDK-8370922), I added Float16 support to the Template Library. I missed to tag Float16.float16ToRawShortBits as having non-deterministic result.

We need to do that, just like for float and double equivalents:
Arithmetic operations (e.g. add, mul, fma, ...) are allowed to pick either input if it gets two NaNs of different bit patterns.
That way, those operations can generate different NaN bit patterns depending on if we use interpreter/C1/C2.
If we now convert to raw bits, we get different bits, and would wrongly conclude that we get a wrong result.

Since this is a test-bug, I have no regression test. But I verified it manually, that with the same seed (for the ExpressionFuzzer) that fails before this change, we now succeed after the change.

<!-- Anything below this marker will be automatically updated, please do not edit manually! -->
---------
### Progress
- [x] Change must be properly reviewed (1 review required, with at least 1 [Reviewer](https://openjdk.org/bylaws#reviewer))
- [x] Change must not contain extraneous whitespace
- [x] Commit message must refer to an issue

### Issue
 * [JDK-8374489](https://bugs.openjdk.org/browse/JDK-8374489): Template Library: need to tag Float16.float16ToRawShortBits as having non-deterministic result because of multiple NaN bit patterns (**Bug** - P4)


### Reviewers
 * [Christian Hagedorn](https://openjdk.org/census#chagedorn) (@chhagedorn - **Reviewer**)
 * [Vladimir Kozlov](https://openjdk.org/census#kvn) (@vnkozlov - **Reviewer**)

### Reviewing
<details><summary>Using <code>git</code></summary>

Checkout this PR locally: \
`$ git fetch https://git.openjdk.org/jdk.git pull/29033/head:pull/29033` \
`$ git checkout pull/29033`

Update a local copy of the PR: \
`$ git checkout pull/29033` \
`$ git pull https://git.openjdk.org/jdk.git pull/29033/head`

</details>
<details><summary>Using Skara CLI tools</summary>

Checkout this PR locally: \
`$ git pr checkout 29033`

Vi

---

> **生成时间**: 2026-03-25

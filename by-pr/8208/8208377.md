# JDK-8208377: Soft hyphens render if not using TextLayout

## 基本信息

| 属性 | 值 |
|------|-----|
| **Issue** | [JDK-8208377](https://bugs.openjdk.org/browse/JDK-8208377) |
| **PR** | [#22670](https://github.com/openjdk/jdk/pull/22670) |
| **Author** | gredler |
| **组织** | Independent |
| **Created** | 2024-12-10 |
| **Merged** | 2025-02-11 |
| **审查天数** | 63 |
| **版本** | JDK 24 |
| **优先级** | P3 |
| **组件** | client-libs |
| **模块** | client |
| **代码变更** | +452 / -30 (6 files) |
| **审查者** | alisenchung, gredler, prrace (3 人) |

## 描述

Soft hyphens should never render, regardless of the rendering path used internally.

This PR does not expand the categorization of "complex" characters in `FontUtilities` in order to force the use of `TextLayout` rendering code paths (as was discussed in JBS).

Instead, it takes the existing (limited) format-category checks in `sun.font.CMap` (a TrueType font helper class), expands it to a more general / complete default-ignorable check (`FontUtilities.isDefaultIgnorable(int)`), and then moves these checks out of `CMap` and up a level into the `CharToGlyphMapper` classes themselves.

The Type1 glyph mapper, the TTF glyph mapper, and the macOS glyph mapper have all been updated.

<!-- Anything below this marker will be automatically updated, please do not edit manually! -->
---------
### Progress
- [x] Change must be properly reviewed (1 review required, with at least 1 [Reviewer](https://openjdk.org/bylaws#reviewer))
- [x] Change must not contain extraneous whitespace
- [x] Commit message must refer to an issue

### Issue
 * [JDK-8208377](https://bugs.openjdk.org/browse/JDK-8208377): Soft hyphens render if not using TextLayout (**Bug** - P3)


### Reviewers
 * [Alisen Chung](https://openjdk.org/census#achung) (@alisenchung - Committer)
 * [Phil Race](https://openjdk.org/census#prr) (@prrace - **Reviewer**)

### Reviewing
<details><summary>Using <code>git</code></summary>

Checkout this PR locally: \
`$ git fetch https://git.openjdk.org/jdk.git pull/22670/head:pull/22670` \
`$ git checkout pull/22670`

Update a local copy of the PR: \
`$ git checkout pull/22670` \
`$ git pull https://git.openjdk.org/jdk.git pull/22670/head`

</details>
<details><summary>Using Skara CLI tools</summary>

Checkout this PR locally: \
`$ git pr checkout 22670`

View PR using the GUI difftool: \
`$ git pr show -t 22670`

</details>
<details><summary>Using diff file</summary>

Download this PR as a diff file: \
<a href="https://git.openjdk.org/jdk/pull/22670.diff">https://git.openjdk.

---

> **生成时间**: 2026-03-25

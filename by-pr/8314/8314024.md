# JDK-8314024: SIGSEGV in PhaseIdealLoop::build_loop_late_post_work due to bad immediate dominator info

## 基本信息

| 属性 | 值 |
|------|-----|
| **Issue** | [JDK-8314024](https://bugs.openjdk.org/browse/JDK-8314024) |
| **PR** | [#15399](https://github.com/openjdk/jdk/pull/15399) |
| **Author** | rwestrel |
| **组织** | Red Hat |
| **Created** | 2023-08-23 |
| **Merged** | 2023-08-30 |
| **审查天数** | 7 |
| **版本** | JDK 21 |
| **优先级** | P2 |
| **组件** | hotspot |
| **模块** | compiler/c2 |
| **代码变更** | +83 / -0 (3 files) |
| **审查者** | chhagedorn, vnkozlov (2 人) |

## 描述

A node is sunk from the pre loop into the main loop. That node, in the
main loop, feeds into a test. When the node is sunk it is pinned
between the main and pre loop. The test it feeds into is then
eliminated by range check elimination: the sunk node becomes input to
an expression that computes the new bound of the pre loop. The
resulting graph is broken because the sunk node is pinned below the
pre loop but used by the exit test of the pre loop.

The fix I propose is in `PhaseIdealLoop::try_sink_out_of_loop()`, to
skip nodes in pre loops that have a use in the companion main loop.

<!-- Anything below this marker will be automatically updated, please do not edit manually! -->
---------
### Progress
- [x] Change must be properly reviewed (1 review required, with at least 1 [Reviewer](https://openjdk.org/bylaws#reviewer))
- [x] Change must not contain extraneous whitespace
- [x] Commit message must refer to an issue

### Issue
 * [JDK-8314024](https://bugs.openjdk.org/browse/JDK-8314024): SIGSEGV in PhaseIdealLoop::build_loop_late_post_work due to bad immediate dominator info (**Bug** - P2)


### Reviewers
 * [Vladimir Kozlov](https://openjdk.org/census#kvn) (@vnkozlov - **Reviewer**)
 * [Christian Hagedorn](https://openjdk.org/census#chagedorn) (@chhagedorn - **Reviewer**)


### Reviewing
<details><summary>Using <code>git</code></summary>

Checkout this PR locally: \
`$ git fetch https://git.openjdk.org/jdk.git pull/15399/head:pull/15399` \
`$ git checkout pull/15399`

Update a local copy of the PR: \
`$ git checkout pull/15399` \
`$ git pull https://git.openjdk.org/jdk.git pull/15399/head`

</details>
<details><summary>Using Skara CLI tools</summary>

Checkout this PR locally: \
`$ git pr checkout 15399`

View PR using the GUI difftool: \
`$ git pr show -t 15399`

</details>
<details><summary>Using diff file</summary>

Download this PR as a diff file: \
<a href="https://git.openjdk.org/jdk/pull/15399.diff">https://git.openjdk.org/jdk/pull/15399.diff</a>

</details>

## 变更文件

| 文件 | 状态 | +/- |
|------|------|-----|
| src/hotspot/share/opto/loopTransform.cpp | modified | +4/-0 |
| src/hotspot/share/opto/loopopts.cpp | modified | +8/-0 |
| test/hotspot/jtreg/compiler/loopopts/TestNodeSunkFromPreLoop.java | added | +71/-0 |

## 审查讨论

- **chhagedorn**: You should add a `@requires vm.compiler2.enabled` since `UseLoopPredicate` is a C2 only flag.


> **Backported to**: JDK 17u

---

> **生成时间**: 2026-03-25

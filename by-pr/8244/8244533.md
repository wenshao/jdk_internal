# JDK-8244533: Configure should abort on missing short names in Windows

## 基本信息

| 属性 | 值 |
|------|-----|
| **Issue** | [JDK-8244533](https://bugs.openjdk.org/browse/JDK-8244533) |
| **PR** | [#24483](https://github.com/openjdk/jdk/pull/24483) |
| **Author** | magicus |
| **组织** | Oracle |
| **Created** | 2025-04-07 |
| **Merged** | 2025-05-07 |
| **审查天数** | 30 |
| **版本** | JDK 25 |
| **优先级** | P3 |
| **组件** | infrastructure |
| **模块** | build |
| **代码变更** | +120 / -49 (6 files) |
| **审查者** | erikj79 (1 人) |

## 描述

Windows can create short names for directories that contain spaces in the name.

However, where short name creation was previously enabled by default, it no longer seems to be in recent version of Windows (the default setting is instead 'per volume', and the default for each volume is 'off').

We should check that the output paths of these utility macros are actually space-free, and emit an error otherwise. We should also add additional documentation about short names, and the short name creation setting to doc/building.md.

When testing this in several different cases with path including spaces that either had or had not a short name, it became apparent that the current handling of such cases were not ... optimal. Hence some bug fixes were needed to properly handle such cases.

<!-- Anything below this marker will be automatically updated, please do not edit manually! -->
---------
### Progress
- [x] Change must be properly reviewed (1 review required, with at least 1 [Reviewer](https://openjdk.org/bylaws#reviewer))
- [x] Change must not contain extraneous whitespace
- [x] Commit message must refer to an issue

### Issue
 * [JDK-8244533](https://bugs.openjdk.org/browse/JDK-8244533): Configure should abort on missing short names in Windows (**Enhancement** - P3)


### Reviewers
 * [Erik Joelsson](https://openjdk.org/census#erikj) (@erikj79 - **Reviewer**)

### Contributors
 * Jorn Vernee `<jvernee@openjdk.org>`
 * Magnus Ihse Bursie `<ihse@openjdk.org>`

### Reviewing
<details><summary>Using <code>git</code></summary>

Checkout this PR locally: \
`$ git fetch https://git.openjdk.org/jdk.git pull/24483/head:pull/24483` \
`$ git checkout pull/24483`

Update a local copy of the PR: \
`$ git checkout pull/24483` \
`$ git pull https://git.openjdk.org/jdk.git pull/24483/head`

</details>
<details><summary>Using Skara CLI tools</summary>

Checkout this PR locally: \
`$ git pr checkout 24483`

View PR using the GUI difftool: \
`$ git pr show -t 24483`

</details>
<det

---

> **生成时间**: 2026-03-25

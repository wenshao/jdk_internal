# JDK-8345169: Implement JEP 503: Remove the 32-bit x86 Port

> **JEP 503: Remove the 32-bit x86 Port** | Closed / Delivered

## 基本信息

| 属性 | 值 |
|------|-----|
| **Issue** | [JDK-8345169](https://bugs.openjdk.org/browse/JDK-8345169) |
| **PR** | [#23906](https://github.com/openjdk/jdk/pull/23906) |
| **Author** | shipilev |
| **组织** | Amazon |
| **Created** | 2025-03-04 |
| **Merged** | 2025-03-26 |
| **审查天数** | 22 |
| **版本** | JDK 25 |
| **优先级** | P4 |
| **组件** | hotspot |
| **代码变更** | +5 / -29729 (25 文件) |
| **审查者** | TheRealMDoerr, coleenp, dholmes-ora, iwanowww, magicus, shipilev, vnkozlov (7 人) |

## PR 描述

This PR implements JEP 503: Remove the 32-bit x86 Port.

The JEP is proposed to target 25, we would not integrate until JEP is ready. Reviews are appreciated meanwhile.

This is only the removal of obvious 32-bit x86 parts, mostly files with `x86_32` in their name.  Those are only built when build system knows we are compiling for x86_32. There is therefore no impact on x86_64. The approach for removing x86_32 files only also makes this PR borderline trivial, and requires no additional testing beyond normal pre-integration checks.

The rest of the code is quite heavily intertwined with x86_64 and/or Zero, and would require accurate untangling. It would be much easier to review and test once we purge the free-standing parts of 32-bit x86 port, which is also a bulk of the port. The tangling with 32-bit x86 Zero is also why I did not touch most of the build system paths that handle x86. There is [JDK-8351148](https://bugs.openjdk.org/browse/JDK-8351148) umbrella that tracks further cleanup work. One can peek the final state that can be reached with all the cleanups in my earlier exploratory https://github.com/openjdk/jdk/pull/22567.

Additional testing:
 - [x] Linux x86_32 Server fastdebug, `make bootcycle-images` (now fails configure)
 - [x] Linux x86_64 Server fastdebug, `make bootcycle-images` (still works)
 - [x] Linux x86_32 Zero fastdebug, `make bootcycle-images` (still works)
 - [x] Linux x86_64 Zero fastdebug, `make bootcycle-images` (still works)

<!-- Anyth

...(截断)

## 变更文件

| 文件 | 状态 | +/- |
|------|------|-----|
| make/autoconf/basic.m4 | modified | +4/-3 |
| make/autoconf/platform.m4 | modified | +1/-11 |
| src/hotspot/cpu/x86/downcallLinker_x86_32.cpp | removed | +0/-42 |
| src/hotspot/cpu/x86/foreignGlobals_x86_32.cpp | removed | +0/-54 |
| src/hotspot/cpu/x86/gc/shenandoah/shenandoah_x86_32.ad | removed | +0/-71 |
| src/hotspot/cpu/x86/interpreterRT_x86_32.cpp | removed | +0/-145 |
| src/hotspot/cpu/x86/jniFastGetField_x86_32.cpp | removed | +0/-323 |
| src/hotspot/cpu/x86/macroAssembler_x86_32_constants.cpp | removed | +0/-52 |
| src/hotspot/cpu/x86/macroAssembler_x86_32_cos.cpp | removed | +0/-427 |
| src/hotspot/cpu/x86/macroAssembler_x86_32_exp.cpp | removed | +0/-329 |
| src/hotspot/cpu/x86/macroAssembler_x86_32_log.cpp | removed | +0/-344 |
| src/hotspot/cpu/x86/macroAssembler_x86_32_log10.cpp | removed | +0/-357 |
| src/hotspot/cpu/x86/macroAssembler_x86_32_pow.cpp | removed | +0/-1855 |
| src/hotspot/cpu/x86/macroAssembler_x86_32_sin.cpp | removed | +0/-1742 |
| src/hotspot/cpu/x86/macroAssembler_x86_32_tan.cpp | removed | +0/-1172 |

> 共 25 个文件变更

## JEP 503 详情

- **标题**: Remove the 32-bit x86 Port
- **负责人**: Aleksey Shipilev
- **状态**: Closed / Delivered
- **目标版本**: JDK 25
- **链接**: [openjdk.org/jeps/503](https://openjdk.org/jeps/503)

## 审查讨论摘要

- **magicus**: Please remove. Old code is always present in git history if you want to reuse it.
- **shipilev**: I don't mind removing it, my concern would be to _remember_ this option was there! I guess it is okay to re-re-invent it later, possibly under a diffe
- **magicus**: It's no that important, no. I'm not sure if previous deprecated ports were handles exactly like this.   And you can always do like `git log | grep -

---

> **数据来源**: GitHub API + JBS | **生成时间**: 2026-03-25

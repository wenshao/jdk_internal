# JDK-8369517: Compilation mismatch for equivalent lambda and method reference

## 基本信息

| 属性 | 值 |
|------|-----|
| **Issue** | [JDK-8369517](https://bugs.openjdk.org/browse/JDK-8369517) |
| **PR** | [#27737](https://github.com/openjdk/jdk/pull/27737) |
| **Author** | vicente-romero-oracle |
| **组织** | Oracle |
| **Created** | 2025-10-09 |
| **Merged** | 2025-11-12 |
| **审查天数** | 34 |
| **版本** | JDK 26 |
| **优先级** | P4 |
| **组件** | tools |
| **模块** | compiler |
| **代码变更** | +58 / -2 (4 files) |
| **审查者** | liach, mcimadamore, vicente-romero-oracle (3 人) |

## 描述

Given the following test case:

```
import java.util.function.Supplier;
interface Main {
    interface X<T> {
        X<T> self();
    }

    static X<?> makeX() {return null;}

    static <R> X<R> create(Supplier<? extends R> supplier) {return null;}

    static X<X<?>> methodRef() {
        return create(Main::makeX).self();
    }

    static X<X<?>> lambda() {
        return create(() -> makeX()).self();
    }
}
```

javac reports an error for the method reference but not for the lambda. There should be no difference here (error in both cases)

<!-- Anything below this marker will be automatically updated, please do not edit manually! -->
---------
### Progress
- [x] Change must be properly reviewed (1 review required, with at least 1 [Reviewer](https://openjdk.org/bylaws#reviewer))
- [x] Change must not contain extraneous whitespace
- [x] Commit message must refer to an issue
- [x] Change requires CSR request [JDK-8369518](https://bugs.openjdk.org/browse/JDK-8369518) to be approved

### Issues
 * [JDK-8369517](https://bugs.openjdk.org/browse/JDK-8369517): Compilation mismatch for equivalent lambda and method reference (**Bug** - P4)
 * [JDK-8369518](https://bugs.openjdk.org/browse/JDK-8369518): Compilation mismatch for equivalent lambda and method reference (**CSR**)


### Reviewers
 * [Maurizio Cimadamore](https://openjdk.org/census#mcimadamore) (@mcimadamore - **Reviewer**)

### Reviewing
<details><summary>Using <code>git</code></summary>

Checkout this PR locally: \
`$ git fetch https://git.openjdk.org/jdk.git pull/27737/head:pull/27737` \
`$ git checkout pull/27737`

Update a local copy of the PR: \
`$ git checkout pull/27737` \
`$ git pull https://git.openjdk.org/jdk.git pull/27737/head`

</details>
<details><summary>Using Skara CLI tools</summary>

Checkout this PR locally: \
`$ git pr checkout 27737`

View PR using the GUI difftool: \
`$ git pr show -t 27737`

</details>
<details><summary>Using diff file</summary>

Download this PR

---

> **生成时间**: 2026-03-25

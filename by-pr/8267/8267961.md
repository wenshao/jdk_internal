# JDK-8267961: JInternalFrame.getNormalBounds() returns normalBounds when maximized state is false instead of bounds

## 基本信息

| 属性 | 值 |
|------|-----|
| **Issue** | [JDK-8267961](https://bugs.openjdk.org/browse/JDK-8267961) |
| **PR** | [#30065](https://github.com/openjdk/jdk/pull/30065) |
| **Author** | prsadhuk |
| **组织** | Oracle |
| **Created** | 2026-03-05 |
| **Merged** | 2026-03-20 |
| **审查天数** | 15 |
| **版本** | JDK 26 |
| **优先级** | P3 |
| **组件** | client-libs |
| **模块** | client |
| **代码变更** | +79 / -0 (2 files) |
| **审查者** | DamonGuy, TejeshR13, prsadhuk (3 人) |

## 描述

The behavior of getNormallBounds() is not as per spec when - "JInternalFrame is not in maximized state" 
As per JinternalFrame.getNormalBounds [spec](https://docs.oracle.com/en/java/javase/25/docs/api/java.desktop/javax/swing/JInternalFrame.html#getNormalBounds())

> If the JInternalFrame is not in maximized state, getNormalBounds() returns getBounds() 

but it seems in certain cases it can return cached normal bounds value and not getBounds() value.
Fix is made to check if internalFrame is unmaximized, the normalBounds is reset so that it can return getBounds() value in all cases
https://github.com/openjdk/jdk/blob/ff8b0ac048f6a6f75d1ad738d3354890d76d8128/src/java.desktop/share/classes/javax/swing/JInternalFrame.java#L1336-L1347

An existing test is also rectified to honour this spec

<!-- Anything below this marker will be automatically updated, please do not edit manually! -->
---------
### Progress
- [x] Change must be properly reviewed (1 review required, with at least 1 [Reviewer](https://openjdk.org/bylaws#reviewer))
- [x] Change must not contain extraneous whitespace
- [x] Commit message must refer to an issue

### Issue
 * [JDK-8267961](https://bugs.openjdk.org/browse/JDK-8267961): JInternalFrame.getNormalBounds() returns normalBounds when maximized state is false instead of bounds (**Enhancement** - P3)


### Reviewers
 * [Tejesh R](https://openjdk.org/census#tr) (@TejeshR13 - **Reviewer**)
 * [Damon Nguyen](https://openjdk.org/census#dnguyen) (@DamonGuy - **Reviewer**)

### Reviewing
<details><summary>Using <code>git</code></summary>

Checkout this PR locally: \
`$ git fetch https://git.openjdk.org/jdk.git pull/30065/head:pull/30065` \
`$ git checkout pull/30065`

Update a local copy of the PR: \
`$ git checkout pull/30065` \
`$ git pull https://git.openjdk.org/jdk.git pull/30065/head`

</details>
<details><summary>Using Skara CLI tools</summary>

Checkout this PR locally: \
`$ git pr checkout 30065`

View PR using the GUI difftool: \
`$ git pr s

---

> **生成时间**: 2026-03-25

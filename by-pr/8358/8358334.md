# JDK-8358334: C2/Shenandoah: incorrect execution with Unsafe

## 基本信息

| 属性 | 值 |
|------|-----|
| **Issue** | [JDK-8358334](https://bugs.openjdk.org/browse/JDK-8358334) |
| **PR** | [#25729](https://github.com/openjdk/jdk/pull/25729) |
| **Author** | rwestrel |
| **组织** | Red Hat |
| **Created** | 2025-06-10 |
| **Merged** | 2025-06-12 |
| **审查天数** | 2 |
| **版本** | JDK 25 |
| **优先级** | P2 |
| **组件** | hotspot |
| **模块** | gc/shenandoah |
| **代码变更** | +127 / -36 (3 files) |
| **审查者** | JohnTortugo, earthling-amzn, shipilev (3 人) |

## 描述

When a barrier is expanded, some control is picked as a location for
the barrier. The control input of data nodes that depend on that
control are updated so the nodes are after the expanded barrier unless
the barrier itself depends on some of those nodes.

In this particular failure, a raw memoy `Store` is the input memory to
the barrier. That `Store` has an anti-dependent `Load`. All 3 nodes
(barrier, `Load` and `Store`) are at the same control. The `Store` is
an input to the barrier so it stays before the barrier. The `Load`'s
control is updated to be after the barrier which breaks the
anti-dependency. The bug is that the logic that sorts nodes that need
to be before the barrier and those that can be after ignores
anti-dependencies. The fix simply extends that logic to take them into
account.


/cc shenandoah,hotspot-compiler

<!-- Anything below this marker will be automatically updated, please do not edit manually! -->
---------
### Progress
- [x] Change must be properly reviewed (1 review required, with at least 1 [Reviewer](https://openjdk.org/bylaws#reviewer))
- [x] Change must not contain extraneous whitespace
- [x] Commit message must refer to an issue

### Issue
 * [JDK-8358334](https://bugs.openjdk.org/browse/JDK-8358334): C2/Shenandoah: incorrect execution with Unsafe (**Bug** - P4)


### Reviewers
 * [William Kemper](https://openjdk.org/census#wkemper) (@earthling-amzn - **Reviewer**)
 * [Aleksey Shipilev](https://openjdk.org/census#shade) (@shipilev - **Reviewer**)

### Reviewing
<details><summary>Using <code>git</code></summary>

Checkout this PR locally: \
`$ git fetch https://git.openjdk.org/jdk.git pull/25729/head:pull/25729` \
`$ git checkout pull/25729`

Update a local copy of the PR: \
`$ git checkout pull/25729` \
`$ git pull https://git.openjdk.org/jdk.git pull/25729/head`

</details>
<details><summary>Using Skara CLI tools</summary>

Checkout this PR locally: \
`$ git pr checkout 25729`

View PR using the GUI difftool: \
`$ git pr show -t 257

## 变更文件

| 文件 | 状态 | +/- |
|------|------|-----|
| src/hotspot/share/gc/shenandoah/c2/shenandoahSupport.cpp | modified | +50/-35 |
| src/hotspot/share/gc/shenandoah/c2/shenandoahSupport.hpp | modified | +10/-1 |
| ...jtreg/gc/shenandoah/compiler/TestLostAntiDependencyAtExpansion.java | added | +67/-0 |

---

> **生成时间**: 2026-03-25

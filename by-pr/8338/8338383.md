# JDK-8338383: Implement JEP 491: Synchronize Virtual Threads without Pinning

> **JEP 491: Synchronize Virtual Threads without Pinning** | Closed / Delivered

## 基本信息

| 属性 | 值 |
|------|-----|
| **Issue** | [JDK-8338383](https://bugs.openjdk.org/browse/JDK-8338383) |
| **PR** | [#21565](https://github.com/openjdk/jdk/pull/21565) |
| **Author** | pchilano |
| **组织** | Oracle |
| **Created** | 2024-10-17 |
| **Merged** | 2024-11-12 |
| **审查天数** | 26 |
| **版本** | JDK 24 |
| **优先级** | P2 |
| **组件** | hotspot |
| **代码变更** | +8283 / -2743 (246 文件) |
| **审查者** | AlanBateman, dholmes-ora, pchilano, theRealAph, turbanoff, xmas92 (6 人) |

## PR 描述

This is the implementation of JEP 491: Synchronize Virtual Threads without Pinning. See [JEP 491](https://bugs.openjdk.org/browse/JDK-8337395) for further details.

In order to make the code review easier the changes have been split into the following initial 4 commits:

- Changes to allow unmounting a virtual thread that is currently holding monitors.
- Changes to allow unmounting a virtual thread blocked on synchronized trying to acquire the monitor.
- Changes to allow unmounting a virtual thread blocked in `Object.wait()` and its timed-wait variants.
- Changes to tests, JFR pinned event, and other changes in the JDK libraries.

The changes fix pinning issues for all 4 ports that currently implement continuations: x64, aarch64, riscv and ppc. Note: ppc changes were added recently and stand in its own commit after the initial ones.

The changes fix pinning issues when using `LM_LIGHTWEIGHT`, i.e. the default locking mode, (and `LM_MONITOR` which comes for free), but not when using `LM_LEGACY` mode. Note that the `LockingMode` flag has already been deprecated ([JDK-8334299](https://bugs.openjdk.org/browse/JDK-8334299)), with the intention to remove `LM_LEGACY` code in future releases.


## Summary of changes

### Unmount virtual thread while holding monitors

As stated in the JEP, currently when a virtual thread enters a synchronized method or block, the JVM records the virtual thread's carrier platform thread as holding the monitor, not the virtual thread i

...(截断)

## 变更文件

| 文件 | 状态 | +/- |
|------|------|-----|
| src/hotspot/cpu/aarch64/aarch64.ad | modified | +3/-4 |
| src/hotspot/cpu/aarch64/c1_MacroAssembler_aarch64.cpp | modified | +2/-2 |
| src/hotspot/cpu/aarch64/c1_Runtime1_aarch64.cpp | modified | +27/-10 |
| src/hotspot/cpu/aarch64/c2_MacroAssembler_aarch64.cpp | modified | +16/-15 |
| src/hotspot/cpu/aarch64/continuationFreezeThaw_aarch64.inline.hpp | modified | +35/-7 |
| src/hotspot/cpu/aarch64/continuationHelper_aarch64.inline.hpp | modified | +18/-2 |
| src/hotspot/cpu/aarch64/frame_aarch64.cpp | modified | +30/-0 |
| src/hotspot/cpu/aarch64/frame_aarch64.hpp | modified | +2/-1 |
| src/hotspot/cpu/aarch64/interp_masm_aarch64.cpp | modified | +66/-17 |
| src/hotspot/cpu/aarch64/interp_masm_aarch64.hpp | modified | +5/-0 |
| src/hotspot/cpu/aarch64/macroAssembler_aarch64.cpp | modified | +43/-1 |
| src/hotspot/cpu/aarch64/macroAssembler_aarch64.hpp | modified | +5/-2 |
| src/hotspot/cpu/aarch64/sharedRuntime_aarch64.cpp | modified | +46/-14 |
| src/hotspot/cpu/aarch64/stackChunkFrameStream_aarch64.inline.hpp | modified | +1/-0 |
| src/hotspot/cpu/aarch64/stubGenerator_aarch64.cpp | modified | +32/-0 |

> 共 246 个文件变更

## JEP 491 详情

- **标题**: Synchronize Virtual Threads without Pinning
- **负责人**: Alan Bateman
- **状态**: Closed / Delivered
- **目标版本**: JDK 24
- **链接**: [openjdk.org/jeps/491](https://openjdk.org/jeps/491)

## 审查讨论摘要

- **dholmes-ora**: ```suggestion   // attached thread cases where this field can have a temporary value. ``` Presumably this is for when the attaching thread is execu
- **pchilano**: Exactly.
- **xmas92**: Can we not hook the `_use_pop_on_epilogue` into `return_state_t`, simplify the constructors and keep the old should_not_reach_here guard for stubs whi

---

> **数据来源**: GitHub API + JBS | **生成时间**: 2026-03-25

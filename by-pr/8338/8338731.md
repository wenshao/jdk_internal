# JDK-8338731: MemoryLayout::offsetHandle can return a negative offset

## 基本信息

| 属性 | 值 |
|------|-----|
| **Issue** | [JDK-8338731](https://bugs.openjdk.org/browse/JDK-8338731) |
| **PR** | [#20662](https://github.com/openjdk/jdk/pull/20662) |
| **Author** | mcimadamore |
| **组织** | Oracle |
| **Created** | 2024-08-21 |
| **Merged** | 2024-08-28 |
| **审查天数** | 7 |
| **版本** | JDK 23 |
| **优先级** | P3 |
| **组件** | core-libs |
| **模块** | core-libs |
| **代码变更** | +23 / -10 (3 files) |
| **审查者** | PaulSandoz, minborg (2 人) |

## 描述

When working on startup improvements, I noticed that the method handle returned by `MemoryLayout::offsetHandle` can overflow if the client calls the handle with a base offset that is too big.

In other similar situations, the layout API always fails with `ArithmeticException` (see `MemoryLayout::scale`), so we should do the same here.

The fix is to use a `Math::addExact(long, long)` for the outermost add operation in the computation of the offset method handle. That outermost computation in fact is the only one that can overflow: it is an addition between a user-provided base offset `B` and a layout offset `L`. `L` is guaranteed not to overflow, by construction (as `L` is derived from a layout path). But `B` + `L` might overflow, so the new logic checks for that.

<!-- Anything below this marker will be automatically updated, please do not edit manually! -->
---------
### Progress
- [x] Change must be properly reviewed (1 review required, with at least 1 [Reviewer](https://openjdk.org/bylaws#reviewer))
- [x] Change must not contain extraneous whitespace
- [x] Commit message must refer to an issue
- [x] Change requires CSR request [JDK-8338742](https://bugs.openjdk.org/browse/JDK-8338742) to be approved

### Issues
 * [JDK-8338731](https://bugs.openjdk.org/browse/JDK-8338731): MemoryLayout::offsetHandle can return a negative offset (**Enhancement** - P3)
 * [JDK-8338742](https://bugs.openjdk.org/browse/JDK-8338742): MemoryLayout::offsetHandle can return a negative offset (**CSR**)


### Reviewers
 * [Per Minborg](https://openjdk.org/census#pminborg) (@minborg - **Reviewer**)
 * [Paul Sandoz](https://openjdk.org/census#psandoz) (@PaulSandoz - **Reviewer**) Review applies to [8ac9d262](https://git.openjdk.org/jdk/pull/20662/files/8ac9d26209f15d4d3947ab4d292e7bcc85029057)

### Reviewing
<details><summary>Using <code>git</code></summary>

Checkout this PR locally: \
`$ git fetch https://git.openjdk.org/jdk.git pull/20662/head:pull/20662` \
`$ git checkout pull/20662`

U

---

> **生成时间**: 2026-03-25

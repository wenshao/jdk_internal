# JDK-8338452: (dc) DatagramChannelImpl.blockingReceive with timeout may block indefinitely if all datagrams blocked by Securi

## 基本信息

| 属性 | 值 |
|------|-----|
| **Issue** | [JDK-8338452](https://bugs.openjdk.org/browse/JDK-8338452) |
| **PR** | [#20609](https://github.com/openjdk/jdk/pull/20609) |
| **Author** | AlanBateman |
| **组织** | Oracle |
| **Created** | 2024-08-16 |
| **Merged** | 2024-08-19 |
| **审查天数** | 3 |
| **版本** | JDK 23 |
| **优先级** | P2 |
| **组件** | core-libs |
| **模块** | core-libs/java.net |
| **代码变更** | +11 / -4 (1 files) |
| **审查者** | dfuch (1 人) |

## 描述

A small regression from JDK-8338142 that arises with DatagramSocket.receive with a timeout and a SecurityManager, or when using a really small timeout (like 1ms). In these cases, the remaining time may be computed as negative which causes the receive to block indefinitely waiting for a datagram. 

Timed receive requires timeout adjustment in two places (outer and inner loops). This complexity will go away when the SecurityManager implementation is removed. For now, I've kept the changes as small as possible so that the adjustment in the outer loop is only done when the datagram is denied by the security manager.

The test java/net/DatagramSocket/TimeoutWithSM.java exercises this code. It has failed at least once since JDK-8338142 was integrated.

<!-- Anything below this marker will be automatically updated, please do not edit manually! -->
---------
### Progress
- [x] Change must be properly reviewed (1 review required, with at least 1 [Reviewer](https://openjdk.org/bylaws#reviewer))
- [x] Change must not contain extraneous whitespace
- [x] Commit message must refer to an issue

### Issue
 * [JDK-8338452](https://bugs.openjdk.org/browse/JDK-8338452): (dc) DatagramChannelImpl.blockingReceive with timeout may block indefinitely if all datagrams blocked by SecurityManager (**Bug** - P2)


### Reviewers
 * [Daniel Fuchs](https://openjdk.org/census#dfuchs) (@dfuch - **Reviewer**)

### Reviewing
<details><summary>Using <code>git</code></summary>

Checkout this PR locally: \
`$ git fetch https://git.openjdk.org/jdk.git pull/20609/head:pull/20609` \
`$ git checkout pull/20609`

Update a local copy of the PR: \
`$ git checkout pull/20609` \
`$ git pull https://git.openjdk.org/jdk.git pull/20609/head`

</details>
<details><summary>Using Skara CLI tools</summary>

Checkout this PR locally: \
`$ git pr checkout 20609`

View PR using the GUI difftool: \
`$ git pr show -t 20609`

</details>
<details><summary>Using diff file</summary>

Download this PR as a diff file: \
<a hr

## 变更文件

| 文件 | 状态 | +/- |
|------|------|-----|
| src/java.base/share/classes/sun/nio/ch/DatagramChannelImpl.java | modified | +11/-4 |

---

> **生成时间**: 2026-03-25

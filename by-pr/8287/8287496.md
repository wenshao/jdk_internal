# JDK-8287496: Alternative virtual thread implementation that maps to OS thread

## 基本信息

| 属性 | 值 |
|------|-----|
| **Issue** | [JDK-8287496](https://bugs.openjdk.org/browse/JDK-8287496) |
| **PR** | [#8939](https://github.com/openjdk/jdk/pull/8939) |
| **Author** | AlanBateman |
| **组织** | Oracle |
| **Created** | 2022-05-29 |
| **Merged** | 2022-06-02 |
| **审查天数** | 4 |
| **版本** | JDK 19 |
| **优先级** | P4 |
| **组件** | core-libs |
| **模块** | runtime/threading |
| **代码变更** | +694 / -173 (72 files) |
| **审查者** | AlanBateman, ExE-Boss, dholmes-ora, mlchung, robehn (5 人) |

## 描述

This patch adds an alternative virtual thread implementation where each virtual thread is backed by an OS thread. It doesn't scale but it can be used by ports that don't have continuations support in the VM. Aside from scalability, the lack of continuations support means:

1. JVM TI is not supported when running with --enable-preview (the JVM TI spec allows for this) 
2. jshell --enable-preview can't be used (as jshell uses the debugger APIs and so needs JVM TI)

The VM option "VMContinuations" is added as an experimental option so it can be used by tests. A number of tests are changed to re-run with -XX:-VMContinuations. A new jtreg property is added so that tests that need the underlying VM support to be present can use "@requires vm.continuations" in the test description. A follow-up change would be to add "@requires vm.continuations" to the ~70 serviceability/jvmti/vthread that run with preview features enabled.

<!-- Anything below this marker will be automatically updated, please do not edit manually! -->
---------
### Progress
- [x] Change must be properly reviewed (1 review required, with at least 1 [Reviewer](https://openjdk.java.net/bylaws#reviewer))
- [x] Change must not contain extraneous whitespace
- [x] Commit message must refer to an issue

### Issue
 * [JDK-8287496](https://bugs.openjdk.java.net/browse/JDK-8287496): Alternative virtual thread implementation that maps to OS thread


### Reviewers
 * [Robbin Ehn](https://openjdk.java.net/census#rehn) (@robehn - **Reviewer**) ⚠️ Review applies to [126e38b6](https://git.openjdk.java.net/jdk/pull/8939/files/126e38b634518f2178d952481944a65ca99c0d58)
 * [Mandy Chung](https://openjdk.java.net/census#mchung) (@mlchung - **Reviewer**) ⚠️ Review applies to [126e38b6](https://git.openjdk.java.net/jdk/pull/8939/files/126e38b634518f2178d952481944a65ca99c0d58)


### Reviewing
<details><summary>Using <code>git</code></summary>

Checkout this PR locally: \
`$ git fetch https://git.openjdk.java.net/jdk pull/8939/

---

> **生成时间**: 2026-03-25

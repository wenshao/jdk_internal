# JDK-8221580: Confusing diagnostic for assigning a static final field in a constructor

## 基本信息

| 属性 | 值 |
|------|-----|
| **Issue** | [JDK-8221580](https://bugs.openjdk.org/browse/JDK-8221580) |
| **PR** | [#12411](https://github.com/openjdk/jdk/pull/12411) |
| **Author** | archiecobbs |
| **组织** | Oracle |
| **Created** | 2023-02-03 |
| **Merged** | 2023-02-06 |
| **审查天数** | 3 |
| **版本** | JDK 21 |
| **优先级** | P4 |
| **组件** | tools |
| **模块** | tools |
| **代码变更** | +25 / -15 (11 files) |
| **审查者** | vicente-romero-oracle (1 人) |

## 描述

When a program attempts to assign a value to a final field, javac reports `cannot assign a value to final variable x`.

If the field is `static`, the exact same error message is used, which can be confusing, because no mention is made of the field's `static`ness.

This change does two things:
* Refactor the error message so that arbitrary modifiers can be included, instead of hard-wiring `final`
* Include both `static final` in the error message in the case of `static final` variables.

So for a `static final` field the error is now `cannot assign a value to static final variable x`.

<!-- Anything below this marker will be automatically updated, please do not edit manually! -->
---------
### Progress
- [x] Change must be properly reviewed (1 review required, with at least 1 [Reviewer](https://openjdk.org/bylaws#reviewer))
- [x] Change must not contain extraneous whitespace
- [x] Commit message must refer to an issue

### Issue
 * [JDK-8221580](https://bugs.openjdk.org/browse/JDK-8221580): Confusing diagnostic for assigning a static final field in a constructor


### Reviewers
 * [Vicente Romero](https://openjdk.org/census#vromero) (@vicente-romero-oracle - **Reviewer**)


### Reviewing
<details><summary>Using <code>git</code></summary>

Checkout this PR locally: \
`$ git fetch https://git.openjdk.org/jdk pull/12411/head:pull/12411` \
`$ git checkout pull/12411`

Update a local copy of the PR: \
`$ git checkout pull/12411` \
`$ git pull https://git.openjdk.org/jdk pull/12411/head`

</details>
<details><summary>Using Skara CLI tools</summary>

Checkout this PR locally: \
`$ git pr checkout 12411`

View PR using the GUI difftool: \
`$ git pr show -t 12411`

</details>
<details><summary>Using diff file</summary>

Download this PR as a diff file: \
<a href="https://git.openjdk.org/jdk/pull/12411.diff">https://git.openjdk.org/jdk/pull/12411.diff</a>

</details>

---

> **生成时间**: 2026-03-25

# JDK-8266773: Release VM is broken with GCC 9 after 8214237

## 基本信息

| 属性 | 值 |
|------|-----|
| **Issue** | [JDK-8266773](https://bugs.openjdk.org/browse/JDK-8266773) |
| **PR** | [#3941](https://github.com/openjdk/jdk/pull/3941) |
| **Author** | DamonFool |
| **组织** | Tencent |
| **Created** | 2021-05-10 |
| **Merged** | 2021-05-11 |
| **审查天数** | 1 |
| **版本** | JDK 17 |
| **优先级** | P2 |
| **组件** | hotspot |
| **模块** | hotspot |
| **代码变更** | +13 / -19 (2 files) |
| **审查者** | DamonFool, kimbarrett, lkorinth, tstuefe (4 人) |

## 描述

Hi all,

Release VM is broken with GCC 9 due to -Werror=format-overflow=.
And if this assert[1] is removed, fastdebug VM can also reproduce the same bug.
The key to reproduce it is to compile with -O3.

IMO, it seems like a false positive gcc bug.
But we'd better fix it since gcc 9 is assumed to be supported to build OpenJDK.

Thanks.
Best regards,
Jie


[1] https://github.com/openjdk/jdk/blob/master/src/hotspot/share/gc/g1/g1GCPhaseTimes.cpp#L47

<!-- Anything below this marker will be automatically updated, please do not edit manually! -->
---------
### Progress
- [x] Change must not contain extraneous whitespace
- [x] Commit message must refer to an issue
- [x] Change must be properly reviewed

### Issue
 * [JDK-8266773](https://bugs.openjdk.java.net/browse/JDK-8266773): Release VM is broken with GCC 9 after 8214237


### Reviewers
 * [Thomas Stuefe](https://openjdk.java.net/census#stuefe) (@tstuefe - **Reviewer**) ⚠️ Review applies to 0561a9c47d5f53a39796ea67c39937bf209e8189
 * [Leo Korinth](https://openjdk.java.net/census#lkorinth) (@lkorinth - Committer)


### Reviewing
<details><summary>Using <code>git</code></summary>

Checkout this PR locally: \
`$ git fetch https://git.openjdk.java.net/jdk pull/3941/head:pull/3941` \
`$ git checkout pull/3941`

Update a local copy of the PR: \
`$ git checkout pull/3941` \
`$ git pull https://git.openjdk.java.net/jdk pull/3941/head`

</details>
<details><summary>Using Skara CLI tools</summary>

Checkout this PR locally: \
`$ git pr checkout 3941`

View PR using the GUI difftool: \
`$ git pr show -t 3941`

</details>
<details><summary>Using diff file</summary>

Download this PR as a diff file: \
<a href="https://git.openjdk.java.net/jdk/pull/3941.diff">https://git.openjdk.java.net/jdk/pull/3941.diff</a>

</details>

## 变更文件

| 文件 | 状态 | +/- |
|------|------|-----|
| src/hotspot/share/gc/g1/g1GCPhaseTimes.cpp | modified | +12/-18 |
| src/hotspot/share/gc/g1/g1GCPhaseTimes.hpp | modified | +1/-1 |

## 审查讨论

- **kimbarrett**: No, don't do this.  `indent` never returns NULL.  It asserts the given argument is in bounds.  The warning is a (gcc9-specific?) false positive, simil
- **DamonFool**: > No, don't do this. `indent` never returns NULL. It asserts the given argument is in bounds. The warning is a (gcc9-specific?) false positive, simila
- **lkorinth**: Maybe instead use something like: `out->print("%*s%s", 2 * indent_level, "", "indented*s");` or: `out->print("%*c%s", 2 * indent_level, ' ', "inden

---

> **生成时间**: 2026-03-25

# JDK-8333729: C2 SuperWord: remove some @requires usages in test/hotspot/jtreg/compiler/loopopts/superword

## 基本信息

| 属性 | 值 |
|------|-----|
| **Issue** | [JDK-8333729](https://bugs.openjdk.org/browse/JDK-8333729) |
| **PR** | [#19581](https://github.com/openjdk/jdk/pull/19581) |
| **Author** | eme64 |
| **组织** | Oracle |
| **Created** | 2024-06-06 |
| **Merged** | 2024-06-17 |
| **审查天数** | 11 |
| **版本** | JDK 23 |
| **优先级** | P4 |
| **组件** | hotspot |
| **模块** | compiler/c2 |
| **代码变更** | +76 / -68 (22 files) |
| **审查者** | chhagedorn, vnkozlov (2 人) |

## 描述

I'm removing as many `@requires` statements as possible. Generally, they just limit what tests can be executed where, and in most cases such limitations do not make much sense.

For example, tests should not be limited to `@requires vm.compiler2.enabled`, i.e. only run if C2 is available - we can also run the tests if GraalVM is available.

We should also not limit tests to `@requires vm.bits == "64"`, but rather limit the `@IR` rules with `applyIfPlatform = {"64-bit", "true"}`.

Similarly, we should not limit the platform for a run statement, but do that per `@IR` rule if necessary.

Update: I had to add some extra `-XX:+IgnoreUnrecognizedVMOptions`, so that the C2 flags do not make the VM crash if C2 is not available. I ran all the relevant tests on a build without C2, see discussion in the thread below.

<!-- Anything below this marker will be automatically updated, please do not edit manually! -->
---------
### Progress
- [x] Change must be properly reviewed (1 review required, with at least 1 [Reviewer](https://openjdk.org/bylaws#reviewer))
- [x] Change must not contain extraneous whitespace
- [x] Commit message must refer to an issue

### Issue
 * [JDK-8333729](https://bugs.openjdk.org/browse/JDK-8333729): C2 SuperWord: remove some @<!---->requires usages in test/hotspot/jtreg/compiler/loopopts/superword (**Sub-task** - P4)


### Reviewers
 * [Christian Hagedorn](https://openjdk.org/census#chagedorn) (@chhagedorn - **Reviewer**) ⚠️ Review applies to [2733a868](https://git.openjdk.org/jdk/pull/19581/files/2733a8689b27087b68e9f22bd8223989c2a31cac)
 * [Vladimir Kozlov](https://openjdk.org/census#kvn) (@vnkozlov - **Reviewer**)


### Reviewing
<details><summary>Using <code>git</code></summary>

Checkout this PR locally: \
`$ git fetch https://git.openjdk.org/jdk.git pull/19581/head:pull/19581` \
`$ git checkout pull/19581`

Update a local copy of the PR: \
`$ git checkout pull/19581` \
`$ git pull https://git.openjdk.org/jdk.git pull/19581/head`

</details

---

> **生成时间**: 2026-03-25

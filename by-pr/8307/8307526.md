# JDK-8307526: [JFR] Better handling of tampered JFR repository

## 基本信息

| 属性 | 值 |
|------|-----|
| **Issue** | [JDK-8307526](https://bugs.openjdk.org/browse/JDK-8307526) |
| **PR** | [#14360](https://github.com/openjdk/jdk/pull/14360) |
| **Author** | jaokim |
| **Created** | 2023-06-07 |
| **Merged** | 2023-07-05 |
| **审查天数** | 28 |
| **版本** | JDK 21 |
| **优先级** | P2 |
| **组件** | hotspot |
| **模块** | runtime/jfr |
| **代码变更** | +136 / -42 (8 files) |
| **审查者** | egahlin, jaokim, turbanoff (3 人) |

## 描述

This change makes sure that "JFR Periodic Task" isn't interrupted by any errors. This can happen when an unfinished chunkfile is removed "in-flight", which would lead to the chunkfiles not being rotated properly, and the maxsize/maxage being ignored.

With this fix, when an unfinished chunkfile is detected, all of the chunkfiles in the recording are checked for existence (since one likely cause for this could be f.i. 'rm -r /tmp' being invoked, effectively deleting all chunkfiles). Upon seeing missing chunkfiles, an error is logged, and emitted to the recording to signal that some data might be missing.
A check to catch any missing chunkfiles is also added when dumping a recording -- this can happen if only finished chunkfiles have been removed, which is not detected in-flight.

Also, a check was added to the file purger to check whether the file had already been deleted to not fill the purge list with already removed files.

### Testing
 - [x] tier1-3
 - [x] jdk_jfr

<!-- Anything below this marker will be automatically updated, please do not edit manually! -->
---------
### Progress
- [x] Change must be properly reviewed (1 review required, with at least 1 [Reviewer](https://openjdk.org/bylaws#reviewer))
- [x] Change must not contain extraneous whitespace
- [x] Commit message must refer to an issue

### Issue
 * [JDK-8307526](https://bugs.openjdk.org/browse/JDK-8307526): [JFR] Better handling of tampered JFR repository (**Bug** - P2)


### Reviewers
 * [Erik Gahlin](https://openjdk.org/census#egahlin) (@egahlin - **Reviewer**) ⚠️ Review applies to [4f7427c7](https://git.openjdk.org/jdk/pull/14360/files/4f7427c7e391bc4f92afc929ab55547f7c6fd626)


### Reviewing
<details><summary>Using <code>git</code></summary>

Checkout this PR locally: \
`$ git fetch https://git.openjdk.org/jdk.git pull/14360/head:pull/14360` \
`$ git checkout pull/14360`

Update a local copy of the PR: \
`$ git checkout pull/14360` \
`$ git pull https://git.openjdk.org/jdk.git pull/14360

## 变更文件

| 文件 | 状态 | +/- |
|------|------|-----|
| src/hotspot/share/jfr/jni/jfrJniMethod.cpp | modified | +4/-0 |
| src/hotspot/share/jfr/jni/jfrJniMethod.hpp | modified | +2/-0 |
| src/hotspot/share/jfr/jni/jfrJniMethodRegistration.cpp | modified | +2/-1 |
| src/jdk.jfr/share/classes/jdk/jfr/internal/FilePurger.java | modified | +8/-1 |
| src/jdk.jfr/share/classes/jdk/jfr/internal/JVM.java | modified | +7/-0 |
| src/jdk.jfr/share/classes/jdk/jfr/internal/PlatformRecorder.java | modified | +30/-13 |
| src/jdk.jfr/share/classes/jdk/jfr/internal/PlatformRecording.java | modified | +53/-11 |
| src/jdk.jfr/share/classes/jdk/jfr/internal/RepositoryChunk.java | modified | +30/-16 |

## 审查讨论

- **turbanoff**: ```suggestion         } catch (FileNotFoundException e) { ```
- **turbanoff**: ```suggestion             } catch (Throwable t) { ```
- **egahlin**: Looks like there could be a race here. If the file is removed after the exists check, but before the file is deleted, the method may return false inco

---

> **生成时间**: 2026-03-25

# JDK-8307105: JFileChooser InvalidPathException when selecting some system folders on Windows

## 基本信息

| 属性 | 值 |
|------|-----|
| **Issue** | [JDK-8307105](https://bugs.openjdk.org/browse/JDK-8307105) |
| **PR** | [#13998](https://github.com/openjdk/jdk/pull/13998) |
| **Author** | TejeshR13 |
| **组织** | Oracle |
| **Created** | 2023-05-16 |
| **Merged** | 2023-06-02 |
| **审查天数** | 17 |
| **版本** | JDK 21 |
| **优先级** | P3 |
| **组件** | client-libs |
| **模块** | core-libs/java.io |
| **代码变更** | +97 / -10 (3 files) |
| **审查者** | DamonGuy, TejeshR13, kumarabhi006 (3 人) |

## 描述

This is a regression from fix [JDK-8281966](https://bugs.openjdk.org/browse/JDK-8281966): Absolute path of symlink is null in JFileChooser. The fix checks whether the file path is a symbolic link using `Files.isSymbolicLink()` method with path as input. In windows for specific folders like "This PC"/"Network"/"Libraries" the path value will be a hex values which causes InvalidPathException. In order to resolve the issue, since no other checks are available to validate the path of these folders, checking if the file is link firstly and then for symbolic link resolves the problem (since File.isLink() doesn't take path as input rather file is a parameter). Since every symbolic link is a link, this fix seems logical to me.
The fix is tested in CI for regression and is green. The regression fix is also tested for confirmation and works fine.

<!-- Anything below this marker will be automatically updated, please do not edit manually! -->
---------
### Progress
- [x] Change must be properly reviewed (1 review required, with at least 1 [Reviewer](https://openjdk.org/bylaws#reviewer))
- [x] Change must not contain extraneous whitespace
- [x] Commit message must refer to an issue

### Issue
 * [JDK-8307105](https://bugs.openjdk.org/browse/JDK-8307105): JFileChooser InvalidPathException when selecting some system folders on Windows


### Reviewers
 * [Alexey Ivanov](https://openjdk.org/census#aivanov) (@aivanov-jdk - **Reviewer**)
 * [Abhishek Kumar](https://openjdk.org/census#abhiscxk) (@kumarabhi006 - Committer)
 * [Damon Nguyen](https://openjdk.org/census#dnguyen) (@DamonGuy - Committer)


### Reviewing
<details><summary>Using <code>git</code></summary>

Checkout this PR locally: \
`$ git fetch https://git.openjdk.org/jdk.git pull/13998/head:pull/13998` \
`$ git checkout pull/13998`

Update a local copy of the PR: \
`$ git checkout pull/13998` \
`$ git pull https://git.openjdk.org/jdk.git pull/13998/head`

</details>
<details><summary>Using Skara CLI tools</summary>

Check

---

> **生成时间**: 2026-03-25

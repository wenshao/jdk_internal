# JDK-8315735: VerifyError when switch statement used with synchronized block

## 基本信息

| 属性 | 值 |
|------|-----|
| **Issue** | [JDK-8315735](https://bugs.openjdk.org/browse/JDK-8315735) |
| **PR** | [#15584](https://github.com/openjdk/jdk/pull/15584) |
| **Author** | lahodaj |
| **组织** | Oracle |
| **Created** | 2023-09-06 |
| **Merged** | 2023-09-07 |
| **审查天数** | 1 |
| **版本** | JDK 22 |
| **优先级** | P4 |
| **组件** | tools |
| **模块** | runtime/threading |
| **代码变更** | +74 / -5 (2 files) |
| **审查者** | ExE-Boss, forax, lahodaj, vicente-romero-oracle (4 人) |

## 描述

Having code like:
```
    public static void main(String... args) {
        int i1 = 0 + switch (args.length) {
            default -> {
                synchronized (args) {
                    yield 1;
                }
            }
        };
    }
```

fails with a verification error at runtime. In the classfile, the synchronized block is basically a try-finally (which has an implicit "catch (any)" block), and so it needs the same handling as ordinary try statements.

(When there's a try statement inside a switch expression, the pre-existing stack is stored in local variables, and restored at return from the case. This is because catch clauses start with an empty stack.)

<!-- Anything below this marker will be automatically updated, please do not edit manually! -->
---------
### Progress
- [x] Change must be properly reviewed (1 review required, with at least 1 [Reviewer](https://openjdk.org/bylaws#reviewer))
- [x] Change must not contain extraneous whitespace
- [x] Commit message must refer to an issue

### Issue
 * [JDK-8315735](https://bugs.openjdk.org/browse/JDK-8315735): VerifyError when switch statement used with synchronized block (**Bug** - P4)


### Reviewers
 * [Vicente Romero](https://openjdk.org/census#vromero) (@vicente-romero-oracle - **Reviewer**) ⚠️ Review applies to [26e29fe0](https://git.openjdk.org/jdk/pull/15584/files/26e29fe0c06732b4d1cffc118d820bec6a3b93b2)


### Reviewing
<details><summary>Using <code>git</code></summary>

Checkout this PR locally: \
`$ git fetch https://git.openjdk.org/jdk.git pull/15584/head:pull/15584` \
`$ git checkout pull/15584`

Update a local copy of the PR: \
`$ git checkout pull/15584` \
`$ git pull https://git.openjdk.org/jdk.git pull/15584/head`

</details>
<details><summary>Using Skara CLI tools</summary>

Checkout this PR locally: \
`$ git pr checkout 15584`

View PR using the GUI difftool: \
`$ git pr show -t 15584`

</details>
<details><summary>Using diff file</summary>

Download this PR as

---

> **生成时间**: 2026-03-25

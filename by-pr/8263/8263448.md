# JDK-8263448: CTW: fatal error: meet not symmetric

## 基本信息

| 属性 | 值 |
|------|-----|
| **Issue** | [JDK-8263448](https://bugs.openjdk.org/browse/JDK-8263448) |
| **PR** | [#3298](https://github.com/openjdk/jdk/pull/3298) |
| **Author** | vnkozlov |
| **组织** | Oracle |
| **Created** | 2021-03-31 |
| **Merged** | 2021-04-01 |
| **审查天数** | 1 |
| **版本** | JDK 17 |
| **优先级** | P3 |
| **组件** | hotspot |
| **模块** | hotspot |
| **代码变更** | +25 / -58 (1 files) |
| **审查者** | rwestrel (1 人) |

## 描述

In bug's case instance_id should be changed to InstanceBot only if it is real id and not InstanceTop or InstanceBot (similar to Constant in type lattice). Otherwise we incorrectly change InstanceTop to InstanceBot.  In short, we should follow PTR transformation in this code:
```
      if (ptr == Constant) {
         ptr = NotNull;
      }
-     instance_id = InstanceBot;
+     if (instance_id > 0) {
+       instance_id = InstanceBot;
+     }
```
I noticed that `TypeInstPtr::xmeet_helper()` has the same code as `TypeAryPtr::xmeet_helper()` for case when `InstPtr` meets `AryPtr`. So instead of fixing two places I decided to call TypeAryPtr::xmeet_helper() from `TypeInstPtr::xmeet_helper()` for this case. We do similar thing in `TypeOopPtr::xmeet()`:
https://github.com/openjdk/jdk/blob/master/src/hotspot/share/opto/type.cpp#L3283

Fixed code style in touched code.

Tested failing test from bug report, hs-tier1-4, hs-comp.

<!-- Anything below this marker will be automatically updated, please do not edit manually! -->
---------
### Progress
- [x] Change must not contain extraneous whitespace
- [x] Commit message must refer to an issue
- [x] Change must be properly reviewed

### Issue
 * [JDK-8263448](https://bugs.openjdk.java.net/browse/JDK-8263448): CTW: fatal error: meet not symmetric


### Reviewers
 * [Roland Westrelin](https://openjdk.java.net/census#roland) (@rwestrel - **Reviewer**)


### Reviewing
<details><summary>Using <code>git</code></summary>

Checkout this PR locally: \
`$ git fetch https://git.openjdk.java.net/jdk pull/3298/head:pull/3298` \
`$ git checkout pull/3298`

Update a local copy of the PR: \
`$ git checkout pull/3298` \
`$ git pull https://git.openjdk.java.net/jdk pull/3298/head`

</details>
<details><summary>Using Skara CLI tools</summary>

Checkout this PR locally: \
`$ git pr checkout 3298`

View PR using the GUI difftool: \
`$ git pr show -t 3298`

</details>
<details><summary>Using diff file</summary>

Download this PR as a d

---

> **生成时间**: 2026-03-25

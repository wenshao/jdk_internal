# JDK-8263497: Clean up sun.security.krb5.PrincipalName::toByteArray

## 基本信息

| 属性 | 值 |
|------|-----|
| **Issue** | [JDK-8263497](https://bugs.openjdk.org/browse/JDK-8263497) |
| **PR** | [#2962](https://github.com/openjdk/jdk/pull/2962) |
| **Author** | shipilev |
| **组织** | Amazon |
| **Merged** | 2021-03-15 |
| **版本** | JDK 17 |
| **优先级** | P5 |
| **组件** | security-libs |
| **模块** | core-libs/java.net |
| **代码变更** | +1 / -2 (1 files) |
| **审查者** | wangweij |

## 描述

SonarCloud actually found this:
  Verify this is the index that was intended; it was already set before.

``` 
    public byte[][] toByteArray() {
        byte[][] result = new byte[nameStrings.length][];
        for (int i = 0; i < nameStrings.length; i++) {
            result[i] = new byte[nameStrings[i].length()]; // <-- here
            result[i] = nameStrings[i].getBytes();
        }
        return result;
    }
```

`getBytes()` returns the `byte[]` array, there is no need to allocate the array before it.

<!-- Anything below this marker will be automatically updated, please do not edit manually! -->
---------
### Progress
- [x] Change must not contain extraneous whitespace
- [x] Commit message must refer to an issue
- [x] Change must be properly reviewed

### Issue
 * [JDK-8263497](https://bugs.openjdk.java.net/browse/JDK-8263497): Clean up sun.security.krb5.PrincipalName::toByteArray 


### Reviewers
 * [Weijun Wang](https://openjdk.java.net/census#weijun) (@wangweij - **Reviewer**) ⚠️ Review applies to 6223ee6fcd3e278a98b664b30b469afd5f717ec3


### Download
`$ git fetch https://git.openjdk.java.net/jdk pull/2962/head:pull/2962`
`$ git checkout pull/2962`

---

> **生成时间**: 2026-03-25

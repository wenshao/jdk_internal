# JDK-8373420: C2: Add true/false_proj*() methods for IfNode as a replacement for proj_out*(true/false)

## 基本信息

| 属性 | 值 |
|------|-----|
| **Issue** | [JDK-8373420](https://bugs.openjdk.org/browse/JDK-8373420) |
| **PR** | [#28745](https://github.com/openjdk/jdk/pull/28745) |
| **Author** | chhagedorn |
| **组织** | Oracle |
| **Created** | 2025-12-10 |
| **Merged** | 2025-12-17 |
| **审查天数** | 7 |
| **版本** | JDK 26 |
| **优先级** | P4 |
| **组件** | hotspot |
| **模块** | compiler/c2 |
| **代码变更** | +62 / -46 (11 files) |
| **审查者** | dafedafe, eme64, rwestrel (3 人) |

## 描述

There are a lot of places in the code where we call `proj_out*(true/false)` on an `IfNode`. In some cases, we then cast the returned `ProjNode` back to `IfProjNode` or `IfTrueNode/IfFalseNode`. I often visit such code and now decided to clean this up.

The patch proposes new `IfNode::true/false_proj*()` methods that return `IfTrueNode/IfFalseNode` directly. I walked through all `proj_out*()` calls and replaced those that used a direct `true/false` or `1/0` as argument.

There are still more things to clean up in this area, for example, when we return `ProjNode` even though it should be an `IfProjNode` which requires more casting. But let's do that step by step in follow-up clean ups.

Thanks,
Christian

<!-- Anything below this marker will be automatically updated, please do not edit manually! -->
---------
### Progress
- [x] Change must be properly reviewed (1 review required, with at least 1 [Reviewer](https://openjdk.org/bylaws#reviewer))
- [x] Change must not contain extraneous whitespace
- [x] Commit message must refer to an issue

### Issue
 * [JDK-8373420](https://bugs.openjdk.org/browse/JDK-8373420): C2: Add true/false_proj*() methods for IfNode as a replacement for proj_out*(true/false) (**Enhancement** - P4)


### Reviewers
 * [Damon Fenacci](https://openjdk.org/census#dfenacci) (@dafedafe - Committer)
 * [Roland Westrelin](https://openjdk.org/census#roland) (@rwestrel - **Reviewer**)
 * [Emanuel Peter](https://openjdk.org/census#epeter) (@eme64 - **Reviewer**)

### Reviewing
<details><summary>Using <code>git</code></summary>

Checkout this PR locally: \
`$ git fetch https://git.openjdk.org/jdk.git pull/28745/head:pull/28745` \
`$ git checkout pull/28745`

Update a local copy of the PR: \
`$ git checkout pull/28745` \
`$ git pull https://git.openjdk.org/jdk.git pull/28745/head`

</details>
<details><summary>Using Skara CLI tools</summary>

Checkout this PR locally: \
`$ git pr checkout 28745`

View PR using the GUI difftool: \
`$ git pr show -t 2874

---

> **生成时间**: 2026-03-25

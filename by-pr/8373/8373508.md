# JDK-8373508: C2: sinking CreateEx out of loop breaks the graph

## 基本信息

| 属性 | 值 |
|------|-----|
| **Issue** | [JDK-8373508](https://bugs.openjdk.org/browse/JDK-8373508) |
| **PR** | [#28842](https://github.com/openjdk/jdk/pull/28842) |
| **Author** | rwestrel |
| **组织** | Red Hat |
| **Created** | 2025-12-16 |
| **Merged** | 2026-01-05 |
| **审查天数** | 20 |
| **版本** | JDK 26 |
| **优先级** | P3 |
| **组件** | hotspot |
| **模块** | compiler/c2 |
| **代码变更** | +159 / -0 (3 files) |
| **审查者** | chhagedorn, dean-long, rwestrel (3 人) |

## 描述

A `CreateEx` gets sunk out of loop by
`PhaseIdealLoop::try_sink_out_of_loop()` and, as a consequence, the
following logic:

```
  return (in(0)->is_CatchProj() && in(0)->in(0)->is_Catch() &&
          in(0)->in(0)->in(1) == in(1)) ? this : call->in(TypeFunc::Parms);
```

in `CreateExNode::Identity()` triggers which leads to the crash
because `call->in(TypeFunc::Parms)` is not even an object in this
particular case.

It's actually not clear to me what that logic in
`CreateExNode::Identity()` is expected to do and I wonder if it's
still needed.

Anyway, the fix I propose is to skip `CreateEx` in
`PhaseIdealLoop::try_sink_out_of_loop()`.

<!-- Anything below this marker will be automatically updated, please do not edit manually! -->
---------
### Progress
- [x] Change must be properly reviewed (1 review required, with at least 1 [Reviewer](https://openjdk.org/bylaws#reviewer))
- [x] Change must not contain extraneous whitespace
- [x] Commit message must refer to an issue

### Issue
 * [JDK-8373508](https://bugs.openjdk.org/browse/JDK-8373508): C2: sinking CreateEx out of loop breaks the graph (**Bug** - P3)(⚠️ The fixVersion in this issue is [26] but the fixVersion in .jcheck/conf is 27, a new backport will be created when this pr is integrated.)


### Reviewers
 * [Christian Hagedorn](https://openjdk.org/census#chagedorn) (@chhagedorn - **Reviewer**)
 * [Dean Long](https://openjdk.org/census#dlong) (@dean-long - **Reviewer**)

### Reviewing
<details><summary>Using <code>git</code></summary>

Checkout this PR locally: \
`$ git fetch https://git.openjdk.org/jdk.git pull/28842/head:pull/28842` \
`$ git checkout pull/28842`

Update a local copy of the PR: \
`$ git checkout pull/28842` \
`$ git pull https://git.openjdk.org/jdk.git pull/28842/head`

</details>
<details><summary>Using Skara CLI tools</summary>

Checkout this PR locally: \
`$ git pr checkout 28842`

View PR using the GUI difftool: \
`$ git pr show -t 28842`

</details>
<details><summary>Using diff file</summa

---

> **生成时间**: 2026-03-25

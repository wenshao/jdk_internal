# JDK-8373366: HandshakeState should disallow suspend ops for disabler threads

## 基本信息

| 属性 | 值 |
|------|-----|
| **Issue** | [JDK-8373366](https://bugs.openjdk.org/browse/JDK-8373366) |
| **PR** | [#28740](https://github.com/openjdk/jdk/pull/28740) |
| **Author** | sspitsyn |
| **组织** | Oracle |
| **Created** | 2025-12-10 |
| **Merged** | 2026-01-22 |
| **审查天数** | 43 |
| **版本** | JDK 26 |
| **优先级** | P3 |
| **组件** | hotspot |
| **模块** | runtime/threading |
| **代码变更** | +286 / -20 (7 files) |
| **审查者** | dholmes-ora, lmesnik, pchilano, sspitsyn (4 人) |

## 描述

An asynchronous handshake operation (`ThreadSelfSuspensionHandshakeClosure`) can be installed when the target thread is not in a `MountUnmountDisabler` scope. But the target thread can enter such scope by the time the operation is self-processed by the target thread.

This is fixed by a small tweak in the function
`HandshakeOperation* HandshakeState::get_op_for_self(bool allow_suspend, bool check_async_exception)`.
The tweak is to skip a `HandshakeOperation` if `_handshakee->is_vthread_transition_disabler() == true`, so the same temporary suspension disabling mechanism would be used as for `_handshakee->is_disable_suspend() == true`.

All other changes are to move the `is_vthread_transition_disabler()` out of DEBUG to product.

Testing:
 - In progress: mach5 tiers 1-6

<!-- Anything below this marker will be automatically updated, please do not edit manually! -->
---------
### Progress
- [x] Change must be properly reviewed (1 review required, with at least 1 [Reviewer](https://openjdk.org/bylaws#reviewer))
- [x] Change must not contain extraneous whitespace
- [x] Commit message must refer to an issue

### Issues
 * [JDK-8373366](https://bugs.openjdk.org/browse/JDK-8373366): HandshakeState should disallow suspend ops for disabler threads (**Bug** - P3)
 * [JDK-8375362](https://bugs.openjdk.org/browse/JDK-8375362): Deadlock with unmount of suspended virtual thread interrupting another virtual thread (**Bug** - P4)


### Reviewers
 * [Leonid Mesnik](https://openjdk.org/census#lmesnik) (@lmesnik - **Reviewer**)
 * [Patricio Chilano Mateo](https://openjdk.org/census#pchilanomate) (@pchilano - **Reviewer**) Review applies to [bb0fc064](https://git.openjdk.org/jdk/pull/28740/files/bb0fc0648242189d4f9a96ad8136c487299f38fe)

### Reviewing
<details><summary>Using <code>git</code></summary>

Checkout this PR locally: \
`$ git fetch https://git.openjdk.org/jdk.git pull/28740/head:pull/28740` \
`$ git checkout pull/28740`

Update a local copy of the PR: \
`$ git checko

---

> **生成时间**: 2026-03-25

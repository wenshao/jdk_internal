# JDK-8324514: ClassLoaderData::print_on should print address of class loader

## 基本信息

| 属性 | 值 |
|------|-----|
| **Issue** | [JDK-8324514](https://bugs.openjdk.org/browse/JDK-8324514) |
| **PR** | [#17534](https://github.com/openjdk/jdk/pull/17534) |
| **Author** | shipilev |
| **组织** | Amazon |
| **Created** | 2024-01-23 |
| **Merged** | 2024-01-24 |
| **审查天数** | 1 |
| **版本** | JDK 23 |
| **优先级** | P4 |
| **组件** | hotspot |
| **模块** | client |
| **代码变更** | +5 / -1 (1 files) |
| **审查者** | coleenp, kimbarrett, shipilev (3 人) |

## 描述

This looks like a simple diagnostic regression from [JDK-8201556](https://bugs.openjdk.org/browse/JDK-8201556). Instead of printing the address of the `OopHandle` slot, we really want to print the address of the classloader that handle holds. That was not intentional, right, @coleenp?

I think we can `peek()` into handle, but the whole reason for [JDK-8201556](https://bugs.openjdk.org/browse/JDK-8201556) seems to avoid touching the classloader oops when unloading, and `peek()` would do a GC barrier on it. We could specialize for `!unloading` path, but I think we still want to see what is in the handle. So, this PR introduces the "peek_raw" version that would compile straight to plain access, after the null check.

<!-- Anything below this marker will be automatically updated, please do not edit manually! -->
---------
### Progress
- [x] Change must be properly reviewed (1 review required, with at least 1 [Reviewer](https://openjdk.org/bylaws#reviewer))
- [x] Change must not contain extraneous whitespace
- [x] Commit message must refer to an issue

### Issue
 * [JDK-8324514](https://bugs.openjdk.org/browse/JDK-8324514): ClassLoaderData::print_on should print address of class loader (**Enhancement** - P4)


### Reviewers
 * [Kim Barrett](https://openjdk.org/census#kbarrett) (@kimbarrett - **Reviewer**)
 * [Coleen Phillimore](https://openjdk.org/census#coleenp) (@coleenp - **Reviewer**)


### Reviewing
<details><summary>Using <code>git</code></summary>

Checkout this PR locally: \
`$ git fetch https://git.openjdk.org/jdk.git pull/17534/head:pull/17534` \
`$ git checkout pull/17534`

Update a local copy of the PR: \
`$ git checkout pull/17534` \
`$ git pull https://git.openjdk.org/jdk.git pull/17534/head`

</details>
<details><summary>Using Skara CLI tools</summary>

Checkout this PR locally: \
`$ git pr checkout 17534`

View PR using the GUI difftool: \
`$ git pr show -t 17534`

</details>
<details><summary>Using diff file</summary>

Download this PR as a diff file:

---

> **生成时间**: 2026-03-25

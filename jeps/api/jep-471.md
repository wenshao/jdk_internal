# JEP 471: JSON Binding (JSON.B)

> **Status**: Preview | **Type**: Feature | **Component**: Java SE / API
>
> **Lead**: Dmitry Kornilov (Oracle)
>
> **JDK Version**: JDK 24 (2025-03)
>
> **Organization**: [Oracle](../../contributors/orgs/oracle.md)

---

## 概览

引入 JSON Binding API 预览，支持 Java 对象与 JSON 文档之间的映射。

---

## 目标

- 提供对象 -JSON 映射
- 支持注解配置
- 简化 JSON 处理
- 收集社区反馈

---

## 核心 API

### 序列化

```java
// 创建配置
JsonbConfig config = new JsonbConfig()
    .withFormatting(true)
    .withNullValues(false);
Jsonb jsonb = JsonbBuilder.create(config);

// 序列化对象
Person person = new Person("John", 30);
String json = jsonb.toJson(person);
```

### 反序列化

```java
// 反序列化为对象
Person person = jsonb.fromJson(jsonString, Person.class);

// 反序列化为列表
List<Person> people = jsonb.fromJson(jsonArray, 
    new JsonType<List<Person>>() {});
```

### 注解

```java
public class Person {
    @JsonbProperty("full_name")
    private String name;
    
    @JsonbTransient
    private String password;
    
    @JsonbDateFormat("yyyy-MM-dd")
    private LocalDate birthDate;
}
```

---

## 演进历程

| JDK 版本 | 状态 | 关键变化 |
|----------|------|----------|
| **JDK 24** | Preview | 初始预览 |

---

## 相关 JEP

| JEP | 标题 | 关系 |
|-----|------|------|
| [JEP 471](jep-471.md) | JSON Binding | 本 JEP |
| [JEP 353](jep-353.md) | JSON Processing API | JSON 处理基础 |
| [JEP 489](jep-489.md) | JSON Escaping | JSON 转义 |

---

> **文档版本**: 1.0
> **最后更新**: 2026-03-21

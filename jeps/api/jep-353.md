# JEP 353: JSON Processing API

> **Status**: Incubator | **Type**: Feature | **Component**: Java SE / API
>
> **Lead**: Joe Darcy (Oracle)
>
> **JDK Version**: JDK 12 (2019-03)
>
> **Organization**: [Oracle](../../contributors/orgs/oracle.md)

---

## 概览

引入 JSON 处理 API 孵化器，提供简单的 JSON 文档创建、解析和转换功能。

---

## 目标

- 提供 JSON 处理 API
- 支持 JSON 文档创建
- 支持 JSON 文档解析
- 收集社区反馈

---

## 核心 API

### 创建 JSON

```java
// 使用构建器
JsonObject obj = Json.createObjectBuilder()
    .add("name", "John")
    .add("age", 30)
    .add("city", "New York")
    .build();

// 使用工厂
JsonArray array = Json.createArrayBuilder()
    .add("item1")
    .add("item2")
    .build();
```

### 解析 JSON

```java
// 从字符串解析
JsonReader reader = Json.createReader(new StringReader(jsonString));
JsonObject obj = reader.readObject();

// 从流解析
JsonReader reader = Json.createReader(inputStream);
JsonArray array = reader.readArray();
```

### 访问 JSON

```java
// 访问对象
String name = obj.getString("name");
int age = obj.getInt("age");

// 访问数组
for (JsonValue value : array) {
    System.out.println(value.toString());
}
```

---

## 演进历程

| JDK 版本 | 状态 | 关键变化 |
|----------|------|----------|
| **JDK 12** | Incubator | 初始孵化器 |

---

## 相关 JEP

| JEP | 标题 | 关系 |
|-----|------|------|
| [JEP 353](jep-353.md) | JSON Processing API | 本 JEP |
| [JEP 471](jep-471.md) | JSON Binding | JSON 绑定 |
| [JEP 489](jep-489.md) | JSON Escaping | JSON 转义 |

---

> **文档版本**: 1.0
> **最后更新**: 2026-03-21

# JEP 452: Key Encapsulation Mechanism API

> **状态**: ✅ 正式版 (JDK 21) | **JEP 链接**: https://openjdk.org/jeps/452

---
## 目录

1. [概述](#1-概述)
2. [核心组件](#2-核心组件)
3. [使用示例](#3-使用示例)
4. [相关链接](#4-相关链接)

---


## 1. 概述

KEM API 提供密钥封装机制的标准接口，用于增强密钥交换安全性。

---

## 2. 核心组件

### KEM 参数

```java
import javax.crypto.KEM;

// 获取支持的 KEM 算法
KEM kem = KEM.getInstance("DHKEM");
```

### 封装与解封装

```java
// 发送方
KEM.Encapsulator enc = kem.newEncapsulator(publicKey);
KEM.Encapsulated encap = enc.encapsulate();
byte[] ciphertext = encap.encapsulation();
SecretKey sharedKey = encap.key();

// 接收方
KEM.Decapsulator dec = kem.newDecapsulator(privateKey);
SecretKey sharedKey = dec.decapsulate(ciphertext);
```

---

## 3. 使用示例

### 密钥交换

```java
import javax.crypto.KEM;
import javax.crypto.SecretKey;
import java.security.KeyPair;
import java.security.KeyPairGenerator;

// 生成密钥对
KeyPairGenerator kpg = KeyPairGenerator.getInstance("EC");
kpg.initialize(256);
KeyPair kp = kpg.generateKeyPair();

// KEM 封装
KEM kem = KEM.getInstance("DHKEM");
KEM.Encapsulator enc = kem.newEncapsulator(kp.getPublic());
KEM.Encapsulated encap = enc.encapsulate();

SecretKey sharedKey = encap.key();
```

---

## 4. 相关链接

- [JEP 官方文档](https://openjdk.org/jeps/452)
- [安全文档](/by-topic/security/security/timeline.md)

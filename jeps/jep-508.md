# JEP 508: Vector API (Tenth Incubator)

> **状态**: 🔧 孵化版 (JDK 25, 第10次孵化) | **JEP 链接**: https://openjdk.org/jeps/508

---

## 概述

Vector API 提供在支持 SIMD (单指令多数据) 指令的 CPU 上进行向量运算的能力。

---

## 核心类

### Vector<E>

```java
import jdk.incubator.vector.*;

static final VectorSpecies<Float> SPECIES = FloatVector.SPECIES_256;

void vectorComputation(float[] a, float[] b, float[] c) {
    int i = 0;
    int upperBound = SPECIES.loopBound(a.length);

    for (; i < upperBound; i += SPECIES.length()) {
        var va = FloatVector.fromArray(SPECIES, a, i);
        var vb = FloatVector.fromArray(SPECIES, b, i);
        var vc = va.mul(va).add(vb.mul(vb)).neg();
        vc.intoArray(c, i);
    }

    // 处理剩余元素
    for (; i < a.length; i++) {
        c[i] = -(a[i] * a[i] + b[i] * b[i]);
    }
}
```

---

## 孵化历史

- JDK 16: JEP 338 - 第 1 次孵化
- JDK 17: JEP 389 - 第 2 次孵化
- JDK 18: JEP 417 - 第 3 次孵化
- JDK 19: JEP 426 - 第 4 次孵化
- JDK 20: JEP 438 - 第 5 次孵化
- JDK 21: JEP 448 - 第 6 次孵化
- JDK 22: JEP 459 - 第 7 次孵化
- JDK 23: JEP 460 - 第 8 次孵化
- JDK 24: JEP 461 - 第 9 次孵化
- JDK 25: JEP 508 - 第 10 次孵化

---

## 相关链接

- [JEP 官方文档](https://openjdk.org/jeps/508)
- Vector API 指南

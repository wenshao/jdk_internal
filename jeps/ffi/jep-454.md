# JEP 454: Foreign Function & Memory API

> **状态**: ✅ 正式版 (JDK 22) | **JEP 链接**: https://openjdk.org/jeps/454

---
## 目录

1. [概述](#1-概述)
2. [核心组件](#2-核心组件)
3. [使用示例](#3-使用示例)
4. [相关链接](#4-相关链接)

---


## 1. 概述

FFM API 允许 Java 代码安全地与本地代码和内存交互，替代 JNI。

---

## 2. 核心组件

### Memory Segment

```java
try (Arena arena = Arena.ofConfined()) {
    MemorySegment segment = arena.allocate(100);
    segment.set(ValueLayout.JAVA_INT, 0, 42);
}
```

### Linker

```java
Linker linker = Linker.nativeLinker();
SymbolLookup stdlib = linker.defaultLookup();

MethodHandle strlen = linker.downcallHandle(
    stdlib.find("strlen").get(),
    FunctionDescriptor.of(ValueLayout.JAVA_LONG, ValueLayout.ADDRESS)
);
```

---

## 3. 使用示例

### 调用本地函数

```java
Linker linker = Linker.nativeLinker();
MethodHandle printf = linker.downcallHandle(
    linker.defaultLookup().find("printf").get(),
    FunctionDescriptor.of(ValueLayout.JAVA_INT, ValueLayout.ADDRESS, ValueLayout.ADDRESS)
);

int result = (int) printf.invokeExact("Hello, %s!\n", address);
```

### 分配本地内存

```java
try (Arena arena = Arena.ofConfined()) {
    MemorySegment segment = arena.allocate(1024);
    for (int i = 0; i < 1024; i++) {
        segment.set(ValueLayout.JAVA_BYTE, i, (byte) i);
    }
}
```

---

## 4. 相关链接

- [JEP 官方文档](https://openjdk.org/jeps/454)
- [Panama 项目](https://openjdk.org/projects/panama/)

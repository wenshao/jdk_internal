# JEP 455: Primitive Types in Patterns

> **状态**: 🔍 预览版 (JDK 25) | **JEP 链接**: https://openjdk.org/jeps/455

---

## 概述

模式匹配支持原始类型（primitive types），无需包装类。

---

## 核心特性

### instanceof 模式匹配

```java
// 之前：需要包装类型
if (obj instanceof Integer i) {
    int value = i.intValue();
}

// 现在：直接使用原始类型
if (obj instanceof int i) {
    int value = i;  // 无需拆箱
}
```

### switch 模式匹配

```java
String formatted = switch (obj) {
    case int i -> "int: " + i;
    case long l -> "long: " + l;
    case double d -> "double: " + d;
    case float f -> "float: " + f;
    default -> "unknown";
};
```

---

## 使用示例

### 嵌套模式

```java
record Point(int x, int y) {}

Object obj = new Point(1, 2);

if (obj instanceof Point(int x, int y)) {
    System.out.println("x=" + x + ", y=" + y);
}
```

### switch 复杂模式

```java
String result = switch (value) {
    case int i && i > 0 -> "positive int";
    case int i && i < 0 -> "negative int";
    case int i -> "zero";
    case double d -> "double";
    default -> "unknown";
};
```

---

## 相关链接

- [JEP 官方文档](https://openjdk.org/jeps/455)
- [JEP 530](jep-530.md) - 第2次预览

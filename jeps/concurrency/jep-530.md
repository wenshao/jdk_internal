# JEP 530: Primitive Types in Patterns, instanceof, and switch (Fourth Preview)

> **Status**: Closed / Delivered
> **Release**: JDK 26
> **Owner**: Angelos Bimpoudis
> **Issue**: [JDK-8359136](https://bugs.openjdk.org/browse/JDK-8359136)

## 概述

扩展模式匹配以支持原始类型，允许在 `instanceof` 和 `switch` 中使用原始类型模式。

## 关键特性

- 原始类型模式匹配
- `instanceof` 支持原始类型
- `switch` 支持原始类型模式
- 性能优化

## 示例

```java
// 原始类型 instanceof
if (obj instanceof int i) {
    System.out.println("Integer value: " + i);
}

// switch 中的原始类型模式
switch (value) {
    case int i -> System.out.println("int: " + i);
    case long l -> System.out.println("long: " + l);
    case double d -> System.out.println("double: " + d);
    default -> System.out.println("other");
}
```

## 演进历史

| 版本 | JEP | 状态 |
|------|-----|------|
| JDK 23 | JEP 455 | First Preview |
| JDK 24 | JEP 507 | Second Preview |
| JDK 25 | JEP 507 | Third Preview |
| JDK 26 | JEP 530 | Fourth Preview |

## 相关资源

- [OpenJDK JEP 530](https://openjdk.org/jeps/530)
- [Pattern Matching 指南](/guides/pattern-matching.md)
# JEP 505: Structured Concurrency (Fifth Preview)

> **状态**: 🔍 预览版 (JDK 25) | **JEP 链接**: https://openjdk.org/jeps/505

---

## 概述

结构化并发简化并发任务管理，确保任务生命周期在作用域内完成。这是第 5 次预览。

---

## 核心类

### StructuredTaskScope

```java
try (var scope = new StructuredTaskScope<Object>()) {
    Subtask<String> user = scope.fork(() -> fetchUser(id));
    Subtask<List<Order>> orders = scope.fork(() -> fetchOrders(id));

    scope.join();      // 等待所有子任务
    scope.throwIfFailed();  // 如果有失败则抛出异常

    return new Response(user.get(), orders.get());
}
```

### 策略变体

| 变体 | 行为 |
|------|------|
| `StructuredTaskScope<Object>` | 等待所有任务完成 |
| `ShutdownOnFailure()` | 第一个失败时取消其他 |
| `ShutdownOnSuccess()` | 第一个成功时取消其他 |

---

## 预览历史

- JDK 21: JEP 453 - 第 1 次预览
- JDK 22: JEP 462 - 第 2 次预览
- JDK 23: JEP 481 - 第 3 次预览
- JDK 24: JEP 505 - 第 4 次预览
- JDK 25: JEP 505 - 第 5 次预览

---

## 相关链接

- [JEP 官方文档](https://openjdk.org/jeps/505)
- [JEP 453](jep-453.md) - 第 1 次预览
- [JEP 462](jep-462.md) - 第 2 次预览
- [JEP 444](jep-444.md) - Virtual Threads
- [JEP 506](jep-506.md) - Scoped Values

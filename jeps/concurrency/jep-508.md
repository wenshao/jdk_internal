# JEP 508: Vector API (Tenth Incubator)

> **状态**: 🥚 孵化版 (JDK 25, 第10次孵化) | **JEP 链接**: https://openjdk.org/jeps/508

---
## 目录

1. [概述](#1-概述)
2. [核心类](#2-核心类)
3. [JDK 25 新特性](#3-jdk-25-新特性)
4. [孵化历史](#4-孵化历史)
5. [关键提交 (JDK 25)](#5-关键提交-jdk-25)
6. [平台支持](#6-平台支持)
7. [运行要求](#7-运行要求)
8. [性能示例](#8-性能示例)
9. [相关链接](#9-相关链接)

---


## 1. 概述

Vector API 提供在支持 SIMD (单指令多数据) 指令的 CPU 上进行向量运算的能力。

**第10次孵化** (JDK 25) 的主要变更：
- FFM API 集成开始 - 数学库支持迁移到 FFM API
- VectorShuffle 与 MemorySegment 交互
- VectorShape 公开访问
- 子字 gather 加载 API 重构

---

## 2. 核心类

### Vector<E>

```java
import jdk.incubator.vector.*;

static final VectorSpecies<Float> SPECIES = FloatVector.SPECIES_PREFERRED;

void vectorComputation(float[] a, float[] b, float[] c) {
    int i = 0;
    int upperBound = SPECIES.loopBound(a.length);

    for (; i < upperBound; i += SPECIES.length()) {
        var va = FloatVector.fromArray(SPECIES, a, i);
        var vb = FloatVector.fromArray(SPECIES, b, i);
        var vc = va.mul(va).add(vb.mul(vb)).neg();
        vc.intoArray(c, i);
    }

    // 处理剩余元素
    for (; i < a.length; i++) {
        c[i] = -(a[i] * a[i] + b[i] * b[i]);
    }
}
```

---

## 3. JDK 25 新特性

### 1. FFM API 集成

```java
// JDK 25+: 数学库开始使用 FFM API
import java.lang.foreign.*;

// Vector API 数学函数底层通过 FFM API 调用原生库
FloatVector v = FloatVector.fromArray(species, data, 0);
FloatVector sin = v.lanewise(VectorOperators.SIN);  // 使用 FFM API
```

### 2. VectorShuffle 与 MemorySegment

```java
import java.lang.foreign.*;

// JDK 25+: 从 MemorySegment 创建 Shuffle
MemorySegment segment = Arena.ofAuto().allocate(64, 1);
VectorShuffle<Integer> shuffle = VectorShuffle.fromArray(species, segment, 0);

// 应用 Shuffle
IntVector v = IntVector.fromArray(species, array, 0);
IntVector shuffled = v.rearrange(shuffle);

// 将 Shuffle 存储到 MemorySegment
shuffle.intoMemorySegment(segment, 0, ByteOrder.nativeOrder());
```

### 3. VectorShape 公开

```java
// JDK 25+: VectorShape#largestShapeFor 公开访问
VectorShape shape = VectorShape.largestShapeFor(int.class);
System.out.println("最大向量形状: " + shape);
```

---

## 4. 孵化历史

| 版本 | JEP | 状态 | 主要变更 |
|------|-----|------|----------|
| JDK 16 | 338 | First Incubator | 初始实现，基础 API |
| JDK 17 | 414 | Second Incubator | ARM SVE 支持，SVML 集成 |
| JDK 18 | 417 | Third Incubator | 旋转操作，无符号转换 |
| JDK 19 | 426 | Fourth Incubator | MemorySegment 支持 |
| JDK 20 | 438 | Fifth Incubator | 间接加载 |
| JDK 21 | 448 | Sixth Incubator | Float16 支持 |
| JDK 22 | - | Seventh Incubator | 性能优化 |
| JDK 23 | - | Eighth Incubator | 子字 gather |
| JDK 24 | - | Ninth Incubator | UMIN/UMAX |
| **JDK 25** | **508** | **Tenth Incubator** | **FFM 迁移开始** |

---

## 5. 关键提交 (JDK 25)

| JBS | 描述 | 变更量 |
|-----|------|--------|
| [8353786](https://bugs.openjdk.org/browse/JDK-8353786) | Migrate Vector API math library support to FFM API | 重大 |
| [8351993](https://bugs.openjdk.org/browse/JDK-8351993) | VectorShuffle access to and from MemorySegments | 新功能 |
| [8356634](https://bugs.openjdk.org/browse/JDK-8356634) | VectorShape#largestShapeFor should have public access | API |
| [8355563](https://bugs.openjdk.org/browse/JDK-8355563) | Refactor subword gather load API | 重构 |

---

## 6. 平台支持

| 平台 | 指令集 | 支持状态 |
|------|--------|----------|
| x86-64 | SSE/AVX/AVX2/AVX-512 | ✅ 完整 |
| AArch64 | NEON/SVE/SVE2 | ✅ 完整 |
| RISC-V | V 扩展 | ✅ 支持 |
| PowerPC | VSX | ✅ 支持 |
| s390x | Vector Facility | ✅ 支持 |

---

## 7. 运行要求

```bash
# 编译
javac --add-modules jdk.incubator.vector --release 25 VectorDemo.java

# 运行
java --add-modules jdk.incubator.vector VectorDemo
```

---

## 8. 性能示例

### SAXPY

```java
void saxpy(float[] a, float[] x, float[] y, float alpha) {
    VectorSpecies<Float> species = FloatVector.SPECIES_PREFERRED;
    int i = 0, bound = species.loopBound(a.length);

    for (; i < bound; i += species.length()) {
        FloatVector vx = FloatVector.fromArray(species, x, i);
        FloatVector vy = FloatVector.fromArray(species, y, i);
        vx.mul(alpha).add(vy).intoArray(a, i);
    }

    for (; i < a.length; i++) {
        a[i] = alpha * x[i] + y[i];
    }
}
```

### 点积

```java
float dotProduct(float[] a, float[] b) {
    VectorSpecies<Float> species = FloatVector.SPECIES_PREFERRED;
    FloatVector sum = FloatVector.zero(species);
    int i = 0, bound = species.loopBound(a.length);

    for (; i < bound; i += species.length()) {
        FloatVector va = FloatVector.fromArray(species, a, i);
        FloatVector vb = FloatVector.fromArray(species, b, i);
        sum = sum.add(va.mul(vb));
    }

    float result = sum.reduceLanes(VectorOperators.ADD);
    for (; i < a.length; i++) {
        result += a[i] * b[i];
    }
    return result;
}
```

---

## 9. 相关链接

- [JEP 508 官方文档](https://openjdk.org/jeps/508)
- [Vector API 主题文档](../../by-topic/core/vector-api/)
- [Vector API 时间线](../../by-topic/core/vector-api/timeline.md)
- [Vector API 使用指南](../../by-topic/core/vector-api/usage.md)
- [JEP 529 (JDK 26)](../language/jep-529.md) - 下一个版本

---

> **最后更新**: 2026-03-21
